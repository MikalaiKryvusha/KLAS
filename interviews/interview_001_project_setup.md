# Интервью #001 — Настройка проекта: репозиторий, лицензия, свобода замены стека

> Тема: закрыть владельческие пробелы, оставшиеся после деплоя KAIF (2026-07-02).
> Источник идеи: распаковка KAIF; TODO в `AGENT_GUIDE.md`.
> Статус: **🟡 ждёт ответов владельца**

## Контекст / что я уже выяснил

KAIF развёрнут, git-репозиторий инициализирован локально (только `main`). Удалённого репозитория нет —
без него невозможны push (сохранность истории вне ПК) и `/release`. Лицензия не указана. В `GOAL.md`
ты допускаешь, что текущий стек (llama.cpp + gemma-4-12b + Zoo Code) может быть заменён лучшим, — мне
нужно понимать границы моей автономии при работе над Фазами 1–3.

## ВОПРОСЫ

### Q1. Создать удалённый GitHub-репозиторий для этого проекта?
- **A) (рекомендую)** Да, **приватный** репозиторий на твоём GitHub-аккаунте — история и знания проекта
  переживут любой сбой ПК; наружу ничего не утекает. Я создам его через `gh repo create` и настрою push.
- **B)** Да, публичный — если хочешь делиться наработками.
- **C)** Нет, только локальный git — приемлемо, но история живёт лишь на этом диске.
- **D) свой ответ** —

**Ответ:**

### Q2. Лицензия проекта?
- **A) (рекомендую)** Без публичной лицензии (приватный личный проект) — просто ничего не добавляем;
  в `AGENT_GUIDE.md` запишу «private / all rights reserved».
- **B)** MIT — если когда-нибудь захочешь открыть проект, как KAIF.
- **C)** Другая открытая лицензия (напиши какая в D).
- **D) свой ответ** —

**Ответ:**

### Q3. Границы моей автономии при изменении ТЕКУЩЕГО стека (Фаза 1 — стабилизация)?
- **A) (рекомендую)** Настройки — свободно, состав — через интервью: я сам правлю параметры
  `gemma4-12b.bat` и профиля (контекст, порт, сэмплинг) с фиксацией в bugs/ и проверкой харнессом, но
  замену компонентов (движок/модель/фронтенд) предлагаю только через `/interview`. Быстро чинит
  стабильность, судьбоносные развилки остаются за тобой.
- **B)** Всё только через интервью — даже правки параметров bat-файла согласовывать с тобой.
- **C)** Полная свобода — включая замену модели/движка без согласования (не рекомендую: это
  владельческий выбор).
- **D) свой ответ** —

**Ответ:**

### Q4. Docker-сервисы в F:\LOCAL_NOMAD (kiwix, AnythingLLM, homepage, caddy) — их судьба?
- **A) (рекомендую)** Пока не трогаем, но разруливаем конфликты: я не удаляю и не переконфигурирую
  сервисы, но развожу конфликт порта 8080 (kiwix ↔ llama-server) на стороне llama-server; ревизия
  «что из этого нужно» — отдельным интервью после Фазы 2.
- **B)** Проведи ревизию сейчас: проверь, что запущено, и предложи, что удалить/оставить.
- **C)** Они мне все нужны как есть — учитывай их и не предлагай удалять.
- **D) свой ответ** —

**Ответ:**

## Предлагаемый план реализации (после ответов)

1. Q1: создать repo (`gh repo create … --private`), `git remote add origin`, push; вписать URL в
   `AGENT_GUIDE.md` (таблица идентичности).
2. Q2: вписать лицензию в `AGENT_GUIDE.md` (+ файл LICENSE, если открытая).
3. Q3/Q4: зафиксировать границы автономии в `AGENT_GUIDE.md` («Заметки от человека») и журнале решений
   `MASTER_PLAN.md`; продолжить Фазу 1 (баг 01) в этих границах.
