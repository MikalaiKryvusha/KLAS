# Bug 24 — анонимизация проверяет ровно то, что умеет чинить: валидатор подтверждает сам себя

**Статус:** 🔴 ОТКРЫТ. Найден 2026-08-01 при закрытии `bugs/23` — прогоном, а не чтением кода.
**Где:** `tools/anonymize.mjs` (`TEXT_EXT`, `REPLACEMENTS`, `PROBES`).
**Вопрос владельцу (без него баг не закрывается целиком):** `interviews/interview_008`.

## Почему баг стал видимым только сейчас

Пока мастер звал анонимизацию без `--apply` (`bugs/23`), она НИЧЕГО не делала — и её собственная
слепота не проявлялась. Как только вызов стал настоящим, стало видно: скрипт печатает
**«✅ личность автора не найдена — анонимность достигнута»** над копией, в которой личность автора
есть.

## Симптом (замер на свежем клоне после настоящего `--apply`)

| зонд | чем является | где выжил |
|---|---|---|
| `Mikalai Kryvusha` | имя автора | `LICENSE:3` — `Copyright (c) 2026 Mikalai Kryvusha` |
| `MikalaiKryvusha` | GitHub-хендл | `README.pdf` (двоичное зеркало README) |
| `krinikspc` | имя машины (funnel-хост) | `.gitignore:91` |
| `Кот Криник` | ник автора | `logo/klas-cat.svg:2` |
| `Криник` | одиночный ник | **8+ файлов**: `PROJECT_HISTORY.md`, `homeworks/02`, `ideas/05_Jarvis.md`, `plans/07`, `plans/08`, `plans/11`, `researches/07`, `interviews/interview_003`, весь каталог `ideas/psycho_acoustic_interface/` |

Валидатор при этом отрапортовал «чисто».

## Корневая причина — ОДНА, следствий два

**Валидатор смотрит ровно туда же, куда скраббер, и ищет ровно то, что скраббер умеет заменить.**
`PROBES` — подмножество `REPLACEMENTS`, а оба обхода фильтруются одним и тем же `TEXT_EXT`. Охранник,
который может лишь подтвердить сам себя (класс EXP-0042, «бумажный охранник»).

### Следствие A — охват по расширению

`TEXT_EXT` — белый список расширений. Файлы вне него не скрабятся И не проверяются:

- `LICENSE` — `path.extname('LICENSE') === ''`;
- `.gitignore` — у dotfile расширения тоже нет (`extname('.gitignore') === ''`);
- `logo/klas-cat.svg` — `.svg` в списке нет;
- `README.pdf` — двоичное ЗЕРКАЛО `README.md` (реестр пар в `AGENT_GUIDE.md`): источник обезличен,
  зеркало осталось прежним.

Отдельно: каталоги `homepage/` и `caddy/` целиком в `SKIP_DIRS`, и из `homepage/` вручную
обрабатываются ровно два файла — третий утечёт молча.

### Следствие B — правила `\b` на кириллице мертвы с рождения

`anonymize.mjs:57–58`:

```js
[/\bKrinik\b/g, ''],
[/\bКриник\b/g, ''],
```

`\b` в JS определён через `\w` = `[A-Za-z0-9_]`. У кириллического слова, окружённого пробелами или
знаками препинания, обе стороны — не-`\w`, значит **границы нет и совпадения не будет никогда**.
Проверено запуском, а не рассуждением:

```
[/\bКриник\b/g]                          не совпало  «Привет, Криник!»
                                         не совпало  «Кот Криник»
                                         не совпало  «см. Криник — владелец»
(?<!\p{L})Криник(?!\p{L}) с флагом u      СОВПАЛО во всех трёх
```

Правило `[/\bKrinik\b/g, '']` на латинице работает — но не вычищает само себя из
`tools/anonymize.mjs:57`, потому что в исходнике перед `K` стоит буква `b` (из `\b`), то есть
граница слова там как раз ЕСТЬ и текст «Krinik» оказывается частью `\bKrinik\b`.

## План лечения (одним заходом, после ответа владельца)

1. **Развести валидатор и скраббер.** `PROBES` обходят **всё дерево**, а не `TEXT_EXT`, и ищут по
   байтам — тогда `LICENSE`, `.gitignore`, `.svg` и `README.pdf` попадают под проверку, даже пока
   скраббер их не чинит. Тихая ложь превращается в громкий отказ. Это первый шаг: охранник обязан
   уметь видеть больше, чем чинит.
2. **Расширить охват скраббера:** файлы без расширения по ИМЕНИ (`LICENSE`, `.gitignore`,
   `.gitattributes`) + `.svg`.
3. **`README.pdf` — зеркало, а не файл:** после обезличивания источника пересобрать
   (`node tools/render-pdf.mjs --all`) или удалить из анонимной копии. Правило реестра пар:
   зеркало правится У ИСТОЧНИКА и пересобирается, никогда не подпиливается на месте.
4. **Починить кириллические правила:** `(?<!\p{L})Криник(?!\p{L})` с флагом `u` вместо `\b`.
   Добавить `Криник` и `Krinik` в `PROBES` — сейчас их там нет, поэтому валидатор и молчал.
5. **Охранник на класс:** самопроверка `anonymize.mjs --selftest` на фикстуре, где каждый зонд
   встречается в файле КАЖДОГО вида (с расширением, без расширения, dotfile, svg, двоичный), —
   и она обязана краснеть до фикса.

⚠️ Пункт про `LICENSE` в лечение НЕ входит: см. вопрос владельцу.

## Что решает владелец

MIT требует сохранять уведомление об авторском праве в копиях, а замысел анонимной установки
(`ideas/07`) — «по файлам проекта нельзя установить, кто автор». Это конфликт уровня видения, и
агент его не решает. Развилка вынесена в `interviews/interview_008`.

## Ссылки

`bugs/23` (его фикс сделал этот баг видимым) · `bugs/21` · EXP-0042 · EXP-0049 ·
`interviews/interview_008` · README §3.5, §10.3
