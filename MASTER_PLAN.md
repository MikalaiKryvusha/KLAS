# KLAS — Krinik Local Agent System — MASTER PLAN

> Дорожная карта: как мы попадаем из **текущего состояния** проекта к видению в `GOAL.md`.
> Высокоуровневая пошаговая декомпозиция — фазы и вехи, не ежедневные задачи (те живут в `plans/`).
> Derived из `GOAL.md` агентом при деплое (2026-07-02); обновляется скиллом `/revision`, когда цель
> или состояние меняются. Последняя ревизия: **2026-07-17** (реальность после релиза 1.0 + эпик
> ассистента + стратегия «конструктор из OSS-модулей»).
>
> **Живой справочник**, не задача — никогда не помечается DONE.

---

## Видение (одной строкой)

**KLAS** — self-hosted экосистема агентского ИИ на личном ПК: локальная LLM на геймерской GPU +
автономные агенты + веб-дашборд + оффлайн-база знаний (википедия), стабильная, «спящая до вызова»,
максимально близкая к опыту Claude AI в пределах RTX 5070 Ti 16 GB. Горизонт по идеям владельца:
голосовой человекоподобный ассистент **Jarvis/Joi** (идея 05) поверх этого фундамента.

## Руководящие принципы

- **Стабильность > ум > скорость** — порядок приоритетов владельца (`GOAL.md`); ошибки переполнения
  контекста недопустимы в принципе.
- **Минимум сущностей** (KISS + Оккам, `PHILOSOPHY.md`): в идеале один движок и одна модель; каждый
  новый компонент должен доказать необходимость.
- **Сначала измерь, потом меняй:** каждое изменение стека проверяется харнессом (health, `/props`,
  бенчмарк, VRAM, agent-bench v1/v2/v3), а не «на глазок».
- **Исследование прежде покупки сложности:** лучшие практики ищутся в вебе и фиксируются в
  `researches/`, решения о смене стека — через `/interview`.
- **Конструктор из крупных OSS-модулей** (владелец, 2026-07-16): не переписываем то, что уже решено
  открытым миром, — собираем KLAS из готовых живых модулей. Карта «подсистема → готовый модуль» —
  `researches/09_oss_constructor_map.md`; свои пишем только уникальные куски (например, ротация
  памяти по лимиту ГБ).
- **Живость OSS обязательна** (владелец, 2026-07-16): протухшие/архивные проекты не брать — только
  живые (свежие коммиты, активное комьюнити, звёзды); при прочих равных выбирать самый живой.
- **Технологии:** JS (.mjs + Node) для скриптов, инструментов, бекенда и БД; для
  realtime-аудио/ML-кусков язык выбирается по поддержке и сообществу (интервью 003, Q5-принцип).

## Отсюда — туда: путь по фазам

### Фазы 0–6 — Фундамент → рабочий KLAS-сервер ✅ (закрыты релизом 1.0, 2026-07-04)

Свернуто как история; детали — `STATUS.md` («Что сделано») и `plans/02–06 (DONE)`.

- **0 Фундамент** ✅ KAIF развёрнут, `GOAL.md` → план, аудит окружения, git.
- **0.5 Рождение KLAS** ✅ дом `F:\KLAS\`, репозиторий-каркас, логотип, первый push, MIT.
- **1 Стабилизация стека** ✅ баг 01 закрыт и подтверждён владельцем (порт 8080 разведён с kiwix,
  профили исправлены); харнесс `health-check.ps1` / `bench-model.ps1`.
- **2 Исследование оптимального стека** ✅ `researches/02` (+03/04/05); интервью 002: основная —
  **Qwen3.5-35B-A3B** (98K через KV q8_0), llama-swap — находка. Хвост (бенчи новых кандидатов)
  ушёл в непрерывный процесс (см. ниже).
- **3 Развёртывание целевого стека** ✅ 5 моделей в `llama-swap/config.yaml`, профили
  прокомментированы, agent-bench v1/v2/v3 меряют ум; Qwythos-9B — спец long-context (256K).
- **4 «Спит, пока не позовут»** ✅ llama-swap: автозагрузка по запросу ~5.5 с, ttl-автовыгрузка
  подтверждена; тихий фон без окон; автостарт ставится владельцем (`tools/install-autostart.ps1`).
- **5 Пульт + база знаний** ✅ единый вход через Caddy: homepage-пульт (`http://localhost/`),
  llama-swap UI `/ui/`, Kiwix `/wiki/`; Kiwix-поиск доступен агентам и из чатов (идея 11).
- **6 KLAS-сервер для владельца и близких** ✅ = **Релиз 1.0 «Universal KLAS»**: чат Open WebUI
  «под ключ» для родных, удалённый доступ (Tailscale Funnel + basicauth, LLM-API с Bearer),
  трей «Кот Криник» (up/down всего стека), умный мастер-установщик, секреты ротированы.

### Фаза 7 — Самораскрытие «clone + deploy» до идеала 🔧

- **Цель фазы:** идея 03 доведена до конца — развёртывание KLAS на пустом ПК = `git clone` + одна
  команда, которая сама скачивает тяжёлое и настраивает всё; повторный прогон идемпотентен.
- **Шаги:** каркас `tools/deploy.mjs` + манифест ✅ (`plans/02 DONE`); мастер-установщик
  `tools/install.mjs` ✅ (релиз 1.0); осталось: пост-настройка (автостарт по решению владельца,
  финальный health-check в `--apply`), боевой `--apply`-прогон, решить судьбу конфигов homepage в git
  (`homepage/config/services.yaml`). Хвосты — в пуле автономного беклога `STATUS.md`.
- **Статус:** 🔧 близко к готовности (хвосты в автономном пуле)

### Фаза 8 — Эпик ассистента: Jarvis/Joi конструктором из OSS 🔬→🔧

- **Цель фазы:** голосовой человекоподобный ассистент (идея 05: персоны Jarvis/Joi, навыки-CRUD,
  память с ротацией, управление Windows, зрение) собран из **крупных живых OSS-модулей** поверх
  ядра KLAS. Секвенс эпика — `plans/08_assistant_epic_roadmap.md`; карта модулей — `researches/09`.
- **Шаги (порядок = решения интервью 004):**
  1. **Выбор агентного ядра ✅ (2026-07-17): OpenClaw принят ядром** (решение владельца; смоук +
     мини-бенч цепочек + дожим write-дефекта — research 09 §3.4). Выкат ступенями —
     `plans/10_openclaw_core_rollout.md`: ступень 0 ✅ (рабочий профиль, exec-allowlist, гейтвей
     выкл); ступень 1 (гейтвей в tailnet + подтверждения) — следующая, через `/interview`.
  2. **Голосовой тракт (русский):** каркас Pipecat; STT sherpa-onnx + GigaAM-v3; TTS-старт Silero v5,
     затем слепой A/B клонов (CosyVoice 3 / Chatterbox V3 / F5-TTS_RUSSIAN) на сэмплах владельца —
     `plans/07_jarvis_gateway_and_client.md` (Jarvis Gateway поверх Tailscale, голос только tailnet).
  3. **Клиент следует за ядром** (Q2 инт. 004): OpenClaw-бекенд → его Android-нода; иначе — форк HA
     Companion (инт. 003 Q4).
  4. **Полномочия над Windows — ступенчато с allow-list** (Q4 инт. 004): Windows-MCP (UIA-дерево
     сначала, зрение — fallback); exec-approvals, гейтвей только в tailnet.
  5. **Идентичность/доверие/duress** — `plans/09_jarvis_identity_trust.md` (каркас доверия можно рано).
  6. **PAI** (психоакустика, режим A + тренажёр) — после основы (инт. 003 Q3);
     `ideas/psycho_acoustic_interface/`.
- **Развилки владельца:** выбор ядра (после мини-бенча), голоса персон (A/B + сэмплы), каждый шаг
  эскалации полномочий Windows — через `/interview`.
- **Статус:** 🔬 исследования и пилот готовы; реализация — по шагу 1

### Непрерывные процессы (не фазы — идут всегда)

- **Эволюция ядра LLM:** регулярный (раз в 1–2 недели) поиск свежих моделей ≤16 GB VRAM (идея 06),
  прогон `bench-model` + agent-bench v2/v3, таблицы в `researches/02`. Текущая рекомендация бенчей:
  gemma-4-12b — дейли-драйвер агентики, Qwythos-9B — long-context, qwen3.5-35b — основная универсальная.
- **Ревизия OSS-поля:** карта `researches/09` освежается при значимых сдвигах; правило живости —
  перед каждым заимствованием.
- **Гигиена беклога:** `/check-backlog` в автоциклах; DONE-теги синхронны содержимому.

## Журнал решений

| Дата | Решение | Почему |
|------|---------|--------|
| 2026-07-02 | Развернуть KAIF v1.1 (русский, Claude Code) | Запрос владельца; управляемость проекта |
| 2026-07-02 | Git workflow: только `main`, частые коммиты | Простота (KISS), дефолт KAIF |
| 2026-07-02 | Фаза 1 (стабилизация) раньше Фазы 2 (исследование) | Приоритет №1 владельца — стабильность; рабочий baseline нужен для честного сравнения кандидатов |
| 2026-07-02 | Ребрендинг в KLAS, дом `F:\KLAS\` (Фаза 0.5) | Решение владельца: своя система, свой дом, репозиторий MikalaiKryvusha/KLAS (см. `GOAL.md`) |
| 2026-07-02 | Раскладка F:\KLAS: корень = git-репозиторий (каркас: документы, конфиги, скрипты), инфраструктура — поддиректории рядом, вне git | KISS + идея 03: репозиторий — обёртка/фрейм |
| 2026-07-02 | Идея 03 владельца: KLAS = одна единица, развёртывание «clone + deploy», тяжёлое deploy скачивает сам | `ideas/03_klas_single_unit_self_deploy.md`; теперь Фаза 7 |
| 2026-07-02 | Идея 04 владельца: следы разовой стартовой миграции вычищены из проекта — фокус только на видении KLAS | `ideas/04_DONE_forget_local_nomad.md` (✅ исполнена 2026-07-03) |
| 2026-07-03 | Идея 05 владельца: Jarvis — голосовой человекоподобный ИИ-ассистент KLAS (на будущее, после основы) | `ideas/05_Jarvis.md`; теперь Фаза 8 |
| 2026-07-03 | Решение владельца: СНАЧАЛА исследование Фазы 2 (лучший стек на июль 2026), ПОТОМ настройка и стабилизация выбранного | Не тратить силы на тюнинг связки, которую исследование может заменить |
| 2026-07-03 | Основная модель — Qwen3.5-35B-A3B (контекст 98K, KV q8_0); Qwythos-9B — спец long-context | Интервью 002 (A/A/A) + бенчи `researches/02` |
| 2026-07-04 | Релиз 1.0 «Universal KLAS»: чат родным, установщик, трей, безопасность | Фазы 1–6 сошлись в готовый продукт; `plans/06 DONE` |
| 2026-07-07 | Фокус на ЯДРЕ, ассистент — закладками (web-тул в homepage, обе персоны Jarvis/Joi, PAI только режим A, клиент — форк готовой базы, голос только tailnet) | Интервью 003 (Q1–Q5) |
| 2026-07-16 | Стратегия «конструктор из крупных OSS-модулей» подтверждена; правило живости OSS; смоук-пилот OpenClaw перед выбором ядра ассистента | Вопрос владельца + `researches/09` + интервью 004 |
| 2026-07-17 | `/revision`: фазы 1–6 закрыты релизом 1.0 и свёрнуты в историю; новые фазы 7 (самораскрытие) и 8 (эпик ассистента); эволюция ядра LLM — непрерывный процесс | План отставал от реальности (релиз 1.0, эпик, конструктор) |
| 2026-07-17 | **OpenClaw принят агентным ядром ассистента** («принято, выходим из пилота»); выкат ступенями, ступень 0 выполнена (рабочий профиль, exec-allowlist, гейтвей выкл) | Решение владельца по данным: смоук + мини-бенч цепочек (qwen 5/6) + дожим write-дефекта — research 09 §3.4; план `plans/10` |

---

> **Поддержка:** держи этот план в согласии с реальностью. Когда `GOAL.md` или состояние проекта
> существенно сдвигаются — запусти `/revision` для переработки фаз. Детальные планы шагов каждой
> фазы живут в `plans/`.
