---
# For configuration options and examples, please see:
# https://gethomepage.dev/configs/settings/

# Пульт управления KLAS (Фаза 5, идея 08)
title: KLAS — Пульт управления
headerStyle: clean

providers:
  openweathermap: openweathermapapikey
  weatherapi: weatherapiapikey

# Исправление ошибки Host validation failed
disableHostValidation: true

# Дополнительные настройки для работы в Docker на Windows
allowIframe: true
host: 0.0.0.0

# base НЕ задаём: homepage — корневое приложение (ассеты идут от /), а за Caddy/funnel пульт
# смонтирован на КОРНЕ (баг 02). Прежнее base=https://.../homepage было неэффективно и путало.