# План 03 — Идея 07: удалённый доступ, контекст, Qwythos, KAIF 1.2, аноним-деплой, релиз 0.1

> Реализует `ideas/07_remote_api_access.md`. Статус: 🔧 в работе (начато 2026-07-03).
> Порядок — по убыванию ценности для владельца (он прямо сейчас вайб-кодит и страдает от контекста).

## Приоритет A — КОНТЕКСТ (главный крик владельца, дёшево, немедленно)

Диагноз: модели «не помнят минуту назад» = мал контекст + KV-бюджет. qwen3.6 падает 400 (ctx 32768).
- **A1.** qwen3.6-27b: поднять контекст (KV q8_0), чтобы 50K+ запросы не падали; замерить VRAM.
- **A2.** qwen3.5-35b (основная): максимизировать контекст; при запасе скорости (155 t/s) — не жалко.
- **A3.** Проверить каждую модель needle-тестом на новом контексте (`agent-bench`), VRAM ≤ 16 ГБ.
- Критерий: ни одного 400; контекст у основной ≥ прежних 98K, у qwen3.6 ≥ 64K.

## Приоритет B — Qwythos-9B 1M (ГЛАВНАЯ новая модель)

9B Q5_K_M (~6.5 ГБ) → под KV остаётся ~9 ГБ, реально претендует на огромный контекст.
- **B1.** Профиль в llama-swap: `-c` максимально (цель 1M; реально — сколько влезет в VRAM с KV q8_0),
  проверить фактический `n_ctx` через `/props`.
- **B2.** Бенч скорости (`bench-model.ps1`) + ум (`agent-bench.mjs`) + needle на БОЛЬШОМ контексте.
- **B3.** Занести в `researches/02` таблицу; если хороша — кандидат в основные (интервью/решение).

## Приоритет C — Удалённый API-доступ (Zoo Code через интернет)

Инфраструктура уже есть: Caddy (:443, basicauth) + Tailscale Funnel (`tailscale_funnel_443.bat`).
- **C1.** Проксировать llama-swap (127.0.0.1:8080) через Caddy на отдельном пути (напр. `/llm/*` или
  поддомен), с той же basicauth (адрес + пароль — как просил владелец, без логина в Tailscale).
- **C2.** Поднять Tailscale Funnel на :443 → публичный `https://<машина>.ts.net` URL.
- **C3.** Инструкция для Zoo Code: Base URL = публичный URL + путь, ключ = пароль; проверить с телефона/
  другого хоста. Документировать в `researches/` или homeworks.
- ⚠️ Внешний доступ = владельческое решение — оно уже принято в идее 07 (владелец прямо просит).

## Приоритет D — KAIF 1.2 (обновление + проверка после Zoo Code)

Ущерб от Zoo Code проверен 2026-07-03: только `.roo/skills/.gitkeep`, отслеживаемое цело, откат не нужен.
- **D1.** Скачать `KAIF.md` 1.2 из origin (github MikalaiKryvusha/KAIF).
- **D2.** Обновить фреймворк через `/kaif-update` (уважительная миграция, сохранить кастомизации KLAS).
- **D3.** `npm run kaif:check`, обновить `.kaif/kaif.json` → 1.2; зафиксировать что нового в 1.2
  (особенно метод анонимного разворачивания — вход в приоритет E).

## Приоритет E — Анонимное разворачивание KLAS

- **E1.** `tools/deploy.mjs --anon` (или отдельный `tools/anonymize.mjs`): вычистить из ВСЕХ файлов
  имя автора (Николай Кривуша / Mikalai Kryvusha / KOT KRINIK / kotkrinik) и ссылки на его GitHub.
- **E2.** Убрать git remote origin, удалить `.git`-историю (или reinit) — проект больше не тянется из
  origin; заменить каноническую идентичность на обезличенную (KLAS без автора).
- **E3.** Идемпотентность и dry-run; тест на копии (не на живом репозитории!).

## Приоритет F — Датасеты и лидерборды (учёба, гуглёжкой; НЕ deep-research)

- **F1.** `researches/05`: что такое датасеты, разбор `Glint-Research/Fable-5-traces`, польза для KLAS.
- **F2.** Изучить HF leaderboard-data-guide — есть ли живой лидерборд свежих моделей; связать с идеей 06.

## Приоритет G — Финализация и релиз MVP 0.1

- **G1.** Снос наследия: LOCAL_NOMAD (46 ГБ), ollama (удалить + автозапуск). Отмашка владельца есть.
- **G2.** Автостарт llama-swap (persistence разрешён владельцем) — живучесть после ребута.
- **G3.** `deploy.mjs`: финальный health-check в конце `--apply`; манифест дополнить Qwythos и т.п.
- **G4.** README: секции «Развернуть» (обычно/анонимно) + «Удалённый доступ».
- **G5.** `/release` → тег `v0.1`, GitHub Release «First KLAS».

## Принципы (KISS + видение)

- Тяжёлое (модели, .zim, сторонние модули) — НЕ в git: докачивается скриптами при деплое (идея 03).
- Контекст важнее скорости сейчас (прямой запрос владельца) — но стабильность (влезаемость в VRAM) №1.
- Каждое изменение стека — замером на харнессе, не на глазок.
