# План 19 — АКТИВАТОРЫ «Джарвис» и «Джой»: обучить два русских слова-ключа

**Статус:** 🔧 в работе (заведён 2026-07-31, вечер)
**Родитель:** `plans/08_assistant_epic_roadmap.md` (эпик ассистента) · зонтик тракта — `plans/07`
**Разведка (ступень 1 лестницы, закрыта):** `researches/16_wake_word.md` — золотой стандарт сферы,
живые OSS, два замера с отрицательным результатом, порядок работ §4.

> 🧭 **Якорь мета-плана.** `plans/08` строка «Wake-word движок: дефолт **openWakeWord** (пресет
> «Hey Jarvis»); проверить на Блоке 5». **Якорь устарел в части пресета и это уже проверено:**
> пресет `hey_jarvis_v0.1` дал 0/10 на русском произношении (`researches/16` §3.2). Движок остаётся,
> пресет заменяется двумя СВОИМИ обученными моделями. Строка мета-плана правится при закрытии этого
> плана, не раньше — чтобы правка опиралась на результат, а не на намерение.

---

## Заказ владельца дословно

> *«сейчас важно сделать запуск по активационным фразам-именам: Джарвис и Джой»*

Плюс более раннее (`researches/16` §5): *«Имена активаторы: Джарвис. Джои.»* и снятие ограничения по
ресурсам — *«хоть всю видюху занимай на сколько хочешь часов»*.

## Определение готовности (измеримо, а не «работает у меня»)

Обе модели детектируют своё имя на **живом микрофоне владельца** с долей срабатываний, сравнимой с
контролем, и **не зажигают чужой детектор**. Метрики сферы — доля верных срабатываний (TPR) и число
ложных срабатываний в час (FA/h); ориентир openWakeWord — 97%+ TPR при <1 FA/h.

Замер обязан идти на **той же фикстуре**, что §3.1–3.2 разведки, — иначе новые числа не сравнить с уже
полученными 3/6 и 0/10.

---

## ⛔ Что закрыто замером — НЕ переоткрывать

| путь | вердикт | чем доказано |
|---|---|---|
| sherpa-onnx `KeywordSpotter` (без обучения, списком BPE-токенов) | ⛔ потолок **3/6** на русском | развёртка 16 рабочих точек, `researches/16` §3.1 |
| готовая `hey_jarvis_v0.1` из openWakeWord | ⛔ **0/10** | `researches/16` §3.2; голого «Джарвис» среди готовых слов нет вообще |
| Porcupine вместо openWakeWord | ⛔ отвергнут по видению | требует облачный AccessKey от Picovoice ⇒ бьёт по «приватность и офлайн» |

Нулю §3.2 можно верить: рядом стоял **положительный контроль** — «Alexa.» настоящей английской
моделью зажгла свой детектор на 1.000, не тронув чужой (EXP-0027). Стенд исправен, нули — свойство
модели.

---

## 🔎 Разведка окружения 2026-07-31 (вечер) — что уже стоит и сколько это стоит

Владелец занят стримом ⇒ GPU не трогаем. Проверялось ровно то, что проверяется бесплатно.

| что | состояние | как проверено |
|---|---|---|
| `openwakeword` **0.6.0** в `voice\venv-wakeword` | ✅ стоит, подмодуль `train` на месте | `importlib.metadata` + `pkgutil.iter_modules` |
| `torch` в этом venv | ❌ **нет** — обучение без него не пойдёт | `import torch` → ImportError |
| ONNX-фичи в комплекте пакета | ✅ `melspectrogram.onnx` (1.04 МБ) + `embedding_model.onnx` (1.27 МБ) | листинг `resources/models` |
| **Русские голоса Piper (ONNX)** | ✅ **четыре**: `denis`, `dmitri`, `irina`, `ruslan` — все medium | листинг `voice\models\vits-piper-ru_RU-*` |
| Синтез этими голосами на CPU | ✅ уже работает в проекте | `tools/voice/kws-probe.mjs` синтезирует через `sherpa.OfflineTts`, `provider: 'cpu'` |
| `silero_vad.onnx` (следующий слой) | ✅ 0.61 МБ, скачан | `voice\models\silero_vad.onnx` |
| Живость openWakeWord | ⚠️ не архив, 2602 ★, но `pushed_at` = **2025-12-30** (8 мес. без push) | `gh api repos/dscripka/openWakeWord` |

### ⭐ Две грабли из разведки, которые оказались обходимыми

Обе снимают с задачи вес, который `researches/16` ей приписывала. Проверено чтением кода, а не
предположено:

1. **TensorFlow 2.8.1 НЕ нужен.** `openwakeword[full]` тянет `tensorflow-cpu==2.8.1` + `onnx-tf==1.10.0`
   (набор 2022 года, на свежем Python не встанет). Но `train.py:577–579` импортирует `tensorflow`
   **внутри функции** `convert_onnx_to_tflite`, а экспорт в ONNX (`export_to_onnx`, строки 144 и
   426–430) — это чистый `torch.onnx.export`. Наш рантайм ONNX-овый ⇒ ветка tflite нам не нужна,
   и вместе с ней не нужен весь древний хвост зависимостей.
2. **Linux НЕ обязателен.** Утверждение разведки «автоматическое обучение только на Linux» росло из
   зависимости от `piper-sample-generator`. Мы генерируем корпус ИНАЧЕ — своим уже стоящим
   `sherpa-onnx` с четырьмя Piper-ONNX, — а `train.py` это torch, он кроссплатформенный.
   ⇒ WSL2 из плана выпадает, минус одна среда и минус целый класс граблей 9.5.

**Следствие для ресурсов:** генерация корпуса — CPU/ONNX; обучение — маленькая голова над
предвычисленными эмбеддингами. **Гипотеза: вся задача помещается на CPU и не конкурирует со стримом
владельца.** `[NOT-TESTED]` — доказывается замером в шаге 2, а не этим абзацем.

---

## ❓ Открытый вопрос ВЛАДЕЛЬЦУ — блокирует ТОЛЬКО вторую модель

**«Джои» или «Джой»?** Канон расходится сам с собой, и это надо развести до генерации корпуса:

| источник | написание | дата |
|---|---|---|
| `STATUS.md`, таблица «Канон владельца» | **«Джой»** | 07-29 |
| `researches/16` §5, дословная цитата владельца | **«Джои»** | 07-29 |

От написания зависит фонетика тысяч синтезированных клипов: «Джой» — один слог, «Джои» — два.
Переучивать дороже, чем уточнить. **«Джарвис» разночтения не имеет** ⇒ работа по нему не блокируется
и идёт первой.

Ещё три вопроса ждут своей очереди (перенесены из `researches/16` §5, туда же вернуть ответы):
поведение детектора, пока ассистент говорит сам · нужен ли индикатор «услышал» · порог срабатывания.

---

## Шаги

### Шаг 1 — Генератор корпуса (CPU, без GPU) 🔧

- [ ] `tools/voice/wakeword-corpus.mjs`: синтез слова четырьмя голосами Piper × развёртка по темпу ×
      обрамления (слово отдельно, в начале фразы, внутри фразы) → тысячи клипов 16 кГц.
- [ ] **Негативы обязательны** и должны включать фонетические ловушки («джазовая», «джем», «Джарвис»
      внутри чужого слова): детектор, срабатывающий всегда, «находит» слово со 100% точностью.
- [ ] Замерить реальную цену: клипов в минуту на CPU, размер корпуса на диске. Число в этот документ.
- [ ] Охранник корпуса по образцу `piper-dataset-guard.mjs`: пустой корпус — это ПРОВАЛ, а не «0 проблем»
      (класс EXP-0042, оплачен пятью бумажными охранниками).

### Шаг 2 — Обучение двух моделей ⏳

- [ ] Доставить в `voice\venv-wakeword` минимальный набор: `torch`, `torchaudio`, `torchinfo`,
      `torchmetrics`, `speechbrain`, `audiomentations`, `torch-audiomentations`, `pronouncing`,
      `acoustics`, `mutagen`. **Без `tensorflow`/`onnx-tf`** (обоснование выше).
- [ ] Обучить «Джарвис»; экспорт `export_to_onnx`, а не `to_tflite`.
- [ ] Обучить второе имя — ПОСЛЕ ответа владельца о написании.
- [ ] Зафиксировать, пошло ли обучение на CPU и за какое время. Не пошло — тогда GPU, по слову владельца.

### Шаг 3 — Замер на той же фикстуре ⏳

- [ ] Прогнать `tools/voice/wakeword-probe.py` на новых моделях: те же клипы, что дали 3/6 и 0/10.
- [ ] **Контроль обязателен** — «Alexa.» настоящей моделью должна по-прежнему давать 1.000. Без него
      ноль неотличим от сломанного стенда (EXP-0027).
- [ ] Перекрёстная проверка: «Джарвис» не зажигает детектор «Джой» и наоборот.

### Шаг 4 — 🏠 Живая проверка микрофоном владельца ⏳

Синтетика судит МЯГЧЕ реальности — в §3.2 даже настоящий английский синтез дал лишь 0.259 при пороге
0.5. Домашка в `homeworks/`, вердикт даёт владелец.

### Шаг 5 — Активатор открывает сессию вместо Enter ⏳

Прототип поверх `voice-talk`; слово выбирает и голос персоны, и системный промпт (две модели — это
фича переключения персон, а не дублирование).

---

## Риски (по ярусам, `PHILOSOPHY.md` → Мёрфи)

- **(а) Высший — синтетика обманет.** Корпус из четырёх TTS-голосов не покрывает акустику живого
  владельца и его комнаты. Защита: шаг 4 обязателен, положительный синтетический результат — зелёный
  свет к живой проверке, а НЕ вердикт.
- **(а) Высший — детектор услышит себя из динамиков.** Пока ассистент говорит, его собственная речь
  идёт в микрофон (EXP-0017). AEC у нас нет. Дешёвое честное лечение: глушить детектор на время
  собственной речи — но это вопрос владельцу, а не решение агента.
- **(б) Правдоподобный — обучение упрётся в CPU.** Тогда ждём свободной машины: ресурс владельцем уже
  разрешён, просто не сегодня.
- **(б) Правдоподобный — openWakeWord 8 месяцев без push.** Правило живости владельца нарушено де-факто.
  Смягчение: рантайм у нас свой (ONNX через onnxruntime), от их кода в бою зависит только формат модели.
- **(в) Мелкий — 124 открытых issue** в апстриме; ни одна пока нас не касалась.

## Правила, которые этот план обязан соблюдать

- **Фикстура замера не меняется** — иначе новые числа не сравнимы с 3/6 и 0/10.
- **Каждый охранник проверяется на сломанной версии** до того, как верить его зелёному.
- **Агент не судит качество распознавания на слух** — судит счётчик, а финальный вердикт даёт владелец
  на живом микрофоне.
- **GPU не трогать, пока владелец не сказал** (стрим, вечер 2026-07-31).

## Ссылки

`researches/16_wake_word.md` (разведка и оба отрицательных замера) · `interviews/interview_007` Q3 ·
`ideas/05_Jarvis.md` (персоны и имена) · `plans/07` · `plans/08` · EXP-0016 · EXP-0017 · EXP-0027 ·
EXP-0042 · `bugs/18` (охранник корпуса как образец жанра)
