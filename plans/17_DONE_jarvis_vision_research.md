# План 17 — Быстрое и надёжное ЗРЕНИЕ для Jarvis/Joi (задание на разведку)

> **Статус: ✅ DONE (2026-07-29) — разведка выполнена, документ на месте.**
> Заказанный артефакт — **`researches/11_agent_vision.md`** — написан по обязательному составу
> (золотой стандарт · научная база · живые OSS · решение для KLAS · вопросы владельцу).
> Задание было именно на разведку, поэтому его выполнение закрывает план; реализация каскада —
> отдельная работа, которая ляжет в новый план по решению владельца (§5 разведки).

## ✅ STATUS: DONE (2026-07-29)

**Что дала разведка — три вывода, ради которых она и делалась:**

1. **Зрение — это ТРИ слоя, а не один** (захват → локализация → чтение), и путать их дорого:
   у каждого свой эталон и своя цена.
2. **Вердикт индустрии — каскад, а не выбор.** Дерево доступности (UIA) и пиксели не конкурируют:
   дешёвое детерминированное дерево идёт первым, визуальный grounding закрывает его дыры. Для KLAS
   предложен каскад из трёх ступеней, где VLM (единственное, что ест VRAM) вызывается редко и
   осознанно — тот же принцип «тяжёлое по требованию, лёгкое всегда», который в тот же день задал
   владелец по голосу.
3. **🐞 Найдено ограничение НАШЕГО инструмента, которого никто не знал:** `tools/screen-rec.mjs`
   снимает экран через **`gdigrab`**, а он **не видит аппаратно-ускоренные окна** (GPU-рисованное
   минует конвейер GDI → чёрный или битый кадр). То есть у «зрения» KLAS есть класс окон, который
   оно физически не покажет, и выглядеть это будет как «агент смотрит на чёрный прямоугольник и
   выдумывает содержимое». Целевой способ захвата — Windows.Graphics.Capture. Записано в
   `researches/11` §1.3 и в шапке самого инструмента.

## Решения, принятые без владельца

- **План помечен DONE по факту сдачи документа**, а не по факту наличия зрения в коде: заказ владельца
  дословно — «это план на ресёрч-гуглёж». Реализация каскада — новая работа, и она требует его ответа
  на §5 (смотрит постоянно или по команде), поэтому вписывать её в этот же план было бы натяжкой.
- **Ограничение `gdigrab` не чинилось сразу.** Смена способа захвата — правка рабочего инструмента,
  которым пользуются живые UI-прогоны; делать её вместе с разведкой значило бы смешать документ и
  непроверенный код. Заведено как известное ограничение с записью в шапке инструмента.
- **Кандидаты не скачивались и не замерялись** — задание требовало разведку, а замер OCR/захвата
  осмысленно делать уже под выбранную ступень каскада и на фикстуре наших окон.

## Заказ владельца (2026-07-28, дословно)

> «нужно будет запланировать: как наделить быстрым и надёжным зрением Jarvise и Joi. OpenCV - или ещё
> есть подходы. Это план на ресёрч-гуглёж.»

Контекст, в котором заказ прозвучал: в этот же вечер владелец предложил вести запись экрана 1 кадр/с,
чтобы агент анализировал происходившее (реализовано — `tools/screen-rec.mjs`), и сразу отметил, что
Jarvis/Joi этот инструмент понадобится в любом случае.

## Почему это эпик (а значит — сначала разведка)

Зрение — не фича, а **подсистема**: от неё зависят управление Windows (план 16), будущий Jarvis/Joi
(идея 05) и любая проверка «а что реально произошло на экране». Ошибка в выборе подхода дорого
переделывается, поэтому идём по канону: гуглёж → документ разведки → решение → код.

## Что уже есть в KLAS (стартовая база, её надо честно оценить)

| Кирпич | Что даёт | Известные границы |
|--------|----------|-------------------|
| `tools/screen-rec.mjs` | кадры рабочего стола 1/с, кроп в нативном масштабе | это «плёнка», а не понимание; агент читает кадры сам, это медленно и дорого по токенам |
| Windows-MCP `Snapshot` (UIA-дерево) | точные элементы и их координаты, без пикселей | новый Блокнот Windows 11 область ввода НЕ отдаёт — UIA покрывает не всё (`plans/16`) |
| `tools/web-shot.mjs` | зрение в вебе (playwright) | только браузер |
| VLM локально (gemma-4-12b и др.) | «посмотри и скажи» | скорость и VRAM в бюджете 16 ГБ; надёжность координат под вопросом |

**Главный вывод базы:** UIA даёт точность, но покрывает не все окна; пиксели покрывают всё, но требуют
понимания. Разведка должна ответить, как их сочетать, а не выбрать одно.

## На что должен ответить документ разведки (`researches/11_agent_vision.md`)

По обязательному составу из `AGENT_GUIDE.md`:

1. **Золотой стандарт сферы.** Как «зрение экрана» решают сегодня зрелые системы автоматизации и
   агентов: что считается стандартом для (а) захвата, (б) локализации элементов, (в) чтения текста.
2. **Научная/инженерная база.** Работы и спецификации по screen understanding / GUI grounding, метрики
   и бенчмарки этой задачи (со ссылками и датами; честно помечать, что прочитано целиком).
3. **Живые OSS-кандидаты** (по правилу живости: не архив, свежий push, звёзды) — как минимум проверить
   направления: классическое CV (OpenCV: template matching, поиск по признакам), OCR (какие движки
   живы и как быстры на CPU/GPU), парсеры интерфейса по скриншоту, современные API захвата Windows
   (`Windows.Graphics.Capture` против `gdigrab`), гибриды «UIA + пиксели».
4. **Решение для KLAS** — что берём и почему, в терминах приоритетов владельца: **надёжность → ум →
   скорость**, минимум сущностей, всё локально и офлайн, бюджет 16 ГБ VRAM (зрение не должно выселять
   основную модель), рабочий стол 6400×2160.
5. **Открытые вопросы владельцу** — развилки уровня видения (например: приватность записи экрана;
   постоянно ли Jarvis «смотрит» или только по команде) → в `/interview`.

## Критерии годности подхода (счётно, чтобы решение не было «по впечатлению»)

- **Быстро:** время от «нужно посмотреть» до «знаю координаты цели» — цель ≤ 1 с на кадр.
- **Надёжно:** доля верно найденных целей на наборе реальных окон (в т.ч. русский интерфейс,
  тёмная тема, окна без UIA) — измеряется на фикстурах, а не на впечатлении.
- **Дёшево:** сколько VRAM/CPU занимает постоянно и в пике; не мешает ли основной модели.
- **Офлайн и приватно:** ничего не уходит с машины (кадры экрана — самый чувствительный поток в KLAS).

## Связи

- Идея 05 (Jarvis/Joi) — зрение как орган; план 16 (управление Windows UI) — первый потребитель;
  `EXPERIENCE.md` EXP-0010 (зрение врёт, если кадр ужат) — граничное условие для любого решения.
