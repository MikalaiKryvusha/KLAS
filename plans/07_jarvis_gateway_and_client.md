# План 07 — ЗОНТИК голосового тракта: хост ↔ телефон ↔ гарнитура (архитектура)

> План-родоначальник связки «KLAS-хост ↔ Android-клиент ↔ носимое устройство» (вопрос владельца
> 2026-07-07). Знания — [researches/07_jarvis_wearable_transport.md](../researches/07_jarvis_wearable_transport.md).
> Родитель — [Jarvis (idea 05)](../ideas/05_Jarvis.md); смежное — [PAI](../ideas/psycho_acoustic_interface/).
> Статус: 🔧 **ЗОНТИК** — архитектура и принципы актуальны; исполнение НАРЕЗАНО на фазы-планы
> Г1–Г5 (2026-07-17, по указанию владельца), прогресс отслеживается ТАМ. Тег `DONE` не ставить.

## ⚡ Нарезка на исполняемые фазы (2026-07-17) — где что делается

| Фаза | План | Суть | Статус |
|---|---|---|---|
| Г1 «Голос» | [plans/11](11_voice_g1_tts_silero.md) | Русский TTS Silero v5 локально (`voice-say`) | 🔧 |
| Г2 «Уши» | [plans/12](12_voice_g2_stt_gigaam.md) | Русский STT sherpa-onnx+GigaAM-v3 (`voice-hear`, round-trip) | 🔲 |
| Г3 «Скелет разговора» | [plans/13](13_voice_g3_conversation_skeleton.md) | Микрофон → ядро OpenClaw → динамики (push-to-talk, латентность) | 🔲 |
| Г4 «Голос в кармане» | [plans/14](14_voice_g4_android_node.md) | Android-нода OpenClaw в tailnet (+ голосовая аутентификация — видение Q4 инт. 005) | 🔲 |
| Г5 «Свой голос» | [plans/15](15_voice_g5_clones_personas.md) | A/B клоны (Вихров/Joi) + персоны + естественность | 🔲 |

Старые «Фаза 1–5» ниже оставлены как архитектурная память; их актуальные версии — планы выше.
**Фазы 2–3 (свой Gateway API + транспорт tailnet) в исходном виде УСТАРЕЛИ:** ядром принят
OpenClaw (интервью 004/005, [plans/10](10_openclaw_core_rollout.md)) — его гейтвей в tailnet уже
развёрнут (ступень 1 ✅), свой WS-протокол и control-API не пишем; закладка «web-тул ассистента в
homepage» остаётся идеей для пульта (Q1 инт. 003).

---

## Стержневая идея (что именно строим)

**Тонкий «Jarvis Gateway»** на хосте + **лёгкий Android-клиент** на телефоне, связанные **через уже
развёрнутый в KLAS Tailscale**. НЕ «обширный кастомный API» — а маленькое стандартное ядро (realtime-
сессия голоса + control-API), переиспользующее существующие llama-server `/v1` и Tailscale. Обширным
шлюз становится органически, по мере добавления навыков.

**Принципы (PHILOSOPHY.md + заметки владельца + интервью 003):**
- KISS: не выставлять Jarvis в публичный интернет; не плодить эндпоинты; копировать форму сложившихся
  стандартов (OpenAI Realtime / Wyoming), а не изобретать.
- Минимум сущностей: транспорт — Tailscale (есть), LLM — llama-server (есть). Дописать только голос-сессию,
  STT, TTS, роутер навыков.
- Приватность — стержень: голос/транскрипты/память не покидают хост; **канал только в tailnet** (Q5).
- Бери лучшее: Whisper.cpp, Piper, Pipecat/LiveKit, HA Companion App — как база/референс.
- **Принцип выбора стека (⚠️ интервью 003):** язык/технологии — по критериям **поддерживаемость на
  длинной дистанции → активность сообщества → скорость/экономичность на железе → простота/надёжность**,
  а НЕ «Node по умолчанию». ⇒ realtime-аудио/ML (STT/TTS/оркестрация) — вероятно **Python** (Pipecat/
  whisper/piper, зрелее); **Node** — control-API, интеграция с llama-server, homepage-тул.

## Решения интервью 003 (✅ 2026-07-07) — развилки закрыты

1. **Android-клиент:** ✅ **готовая база** (оценить форк Home Assistant Companion App), адаптируем/кастомизируем.
2. **Оркестрация на хосте:** ✅ по принципу стека выше — гибрид: realtime-аудио пайплайн на Python, control-API на Node.
3. **Транспорт:** ✅ поэтапно — WebSocket (прототип в tailnet) → WebRTC (мобильный прод).
4. **STT/TTS старт:** ✅ Whisper.cpp + Piper (голоса Вихрова/Joi — обучить позже).
5. **Wake word:** ✅ on-device (microWakeWord/openWakeWord) + запасная кнопка на гарнитуре.
6. **Внешний доступ:** ✅ только tailnet (Q5). 7. **Персоны:** ✅ дефолт Jarvis, обе на оба имени (Q2, см. idea 05).

## Обновление после ревизии OSS-поля (✅ интервью 004, 2026-07-16)

Ревизия [researches/09_oss_constructor_map.md](../researches/09_oss_constructor_map.md) уточнила
конкретные модули (принцип владельца: **протухшие OSS не брать — только живые**):

1. **Ядро/Gateway:** перед Фазой 2 — **короткий смоук-пилот [OpenClaw](https://github.com/openclaw/openclaw)**
   (Node/TS, MIT, навыки-SKILL.md, память, Android-нода, локальные LLM) в tailnet на llama-server:
   вывозит ли наша локальная модель его агентный цикл. Выбор «OpenClaw vs свой Gateway» — на фактах пилота.
2. **STT:** ~~Whisper.cpp~~ → **sherpa-onnx + GigaAM-v3** (MIT; SOTA по русскому, стриминг, нативно
   Windows, websocket-сервер). whisper.cpp — мультиязычный fallback.
3. **TTS старт:** ~~Piper~~ (заморожен, преемник GPL) → **Silero v5** (MIT, 29 русских голосов,
   ударения, CPU). Клоны Вихрова/Joi — слепой A/B: CosyVoice 3 / Chatterbox V3 / F5-TTS_RUSSIAN (Q3 инт. 004).
4. **Каркас каскада (Фаза 1):** **Pipecat v1.0** (BSD-2; barge-in + turn detection из коробки) вместо
   самописного оркестратора; wake word обвязываем openWakeWord (пресет «Hey Jarvis» есть).
5. **Android (Фаза 4):** клиент следует за ядром — при OpenClaw-бекенде берём **его Android-ноду**
   (Voice Wake/Talk Mode готовы), форк HA Companion — только если ядро своё (Q2 инт. 004).
6. **Windows-контроль (навыки Jarvis):** **Windows-MCP** (MIT), полномочия ступенчато с allow-list
   (Q4 инт. 004): чтение UIA + белый список программ → мышь/клавиатура/shell позже.

---

## Фаза 0 — Исследование и требования ✅ (2026-07-07)

- [x] Гуглёж транспорта (WebRTC vs WS), пайплайна (каскад vs S2S), Android-нюансов, готовых экосистем.
- [x] Ответ на гипотезу владельца: нужен тонкий шлюз, не «обширный API»; транспорт — Tailscale.
- [x] Знания записаны в [researches/07](../researches/07_jarvis_wearable_transport.md); этот план по фазам.

## Фаза 1 — «Скелет разговора» на хосте (без телефона, без сети) 🔲

**Цель:** на самом хосте собрать каскад STT→LLM→TTS с одним локальным клиентом (микрофон/динамики ПК),
доказать пайплайн и качество, ничего не завязывая на мобильность.

- [ ] **STT:** Whisper.cpp / faster-whisper, потоковое распознавание (частичные результаты).
- [ ] **LLM:** переиспользовать llama-server `/v1` (уже есть) — интент/ответ; системный промпт персоны.
- [ ] **TTS:** Piper (быстрый neural TTS) — временный голос, пока не обучены Вихров/Joi.
- [ ] **Оркестратор сессии** (Node или Python/Pipecat — по развилке 2): склейка STT→LLM→TTS со **стримингом**
      («начал говорить, пока думаю») и **barge-in** (перебивание голосом).
- [ ] **Харнесс:** скрипт замера сквозной задержки (микрофон→первый звук ответа) + строка в таблицу
      `AGENT_GUIDE.md`.

**Проверка:** сказать в микрофон ПК фразу → Jarvis отвечает голосом за <~1–2 с до первого слова; перебивание
работает.

## Фаза 2 — Jarvis Gateway: тонкий API (2 поверхности) 🔲

**Цель:** вынести пайплайн за сетевую границу — оформить как сервис с маленьким стандартным API.

- [ ] **Realtime Voice Session (WebSocket)** — событийный протокол (по образу OpenAI Realtime / Wyoming):
      `session.start(персона, язык)`, `audio.chunk`, `barge_in`, `session.stop` (клиент→хост);
      `stt.partial/final`, `assistant.text.delta`, `audio.chunk`(TTS), `skill.invoked`, `error` (хост→клиент).
      Одна долгоживущая stateful-сессия = один разговор.
- [ ] **Control API (маленький REST/JSON):** выбор персоны **Jarvis/Joi**, конфиг голоса, навыки (CRUD из
      idea 05), доступ к памяти, запуск PAI-протоколов ([idea PAI](../ideas/psycho_acoustic_interface/)).
- [ ] **Слой персон (Q2):** дефолт **Jarvis**; обе персоны отзываются на **оба имени** (Joi/Jarvis); при
      обращении по имени НЕактивной — активная предупреждает «коллега пока отдыхает, сейчас я главная» и
      предлагает переключиться. Переключение — голосом и тумблером в web-туле. (Поведение — в idea 05.)
- [ ] **Web-тул управления ассистентом в homepage (закладка Q1):** отдельная страница «Ассистент» в пульте
      — микрофон браузера → WS → ответ голосом + управление (персона, старт/стоп, PAI). **Только для Криника,
      под паролем; близким доступа НЕТ** (отделено от гостевого Open WebUI). Это же — тестовый веб-клиент
      Gateway без Android. Плейсхолдер с правильным auth-контуром можно заложить ещё на этапе пульта (ядро).

**Проверка:** из браузера Криника (под паролем) в tailnet — полноценный голосовой разговор с Jarvis через
Gateway; смена персоны работает; чужой (без пароля) доступа не имеет.

## Фаза 3 — Транспорт через Tailscale (защищённый канал устройство↔хост) 🔲

- [ ] Android-устройство Криника — узел **tailnet** (WireGuard, обход NAT, без публичного IP).
- [ ] Gateway слушает только на tailnet-интерфейсе (НЕ публичный funnel — приватность голоса).
- [ ] Аутентификация: узел tailnet + Bearer-ключ на control-API (практика ключей KLAS уже есть).
- [ ] Проверка устойчивости канала на сотовой сети (потери/джиттер) — метрика для решения о WebRTC (Фаза 5).

**Проверка:** веб-клиент с телефона в сотовой сети через tailnet разговаривает с Jarvis дома.

## Фаза 4 — Android-клиент (носимый Jarvis/Joi) 🔲

> Самый «мобильно-сложный» слой (researches/07 §4). Решить развилку 1 (свой vs форк HA Companion App).

- [ ] **On-device wake word** «Jarvis»/«Joi» (microWakeWord/openWakeWord) — сессия на хост открывается
      только после слова (приватность, батарея, работа при плохой сети).
- [ ] **Foreground service** (Android 14+, `foregroundServiceType=microphone|connectedDevice`) — тихий фон,
      систему не убивает.
- [ ] **Bluetooth-аудио:** захват мик гарнитуры (HFP/**SCO**) + вывод (**A2DP**); аккуратное переключение
      SCO↔A2DP. Запасная **кнопка на гарнитуре** вместо wake word.
- [ ] **Клиент сессии:** стрим аудио на Gateway, проигрывание TTS, barge-in, индикация (говорит/слушает/думает).
- [ ] **Выбор персоны** (Jarvis/Joi) и базовое управление — из клиента.
- [ ] **Батарея:** тайм-ауты сессии, лёгкий детектор; замер расхода.

**Проверка:** Криник в наушниках, телефон в кармане: «Jarvis…» → разговор; «Joi…» → мягкая персона; фон не
жрёт батарею катастрофически; перебивание работает.

## Фаза 5 — Мобильный прод: WebRTC + шлифовка «как с живым» 🔲

- [ ] **WebRTC** для аудио-канала (сабсекундная задержка, jitter buffer, эхо/шумоподавление, NAT traversal)
      — устойчивость «на ходу по городу». Кандидат инфраструктуры — **LiveKit** / Pipecat WebRTC. WebSocket
      остаётся для control-событий.
- [ ] Настоящие голоса: **Вихров** (Jarvis) и **девичий/бархатный** (Joi) — обучение на локальной GPU
      (см. idea 05, раздел голоса/личности).
- [ ] Шлифовка задержки и естественности: пороги VAD, длина первого чанка TTS, интонации.
- [ ] Интеграция **PAI** ([idea](../ideas/psycho_acoustic_interface/)): навык `/pai` доступен голосом через
      Gateway (запуск протоколов в наушники).

**Проверка:** прогулка по городу — разговор без ощутимых лагов и обрывов, интонации приятные, персоны
переключаются, PAI-протоколы запускаются голосом.

---

## Зависимости, риски, что переиспользуем

- **Уже есть в KLAS (переиспользовать, не переделывать):** Tailscale (транспорт), llama-server `/v1` (LLM),
  практика Bearer-ключей, homepage-пульт (тестовый веб-клиент и управление).
- **Заимствуем:** Whisper.cpp/faster-whisper (STT), Piper (TTS), Pipecat/LiveKit (realtime-оркестрация/
  WebRTC), HA Companion App / wyoming-satellite (референс Android-клиента), Wyoming/OpenAI-Realtime (форма API).
- **Риски:**
  - *Мобильные ограничения Android* (фон, батарея, BT SCO) — главная практическая сложность → Фаза 4
    отдельно, возможно форк готового клиента.
  - *Задержка сквозного каскада* — митигировать стримингом+barge-in с Фазы 1; при нужде — S2S-модель в будущем.
  - *Соблазн «обширного API»* — держать шлюз тонким; форма — стандартная; наружу только tailnet.
  - *Стек Node vs Python* — допускается гибрид (заметка владельца): realtime-аудио на Python, control на Node.
- **Главная зависимость:** базовый Jarvis (навыки, память, персоны Jarvis/Joi — idea 05). Фазы 1–2 можно
  начать раньше как фундамент, Фазы 4–5 — после базового Jarvis.

## Ссылки
- Знания/обоснование: [researches/07_jarvis_wearable_transport.md](../researches/07_jarvis_wearable_transport.md)
- Родитель: [Jarvis (idea 05)](../ideas/05_Jarvis.md) · Смежное: [PAI](../ideas/psycho_acoustic_interface/README.md)
