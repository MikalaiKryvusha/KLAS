# KLAS — Krinik Local Agent System — Текущий статус

> Этот файл агент читает перед каждой задачей. Обновляй его при каждом значимом изменении состояния.
> Это ГЛАВНАЯ передача между сессиями: новая сессия агента стартует с пустым контекстом и должна стать
> продуктивной по одному этому файлу. Пиши соответственно — конкретно, с путями и командами.
> 🧠 Главный мыслительный принцип — `PHILOSOPHY.md` (ПРОСТОТА: KISS + Оккам). Рабочий фреймворк —
> в `AGENT_GUIDE.md`.
>
> ⚠️ **STATUS — СВОДКА НАСТОЯЩЕГО, а не летопись.** Файл, который только растёт, превращается в
> книгу по истории проекта, и агент, пришедший за быстрым «где мы», в ней тонет. Правила, которые
> держат его сводкой:
>
> - **Каждая строка проходит два теста:** «убери эту строку — ошибётся ли следующий агент?» и
>   «прочитает ли новичок весь файл за один присест?». Мягкая цель — **~200 строк**.
> - **Закрытое ВЫНОСИТСЯ, а не копится:** запись перестала быть «настоящим» — переезжает ДОСЛОВНО
>   в `PROJECT_HISTORY.md`. Шаг «бонсай-подрезка» есть в `/end-chat`.
> - **Оставляй файл таким, каким хотел бы его найти:** сводка что работает, что дальше, где грабли и
>   ГДЕ ИСКАТЬ подробности — указатели, а не пересказы.

---

## Что сделано (короткий хвост — остальное в `PROJECT_HISTORY.md`)

- **2026-08-16 21:10 +03:00 — 🐳 МЕНЕДЖЕР ВИДЕОКАРТЫ ПЕРЕЕХАЛ В КОНТЕЙНЕР KLAS** по требованию
  владельца (`ideas/21`: «внутри KLAS в докере и не открывать окон терминала», рождено из `bugs/28`).
  Сервис `gpu-manager` в `docker-compose.yml`, код — `tools/gpu-manager.mjs`. Dockerfile НЕ нужен:
  `nvidia-smi` впрыскивается Container Toolkit в голый `node:22-slim`. Проверено наблюдением:
  контейнер видит карту (те же числа, что хост в ту же минуту), достаёт до llama-swap, окон — 0.
  Сегодня режим наблюдения; право грузить и выгружать модель получит в фазах 2–4 сменой режима,
  а не новым сервисом. Подробности и ловушка отбора — блок «ЖИВОЕ ПРЯМО СЕЙЧАС» ниже.
- **2026-08-16 14:45 +03:00 — 👁 фаза 1 эпика «горячая модель» пошла.** `tools/gpu-watch.ps1` поднят
  задачей планировщика `KLAS-gpu-watch` **в сессии пользователя** (проверено: session=1) и пишет
  `logs/gpu-watch/<дата>.jsonl`, не делая НИЧЕГО. Разбор — `node tools/gpu-watch-replay.mjs`
  (шесть правил-кандидатов, самопроверка **14/14**). Цена наблюдения — **0.65% одного ядра**, объём
  журнала **0.508 МиБ/сутки** при бюджете владельца 1 МиБ. Три находки первых минут сузили развилку
  Q1 в `interviews/013` (записаны и туда, и в `plans/28`); урок про мерцающие датчики — **EXP-0067**.
- **2026-08-16 22:00 +03:00 — 🧠 Qwen3.8 = 7.7/8.0, ЛУЧШЕЕ ЧИСЛО В ИСТОРИИ KLAS; движок обновлён до
  b10453; призраки снесены.** Владелец усомнился в замере — и был прав ДВАЖДЫ. Три числа на одной
  модели: **7.2** (рассуждение погашено флагом от Qwen3.6 — мерили покалеченную модель) · **3.9**
  (рассуждение включено, но бюджеты `max_tokens` бенча съедало само рассуждение, ответ обрезался —
  мерили сломанный харнесс) · **7.7** (обе починки — мерили модель). Выше удалённой gemma-4-12b (7.5).
  Урок — **EXP-0068**. Подробности замера — пункт 0 эстафеты.
- **Текущий стек — факты настоящего** (как мы к ним пришли — `PROJECT_HISTORY.md` 2026-08-16):
  единственная модель **Qwen3.8-27B `UD-Q3_K_XL`**, 12.52 ГиБ (4-битный ярус в 16 ГиБ не влезает по
  весам — `researches/27`), **рассуждение ВКЛЮЧЕНО** (`enable_thinking:true`; `reasoning_effort` из
  карточки движок игнорирует). Движок **llama.cpp b10453** (обновлён 2026-08-16 с b10167; откат —
  `F:\KLAS\llamacpp_b10167`). Под загруженной моделью **занято 15 559 МиБ, свободно 437 МиБ** — рядом
  не встанет ничего. Холодный старт **8.2–12.2 с** из файлового кеша (замерено на b10453) · тёплый
  первый токен **0.79 с** (было 0.08 с — выросло, потому что теперь модель думает).
  ⚠️ **Осторожно со сравнением 34.2 → 12.2 с.** Число 34.2 с досталось от ПРОШЛОЙ сессии на b10167 и
  заново под теми же условиями кеша НЕ перемерялось. Выигрыш правдоподобен и, скорее всего,
  движковый, но как ДОКАЗАННЫЙ его записывать нельзя, пока не снят b10167 из отката
  `F:\KLAS\llamacpp_b10167` тем же `cold-ttft` подряд с b10453. Числа «с диска» на b10453 тоже нет —
  прежние 80.4 с относятся к b10167 и больше не действительны.
  Замер: `node tools/cold-ttft.mjs <alias>`; рычаги — `researches/28`.
  ⚠️ Быстрой модели для тривиального больше нет; слот «LLM + голос одновременно» пуст.
- **Релиз 1.0 «Universal KLAS»** (2026-07-04) — https://github.com/MikalaiKryvusha/KLAS/releases/tag/v1.0.

> Сессии 2026-08-14 (ревизия отката слабых сессий + операционная карта `plans/25`; обновление KAIF
> 2.1 → 2.2 «Yolden KAIF») закрыты и переехали в `PROJECT_HISTORY.md` 2026-08-16. Живые хвосты
> оттуда: `mcp/web-search-mcp-v0.3.2/dist` переприколоть к тегу при подключении (`plans/25`, Д5).

> Голосовой тракт (активаторы, разговор по имени, перебивание, сигналы, анонимная копия, охранник
> кодировок, лестница «постоянной жизни») закрыт и переехал в `PROJECT_HISTORY.md` 2026-08-02.
> Живые указатели на незакрытые хвосты этих треков остались ниже — в «Где продолжить» и в багах.

---

## 🚩 КАНОН ВЛАДЕЛЬЦА — в силе, не переоткрывать

Полные формулировки и обстоятельства — в `PROJECT_HISTORY.md` по указанной дате.

| Решение | Дословно / суть | Когда |
|---|---|---|
| **Активаторы** | «Джарвис» и «Джой» (написание подтверждено 07-31); **08-01 добавлено третье — «Ариэль»** (в честь Ариэль Винтер, написание выбрано владельцем). Джой оставлена НАМЕРЕННО: сравниваем устойчивость короткого и длинного имени к перебиванию (`plans/20`) | 07-29 · 08-01 |
| **Голос JARVIS** | `candidates/JARVIS/espeech_bystree.mp3` — ESpeech-TTS-1 RL-V2, эталон `voice/sources/jarvis_vihrov/ref_vihrov_12s.wav`, `speed=1.06`, `TRANSLIT=full` | 07-30 |
| **Голос JOI** | `candidates/JOI/gorokhova_112cps.mp3` — F5-TTS_RUSSIAN v4_winter, эталон `voice/sources/joi_gorokhova/joi_v_112.wav`, `speed=1.0`, `TRANSLIT=full` | 07-30 |
| Piper как движок голоса | ⛔ **«отвратительно… я подозреваю, что Piper как класс»** — подтверждено замером (23.7 М параметров против ~330 М) | 07-30 |
| Silero как голос ассистента | ⛔ **«отвратительно. разные голоса. НЕПРИЕМЛИМО»** (в тракте остаётся рабочей лошадкой, но не как выбор) | 07-29 |
| Транслитерация латиницы | **«вынужденный костыль»** ⇒ настоящее лечение — МУЛЬТИЯЗЫЧНЫЙ движок | 07-29/30 |
| Ресурсы | обучение — **хоть вся видюха**; инференс — голос должен быть **ресурсо лёгким** | 07-29 |
| Темп речи | коридор **12–14 симв/с**. ⚠️ cps предсказывает ТЕМП, но не МАНЕРУ | 07-30 |
| Эталон согласует владелец | клонировать без одобренного им эталона запрещено | 07-29 |
| Правило живости OSS | протухшие репозитории не брать (звёзды, свежий push, не архив) | 07-16 |
| Форма выдачи на выслушку | один файл на вариант, кусочки склеить и удалить, папки по кандидатам, громкость выровнять | 07-30 |
| **Сигнал контура вычитки** | **«звучит хорошо. Ок»** — принято на слух в комнате: три писка **880 → 660 → 990 Гц** (160/160/260 мс) + голос Silero **`eugene`**. ⛔ Не переизобретать от проекта к проекту: владелец должен узнавать сигнал по звуку. Меняется только его словом | 08-02 |

---

## 🎙 АКТИВАТОРЫ — ГОТОВЫ (план 19: шаги 1–4 закрыты, шаг 5 ждёт живого вердикта)

Работают, вердикт владельца на живом микрофоне — **«работает отлично»**. Все числа (полнота, ложные,
перебивание по трём именам, цена круглосуточного ожидания) и перечень инструментов — **`plans/19`**,
таблицами; запись личного голоса — `plans/23`. Числа вынесены отсюда 2026-08-16: сводка не должна их
дублировать. Модели `voice\wakeword\training\*.onnx` вне git — **пересоздаются, а не хранятся**.

⚠️ **Живой хвост — за владельцем:** Ариэль по числам лучшая из трёх (0.988 против 0.890 у Джарвиса на
перебивании), но **живьём им не слышана**; односложная Джой на перебивании не работает вовсе (0.005).
Судьба Джой против Ариэль — его решение, не агента.

### 🗣 Разговор по имени — что решил владелец (2026-08-01)

- **«Слушать всегда, имя перебивает»** — детектор НЕ глушится на время своей речи. Откат —
  `--mute-while-speaking` (не проверен наблюдением). Оба вопроса `researches/16` §5 ОТВЕЧЕНЫ.
  ⚠️ **Замер 2026-08-01 перевернул обе половины цены** (`bugs/25`): страшились ложных
  самоперебиваний — их НЕТ (своя речь даёт 0.001 при пороге 0.5); а вот настоящее перебивание НЕ
  РАБОТАЕТ — имя поверх своей речи даёт 0.478/0.012. Перебивание живёт только в паузах.
- **Сигнал «услышал» — короткий бип**, 0.36 с накладных на запуск проигрывателя + 0.15 с тона.
- ⚠️ **ОБА голоса в тракте — заглушки Silero** (Джарвис `eugene`, Джой `xenia`). Одобренные клоны —
  ESpeech на эталоне Вихрова и F5 на эталоне Гороховой — в резидентный рот не встроены (`plans/18`).
  Владелец заметил это первым же живым прогоном (`homeworks/04`). Не «подбирать красивее» другой
  Silero — лечение это клон в лёгком движке.
- ⚠️ **Беседы персон разные** (`voice-<дата>-<slug>`) ⇒ первый ход каждой персоны за день ~50 с.

### ⛔ Грабли активаторов — НЕ переоткрывать

- **Слово обязано ЗАКАНЧИВАТЬ фразу.** Обучение выравнивает клип по ПРАВОМУ краю окна
  (`create_fixed_size_clip`), поэтому имя в середине фразы не выучивается. Фильтр в
  `prepare-training.mjs`. Боевой сценарий это НЕ обедняет: в живой речи детектор срабатывает сразу
  после имени, когда окно как раз кончается на слове.
- **Окно ровно 32 000 отсчётов (2.0 с)** — его ЖЁСТКО задаёт форма скачанных негативов (16 кадров).
- **Семь несовместимостей апстрима** закрыты прокладками в `tools/voice/wakeword-train-shim.py`,
  каждая с проверкой числами. Апстрим НЕ форкнут. Перечень — в летописи и в шапке шима.
- **`--cross` обязателен.** Без него модели путают чужое имя: контрольный опыт дал 99.7% против 0.0%.
- ⚠️ **Потоковый стенд `wakeword-compare.py` НЕДОСТОВЕРЕН** (0.52/0.052/0.084 на одном клипе).
  Мерить только `wakeword-eval-fixed.py`. Урок — **EXP-0047**.

---

## ▶️ ГДЕ ПРОДОЛЖИТЬ — эстафета для НОВОЙ сессии

Порядок по ценности. Пункт 1 ждёт одного слова владельца, пункт 2 — свободной видеокарты.

> 🗺 **Полный граф дорожек (голос · LLM-ядро · постоянная жизнь · панель · гигиена), гейты и
> правила взятия — `plans/25` (операционная карта).** Ниже — вершина очереди.

> 🖱 **ЧТО СПРАШИВАТЬ У ВЛАДЕЛЬЦА — ТОЛЬКО СТРАНИЦЕЙ, НЕ ЧАТОМ.** `node tools/review.mjs list` →
> `open <документ>`. Правило нарушают агенты, которые его ЗНАЮТ (проверено дважды, `EXP-0062`):
> признак, по которому ловить себя, — **в твоём сообщении вопросительный знак и варианты A/B/C**.
> Это заявка на интервью, а не реплика в чат.

> ## 🟢 ЖИВОЕ ПРЯМО СЕЙЧАС — за картой следят ДВА наблюдателя, и это осознанно
>
> **Фаза 1 эпика «горячая модель» (`plans/28`) идёт с 2026-08-16, шаги Ш1–Ш3 и Ш5 закрыты.**
>
> | наблюдатель | где | что видит | зачем ещё жив |
> |---|---|---|---|
> | **`gpu-manager`** (главный) | сервис докера в стеке KLAS, `docker-compose.yml` | карту целиком + модель llama-swap | ⭐ требование владельца (`ideas/21`): внутри KLAS, **окон не открывает** |
> | `tools/gpu-watch.ps1` | задача планировщика `KLAS-gpu-watch`, сеанс владельца | то же + **сеансовые величины** | ЕДИНСТВЕННЫЙ источник «во весь экран» — вход правил C и C+B |
>
> **Смотреть набранное:** `node tools/gpu-watch-replay.mjs` (хост) · `--source docker` (контейнер).
> Печатает покрытие Ф1-1, износ Ф1-4, **разрывы** и кандидатов в эпизоды. Журналы РАЗВЕДЕНЫ по
> каталогам намеренно: склейка двух источников показала бы сплошное покрытие там, где каждый датчик
> по отдельности умирал. Хостовой не перезапускать руками — держит мьютекс, лишний старт рвёт сутки.
>
> ⚠️ **ГЛАВНАЯ ЛОВУШКА ОТБОРА, уже закрытая кодом (2026-08-16).** Из контейнера сеансовые величины
> (`fs`, `fg`, `idle_s`, список процессов) недостижимы ПО УСТРОЙСТВУ — граница гостя и хоста, а не
> настройка. Правило на `fs` там молча вычислялось бы в «никогда не занято», показало бы НОЛЬ ложных
> тревог и **возглавило бы отбор, не измерив ничего**. Поэтому величины не подменяются нулями: их
> нет, строка помечена `src:"docker"`, и реплей ДИСКВАЛИФИЦИРУЕТ правило без входа (самопроверка
> 14/14 держит именно этот случай). По контейнерному журналу судимы 2 правила из 6.
> Отдельная находка: `idle_s`, ради которого датчик вообще привязали к сеансу, **не использует ни
> одно из шести правил**.
>
> **Что осталось по фазе:** ① набрать ≥ 24 ч непрерывно (Ф1-1) · ② разметить эпизоды пятью классами
> в `logs/gpu-watch/episodes.json` (Ш4; черновик кандидатов печатает сам replay) · ③ прогнать шесть
> правил и выбрать пороги (Ш5) · ④ дописать вывод в `plans/26` (Ш6). Правило выбрано → хостовая
> задача снимается совсем, остаётся один контейнер.
> **За ВЛАДЕЛЬЦЕМ два свидетельства:** смоук перезагрузкой (агент рабочую машину не перезагружает) и
> отлучка на 10 минут для критерия Ф1-5.
> **Выключатель:** `New-Item -ItemType File F:\KLAS\logs\gpu-watch\STOP` (действует на оба) · снять
> совсем: `docker compose stop gpu-manager` · `Unregister-ScheduledTask KLAS-gpu-watch`.
>
> ✅ Призрак `llama-swap` СНЕСЁН 2026-08-16 по слову владельца вместе с шестью неиспользуемыми
> образами и кешем сборки — освободилось ~21 ГБ. `ndim-*` не трогать: другой проект владельца.
>
> ⚠️ **Три находки первых минут уже сузили развилку Q1 в `interviews/013`** (записаны и в интервью,
> и в `plans/28`): память НА ПРОЦЕСС на Windows не читается вовсе (`[N/A]`, WDDM) · «любой чужой
> процесс на карте» мёртв — там постоянно 24 обычные программы · на карте мерцает `sdc_fma.exe`
> (каждые ~10 с гонит утилизацию 1% → 54%), отчего наивный датчик дал 2.66 МиБ/сутки при бюджете
> владельца 1 МиБ. Лечение — подтверждение состояния 15 с плюс агрегаты `util_max`/`toggles`, чтобы
> выдержка не стёрла мерцание из записи; стало **0.508 МиБ/сутки** (урок — **EXP-0067**).

0. **🧠 Qwen3.8-27B В БОЮ — шаги ①②③④ ЗАКРЫТЫ 2026-08-16.** `agent-bench v3 = **7.7/8.0**` —
   лучшее число в истории KLAS. Берёт 7 задач из 8 полностью; единственная потеря —
   `tool-chain-branch` 0.67, которую **одинаково проваливают ВСЕ модели** ⇒ подозревать надо саму
   проверку (`bugs/22`), а не модель.
   ⚠️ **ДВА ДЕФЕКТА ЗАМЕРА, оплаченные здесь; не переоткрывать:**
   · **Настройка.** Профиль гасил размышление ключом `enable_thinking:false` от Qwen3.6. Пробник
     проверял только способы ВЫКЛЮЧИТЬ и по «не думает ни при каком флаге» заключил «флаг ни при
     чём» — неверно. `enable_thinking:true` даёт рассуждение и верный ответ; `reasoning_effort` из
     карточки 3.8 наш движок **игнорирует полностью** (тупик, не пытайся). Рассуждающую модель
     мерить В ОБЕ СТОРОНЫ.
   · **Харнесс.** Бюджеты `max_tokens` бенча (20/60/80/120) выставлены под модели, пишущие ответ
     сразу. Рассуждение съедало бюджет, `content` приходил ПУСТЫМ, задача получала ноль. Доказано:
     `max_tokens=60` → обрыв по длине, пусто; `=300` → верный ответ. Лечение — припуск 2000 токенов,
     определяемый одной пробой (для нерассуждающих 0, прежние числа сравнимы).
   Осталось: ④ `chain-bench` · ⑤ опыты по холодному старту из `researches/28`. ⚠️ **Приёмка эпика
   почти достигнута ОДНИМ обновлением движка:** холодный старт 34.2 с → **8.2–12.2 с** (из кеша).
   Не снято число «с диска» на b10453 — прежнее 80.4 с относится к b10167 и больше не действительно.

0. **🐳 ЗАМЕР «llama-swap в докере против приложения Windows» — спросил ВЛАДЕЛЕЦ, ответ не дан.**
   Он ожидал, что llama-swap уже часть контейнера KLAS; сейчас это процесс Windows, а контейнер с тем
   же именем был неработающей попыткой (снесён). Плюсы докера очевидны и совпадают с его вектором:
   свежий движок одной командой `docker pull`, весь стек в одном месте, ни задач планировщика, ни
   окон, разворачивание на другой машине одной командой. **Два риска названы и ОБА измеримы за вечер,
   решать их спором нельзя:** ① видеопамять на краю — модель занимает 15 559 из 16 303 МиБ, свободно
   437; прослойка WSL2 берёт своё, и модель может просто не влезть; ② чтение весов 12.5 ГиБ с `F:\`
   идёт через прослойку — холодный старт (наша главная боль) может стать ХУЖЕ, а не лучше.
   Опыт: поднять `ghcr.io/ggml-org/llama.cpp:server-cuda` рядом, загрузить ТУ ЖЕ модель, сравнить две
   цифры — свободная видеопамять и время загрузки. Нет потерь → переезжаем целиком.

0. **🔥 ЭПИК «ГОРЯЧАЯ МОДЕЛЬ» — `plans/26_EPIC_hot_model.md`, фаза 0 (нужен СВОБОДНЫЙ GPU).**
   Свежая задача владельца и вершина очереди. Операционный план готов —
   **`plans/27_epic26_phase0_wake_cost.md`**, семь шагов с чекбоксами: разобрать 34 с по стадиям,
   NVMe против SATA, `-lm dio`, `--no-warmup`, `--fit off`, измерить ярус файлового кеша, зафиксировать
   лучший набор. ⚠️ Главная методическая ловушка записана в плане: **без управления файловым кешем
   любой замер врёт** — на этом уже обожглись (34 с против 80 с оказались разницей кеша).
   Фаза 1 этого же эпика УЖЕ ИДЁТ и GPU не требует — блок «ЖИВОЕ ПРЯМО СЕЙЧАС» выше.
   Развилки политики ждут владельца в `interviews/013` — фазы 0 и 1 ими НЕ заблокированы.
   ⛔ Планы фаз 2–4 намеренно НЕ написаны: каждый пишется при закрытии предыдущей.

1. **🏠 Вердикт владельца по своим записям — `homeworks/06`.** Он их УЖЕ прослушал целиком (63 с),
   но вердикт не прозвучал: чат закрылся раньше. Пересобрать прослушку и спросить одно:
   не съедено ли начало «Дж» и конец «-с».
   `…\python.exe tools\voice\wakeword-enroll.py --slug jarvis --audition --play`
   ⚠️ Играет ЗВУК В КОМНАТЕ — предупредить и дождаться «тихо» (правило владельца).

2. **🎓 Обучить «Джарвиса» на личных клипах — `plans/23`, шаги 4–6** (нужен свободный GPU). 50
   клипов лежат в `voice/wakeword/corpus/jarvis/personal/`, оригиналы 48 кГц —
   `voice/sources/own_voice/jarvis/`. ⚠️ Главный нерешённый вопрос шага 4: **кратность дублирования**
   — 50 личных против ~10 000 синтетических утонут без следа; кратность обязана печататься в конфиг,
   как сделано для речевого фона. Личные клипы НЕ заменяют синтетику, а смешиваются с ней.
   Приёмка: не хуже на честном наборе И лучше на живом микрофоне владельца.

3. **🎙 Длинная начитка владельца** — текст готов (`docs/voice_reading_ru.md`, 7 блоков, ≈2.8 мин
   чистой речи), стенд готов (`tools/voice/voice-reading.py`, разбор текста 3/3), **не записана**.
   Даёт биометрию (`plans/09`) и материал для клонирования (`plans/18`) за один заход.
   ⚠️ Стилометрию с начитки снять НЕЛЬЗЯ — чтение чужого текста показывает манеру автора, а не
   чтеца; письменный портрет берётся из `interviews/` и переписки.

4. **🎧 Живая проверка голоса** — накопилось четыре вещи, которые проверяются только ухом и
   микрофоном владельца: перебивание Джарвиса · окно продолжения (5 с без имени) · новые сигналы ·
   не попадает ли в вопрос собственная речь. Плюс два замера, требующих ЗВУКА и его разрешения:
   задержка «убили проигрыватель → тихо» (сейчас стоит ОЦЕНКА 0.32 с) и бенч тракта 35/35.

5. **🚪 Админская панель — `ideas/20`, разведка не начата.** Направление владельца («терминал —
   временный костыль»). Порядок обязателен: разведка (`D:\work\ndim` найден, НЕ изучен; живое на
   GitHub; стыковка с homepage) → интервью по трём вопросам из `ideas/20` → мета-план.
   ⛔ Не начинать посреди голосового тракта: сначала закрыть `plans/23`.

6. **`bugs/22` + перемер** (нужен свободный GPU) — судьи бенчей мягче заявленного. Чинить ОДНИМ
   коммитом с перемером v2/v3/chain-bench, старые колонки в `researches/02` пометить «до 2026-07-31».

7. **Трек В — CosyVoice 3** (`plans/18`, нужен GPU): всё готово к запуску, скрипт
   `tools/voice/clone_cosyvoice.py` написан и веса на месте. Выдать оба режима транслитерации
   владельцу на выслушку.

8. **📉 Разнести 10 унаследованных вопросов** из `tools/questions-baseline.json` по темам в
   `interviews/` — число обязано убывать (`npm run guard:questions` печатает его каждый прогон).
   ⛔ Не сваливать в одно мега-интервью: на документ с 10 разнородными развилками не ответят.
   Разнёс — пересними линию: `node tools/questions-guard.mjs --baseline`.

9. ~~**Легитимизация `qwen2.5-coder-14b`** (`plans/25`, дорожка Б)~~ — **СНЯТО 2026-08-16:** модель
   удалена вместе с зоопарком по слову владельца («нет смысла держать зоопарк глупых моделей»).
   Роль кодера берёт Qwen3.8-27B (заявка вендора SWE-bench Pro 61.7). Паспорт `researches/26`
   остаётся как история класса; вернуть модель — одна команда `tools/try-model.mjs`.

**Первое, что сделать в новой сессии:** `git log --oneline -5` · `npm run kaif:check` ·
`npm run owner:questions` · `npm run guard:questions` · **`node tools/gpu-watch-replay.mjs`** (что
набрал живой датчик — блок «ЖИВОЕ ПРЯМО СЕЙЧАС» выше). Берёшь голосовую дорожку — прочитай
`plans/19_wake_word_activators.md` целиком, там вся механика активаторов с числами.

**Чего НЕ делать:** не трогать голос без просьбы владельца (заморожен осознанно) · не «чинить»
`bugs/15` автоперезапуском (уничтожит единственный путь к причине) · не докручивать темп клона ручкой
`speed` (владелец слышит манипуляцию) · не верить показаниям `wakeword-compare.py`.

---

## 🤖 Пул автономного беклога (не нужен человек / особое железо)

- [ ] **Поиск свежих моделей (идея 06, регулярно):** `node tools/leaderboard.mjs` + веб-поиск.
      Последний раз **2026-08-16** (Qwen3.8-27B, по наводке владельца).
- [ ] **`bugs/16`** — врезать готовый охранник `ref_pair.py` в скрипты синтеза при следующем клонировании.
- [ ] **Бенч «грамотность пользования ПК»** — дизайн согласован (`researches/18`), **кода ещё нет**.
- [ ] **`canonArtifacts` не объявлены** в `.kaif/kaif.json` — гейту происхождения не за что зацепиться.
- [ ] **Разнести 10 унаследованных вопросов** из `tools/questions-baseline.json` по темам в
      `interviews/` (пункт 8 эстафеты). Число обязано убывать.
- [ ] **Разметить сутки датчика видеокарты** (`plans/28` Ш4–Ш6): дождаться ≥ 24 ч журнала, снять
      разметку эпизодов по именам процессов, прогнать `gpu-watch-replay`, вернуть пороги в `plans/26`.
      Человек нужен только на спорных эпизодах — одной строкой в `interviews/013`.
- [ ] **Досократить STATUS до ~200 строк** (`kaif:check` предупреждает каждый прогон). 2026-08-16
      вынесены: запись о Qwen3.8, таблица инструментов активаторов, числа активаторов, разбор
      `bugs/27` (−80 строк суммарно), вся сессия 16 августа — в `PROJECT_HISTORY.md`. Следующие
      кандидаты — блок «🚩 КАНОН ВЛАДЕЛЬЦА» (дубль `PROJECT_HISTORY` по собственному признанию его
      шапки) и «🗣 Разговор по имени». ⚠️ Живые хвосты не выносить: «грабли активаторов» —
      предупреждения будущим сессиям, а не история.
- [ ] **Разметить эпизоды по журналу КОНТЕЙНЕРА** (`--source docker`) — у него нет имён окон, поэтому
      разметка опирается только на память и утилизацию. Сверить с хостовым журналом за те же часы:
      расхождение покажет, чего реально стоят сеансовые величины.

## ❓ Ждёт ревью человека (место вопросов — `interviews/` и `homeworks/`)

> 🖱 **Показывай это владельцу СТРАНИЦЕЙ, а не пересказом в чат:** `node tools/review.mjs list` →
> `open <документ>` (один вопрос) либо `queue` + `batch` (пачка, если владельца сейчас нет).
> Пересказ в чат — тот самый порок, ради устранения которого построен `plans/24`.

- ❓ **`bugs/10`** — переключать ли уши на модель `punct`. Проверить микрофоном:
      `node tools/voice-talk.mjs --ears punct`.
- ✅ **Интервью 008 и 011 ОТВЕЧЕНЫ 2026-08-02 через сам контур вычитки, и ОБА разнесены.**
      008: `LICENSE` не трогаем · `README.pdf` пересобирать (сделано, `guard:anonymize` 11/11).
      011: порядок работ · отчёт в KAIF добавкой (`reports/KAIF_UPDATES/02_…`) · навык раздаёт текст, а не код.
- ❓ **Идеи `15` (дедуп общих модулей) и `16` (сторож гигиены)** — ждут одобрения.
- ❓ **`researches/19`, вопросы владельцу:** funnel-хост в git · чистка истории от транскрипта.
- 🏠 **`homeworks/06` — принять свои 50 записей на слух.** Прослушаны целиком, вердикт не прозвучал.
      Числа ровные (медиана 0.76 с, выбросов 0), но «не обкусано ли начало имени» число не видит.
- ❓ **`ideas/20` — админская панель:** внутри homepage или отдельно за той же дверью · только
      владелец или любой, кто поднял KLAS · **витрина или пульт** (смотреть безопасно, а запускать
      переобучение и службы — уже власть).
- ✅ **Интервью 010 ОТВЕЧЕНО** (Q1=A служба Windows, Docker не нужен · Q2=A круглосуточно +
      выключатель, плюс голосовое «отпусти микрофон» и пробуждение ярлыком · Q3=C служба сразу, но
      логируем каждую смерть). Эпик `plans/22` разблокирован, ни одна фаза не начата.
- 🏠 **`homeworks/05`** — сигналы выбраны (`bell` + `bell-deep`), но живьём в разговоре ещё не
      слушались.
- ❓ **Интервью 009 закрыто** (AEC выбран, все три ответа получены), но сам AEC на этой машине
      бессилен — микрофон USB, звук по HDMI в телевизор, общего клока нет (`plans/20` шаг 2).
      Перебивание вылечено переобучением, а не подавлением эха.
- 🏠 **`homeworks/03`** — выслушка кандидатов голоса.
- ❓ **Интервью 001 (Q3–Q4)** — границы автономии, docker-сервисы. Открыто с 2026-07-02.

## 🐞 Открытые баги

| Баг | Состояние |
|---|---|
| `bugs/04_openWEBUI_FCK_UP.md` | фикс применён и проверен браузером, ждёт подтверждения владельца |
| `bugs/07_windows_mcp_type_char_corruption.md` | `Type` перемежающеся искажает символ; корень не установлен |
| `bugs/10_stt_no_latin_no_punctuation.md` | лечение развёрнуто ОПЦИЕЙ `--ears punct` — ждёт владельца |
| `bugs/15_openclaw_gateway_silent_crash.md` | гейтвей умирает молча; три гипотезы сняты, ловушка стоит |
| `bugs/16_espeech_splices_reference_into_synthesis.md` | охранник готов — врезать при следующем клонировании |
| `bugs/17_pilot_profile_compat_drift.md` | песочница чинена; историческая цифра «qwen 5/6» под сомнением |
| `bugs/18_piper_corpus_quotechar_swallows_rows.md` | охранник поставлен и проверен; документ не закрыт |
| `bugs/22_bench_judges_leniency.md` | судьи мягче заявленного — чинить с ПЕРЕМЕРОМ (нужен GPU) |
| `bugs/25_bargein_deaf_over_own_speech.md` | Джарвис вылечен переобучением; Джой перебивается, но труднее |
| `bugs/26_bargein_captures_own_speech.md` | фикс есть и проверен стендом; ⚠️ задержка «убили → тихо» стоит ОЦЕНКОЙ 0.32 с, не замером |
| `bugs/27_gpu_watch_dies_control_c_exit.md` | датчик умирал молча (`0xC000013A`); корень найден (консольный сигнал всей группе), вылечен флагом + самолечением повтором; ждёт подтверждения сутками без разрывов. ⚠️ Контейнерный `gpu-manager` этому классу не подвержен вовсе — консоли нет |
| `bugs/28_gpu_watch_leaves_a_terminal_window_every_five_minutes.md` | пустое окно терминала каждые 5 мин (нашёл ВЛАДЕЛЕЦ, винил соседний проект). Заплатка соседнего агента (`wscript` вместо `powershell`) проверена — **0 окон**; корень вылечен переездом в контейнер. ⚠️ Документ на английском — перевести |

---

## 🗺 Где искать подробности

| Что нужно | Куда идти |
|---|---|
| Как жил проект, закрытые сессии и фазы | **`PROJECT_HISTORY.md`** (append-only летопись) |
| Правила, карта, харнесс, конвенции | `AGENT_GUIDE.md` (канон, читать перед каждой задачей) |
| Уроки прошлых сессий (grep по тегу) | `EXPERIENCE.md` (EXP-0001…EXP-0061) |
| **Вопрос владельцу страницей, ответ в один клик** | **`plans/24`** · `tools/review.mjs` · `interviews/decisions/README.md` |
| Дорожная карта от цели | `MASTER_PLAN.md`, видение — `GOAL.md` |
| **Активаторы целиком** | **`plans/19`** (механика с числами) · `researches/16` (разведка) · `researches/22` (данные) |
| Голосовые фазы Г1–Г5 | `plans/11`–`plans/15`, лёгкий движок — `plans/18` |
| Разведка по внешней правде | `researches/` (02 стек · 09 OSS · 14 голос · 19 ревизия · **27 кванты Qwen3.8 под 16 ГиБ** · **28 холодный старт**) |
| **Скорость «от запроса до первого токена»** | **`researches/28`** · замер: `node tools/cold-ttft.mjs <alias>` |
| Справочник фреймворка KAIF | `.kaif/KAIF_REFERENCE.md`, обзор — `KAIF_FRAMEWORK.md` |

**Состояние каркаса:** `npm run kaif:check` — зелёный. Работаем только в `main`, коммитим часто,
`git diff --stat` перед каждым коммитом.
