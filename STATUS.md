# KLAS — Krinik Local Agent System — Текущий статус

> Этот файл агент читает перед каждой задачей. Обновляй его при каждом значимом изменении состояния.
> Это ГЛАВНАЯ передача между сессиями: новая сессия агента стартует с пустым контекстом и должна стать
> продуктивной по одному этому файлу. Пиши соответственно — конкретно, с путями и командами.
> 🧠 Главный мыслительный принцип — `PHILOSOPHY.md` (ПРОСТОТА: KISS + Оккам). Рабочий фреймворк —
> в `AGENT_GUIDE.md`.

---

## Что сделано

### Фаза 0 — Фундамент ✅ (2026-07-02)
- KAIF v1.1 развёрнут; `GOAL.md` → `MASTER_PLAN.md`; аудит окружения
  (`researches/01_local_nomad_audit.md`); git (только `main`); баг 01; интервью 001.

### Фаза 0.5 — Рождение KLAS ✅ (2026-07-02)
- Система живёт в `F:\KLAS\`: корень = git-репозиторий-каркас, инфраструктура — поддиректории вне
  git (`.gitignore`; версионируются только текстовые конфиги: `llamacpp/bat/`, `caddy/Caddyfile`,
  `docker-compose.yml`). Remote `origin` = github.com/MikalaiKryvusha/KLAS.
- Логотип владельца `logo/KLAS.jpg` (OG 1200×630, кот 👍) — hero в `README.md` (стиль KAIF-репо).
- Идея 04 владельца исполнена (2026-07-03): следы разовой стартовой миграции вычищены из документов
  и инструментов — фокус только на видении KLAS.

---

## Где мы сейчас

**Видение оформлено идеями владельца:**
- `ideas/03_klas_single_unit_self_deploy.md` — KLAS = одна законченная единица; репозиторий —
  обёртка/каркас/фрейм; развёртывание на пустой ПК = `git clone` + одна команда deploy, которая
  **сама скачивает** тяжёлое (движок, модели, docker-образы, .zim).
- `ideas/05_Jarvis.md` — 💡 на будущее (после основы): голосовой человекоподобный ИИ-ассистент
  (навыки-CRUD, управление Windows 11, зрение экрана, память с ротацией, голос Вихрова).

⚠️ **Требует внимания:**
1. **Первый push в origin не сделан** — заблокирован разрешениями Claude Code. Владелец может сам:
   `git push -u origin main`, ЛИБО разрешить push агенту в настройках.
2. **Наследие в `F:\LOCAL_NOMAD\`** ждёт решения владельца (вопросы — в идее 03): `OLLAMA_MODELS`
   ~46 ГБ (⚠️ ollama автозапускается при старте ПК — обнаружено 2026-07-02), `AnythingLLM` ~10 МБ,
   `1.txt` (bcrypt-хеш, в git ему не место).
3. `/revision` мастер-плана: вписать фазу самораскрытия (идея 03) и горизонт Jarvis (идея 05).

| Фаза | Статус |
|------|--------|
| 0 Фундамент · 0.5 Рождение KLAS | ✅ |
| 1 Стабилизация стека (баг 01) | 🔧 в работе |
| 2 Исследование оптимального стека | 🔲 |
| 3+ (см. `MASTER_PLAN.md`) + идея 03 (self-deploy) + идея 05 (Jarvis) | 🔲 |

---

## 🤖 Пул автономного беклога (не нужен человек / особое железо)

- [ ] **Баг 01 — диагностика:** запустить `llamacpp\bat\gemma4-12b.bat`, снять факты (`/health`,
      `/props`, `nvidia-smi`, `netstat :8080`), проверить гипотезы `-c 256000` и конфликта порта
      8080 с kiwix (`bugs/01_context_overflow_zoo_code.md`).
- [ ] **Тестовый харнесс:** `tools/health-check.ps1` (health + props + VRAM + порт одной командой).
- [ ] **Определить максимальный корректный контекст gemma-4-12b** и VRAM-бюджет модель+KV.
- [ ] **Исследование Фазы 2:** веб-исследование стека на июль 2026 (модели ≤16 GB VRAM, движки,
      фронтенды) → `researches/02_optimal_stack_2026.md`.
- [ ] **План идеи 03:** спроектировать `tools/deploy.mjs` (манифесты версий/URL/SHA; идемпотентность;
      dry-run) → `plans/02_self_deploy.md`; заодно решить, какие конфиги homepage вернуть в git
      (сейчас `homepage/` целиком в .gitignore).

## ❓ Ждёт ревью человека

- 🚀 **Push:** `git push -u origin main` (или дать агенту разрешение на push).
- ❓ Наследие `F:\LOCAL_NOMAD\` — вопросы в `ideas/03_klas_single_unit_self_deploy.md`
  (ollama + 46 ГБ, AnythingLLM, `1.txt`).
- ❓ Интервью 001 — лицензия (Q2), границы автономии (Q3), docker-сервисы (Q4).

---

## Где продолжить в следующей сессии

1. Проверить, сделан ли push (`git log origin/main..main`); если нет — см. выше.
2. Ответы владельца по идее 03 / интервью 001 → внести решения; при отмашке — убрать наследие
   `F:\LOCAL_NOMAD\` (46 ГБ!) и автозапуск ollama.
3. Взять баг 01 (диагностика — первый пункт автономного пула). Помни: порт 8080 делят
   llama-server и kiwix; docker после перезагрузки контейнеры НЕ поднимает (проверено 2026-07-02).
4. Если владельца нет и вопросов нет — `/autoloop` по пулу выше.

---

## Открытые баги

- 🔴 `bugs/01_context_overflow_zoo_code.md` — Zoo Code падает с переполнением контекста
  (llama.cpp + gemma-4-12b).
