# KLAS — Krinik Local Agent System — Текущий статус

> Этот файл агент читает перед каждой задачей. Обновляй его при каждом значимом изменении состояния.
> Это ГЛАВНАЯ передача между сессиями: новая сессия агента стартует с пустым контекстом и должна стать
> продуктивной по одному этому файлу. Пиши соответственно — конкретно, с путями и командами.
> 🧠 Главный мыслительный принцип — `PHILOSOPHY.md` (ПРОСТОТА: KISS + Оккам). Рабочий фреймворк —
> в `AGENT_GUIDE.md`.

---

## Что сделано

### 🚀 Релиз 1.0 «Universal KLAS» ✅ (2026-07-04)
- Опубликован https://github.com/MikalaiKryvusha/KLAS/releases/tag/v1.0 (Latest, тег `v1.0`,
  package.json → 1.0.0). README переписан заново (герой `KLAS_latest.jpg`, без истории версий),
  README.pdf перегенерён, release notes `docs/RELEASE_1.0.md`. План — `plans/06_release_1_0.md`.
- В релизе: чат Open WebUI для родных + поиск Kiwix из чатов (идея 11), трей + ярлыки Run/Stop/Panel
  (идеи 10/13), **умный мастер-установщик** с каталогом баз знаний (идея 12, `tools/install.mjs` +
  `tools/lib/*`, план 05), безопасность (секреты ротированы, история вычищена).

### Фаза 0 — Фундамент ✅ (2026-07-02)
- KAIF v1.1 развёрнут; `GOAL.md` → `MASTER_PLAN.md`; аудит окружения
  (`researches/01_local_nomad_audit.md`); git (только `main`); баг 01; интервью 001.

### Фаза 0.5 — Рождение KLAS ✅ (2026-07-02)
- Система живёт в `F:\KLAS\`: корень = git-репозиторий-каркас, инфраструктура — поддиректории вне
  git (`.gitignore`; версионируются только текстовые конфиги: `llamacpp/bat/`, `caddy/Caddyfile`,
  `docker-compose.yml`). Remote `origin` = github.com/MikalaiKryvusha/KLAS.
- Логотип владельца `logo/KLAS.jpg` (OG 1200×630, кот 👍) — hero в `README.md` (стиль KAIF-репо).
- Идея 04 владельца исполнена (2026-07-03): следы разовой стартовой миграции вычищены из документов
  и инструментов — фокус только на видении KLAS.

---

## Где мы сейчас

**2026-07-29 (утро, блок 2) — 🗣 ДВА ДЕФЕКТА ЖИВОГО ДИАЛОГА ОТ ВЛАДЕЛЬЦА: голос выправлен, уши
разведаны и вылечены опцией:**

- **✅ `bugs/11` ЗАКРЫТ — голос больше не меняется посреди реплики.** Жалоба: «русский мужской, а
  английское слово — женский британский; нужно ОДИН голос». Корень: рот собирает реплику из ДВУХ
  моделей Silero, а английский дефолт `en_0` был взят агентом «первым в списке» и оказался женским
  (Silero не сообщает пол дикторов `v3_en`). **Голос выбран ИЗМЕРЕНИЕМ**, а не на слух: у `eugene` и
  всех 118 английских дикторов замерены F0 и спектральный центроид → **`en_23`: F0 93.8 Гц, ровно как
  у `eugene`** (прежний `en_0` — 202.5 Гц). Новый инструмент харнесса **`tools/voice/pitch-check.py`**
  (`--match` — перебор дикторов; без флага — охранник единства регистра, FAIL при расхождении >25%);
  проверен на сломанной версии: `en_0` → FAIL 115.8%, `en_23` → PASS 0.1%. Флаг `--voice-en` даёт
  владельцу сравнить кандидатов ушами (`en_23` · `en_46` · `en_112`) без правки кода.
- **🟡 `bugs/10` — уши не умеют латиницу и пунктуацию. Лечение НАЙДЕНО, развёрнуто ОПЦИЕЙ, ждёт
  вердикта владельца на живом микрофоне.** Дефект доказан алфавитом: `tokens.txt` нынешней модели —
  **34 токена**, ни одной латинской буквы, ни одного знака препинания, даже «ё» нет. Это предел
  модели, а не поломка. **Разведка `researches/13_multilingual_stt.md`** (правило владельца: гуглёж до
  кода) нашла независимое подтверждение симптома (Хабр 2026: у GigaAM v3 «Gemini превращается в
  „Jemni“») и — главное — **что менять стек НЕ нужно**: у того же GigaAM-v3 от того же автора есть
  выпуск **`…-ctc-punct-…`** в том же формате NeMo-CTC → **257 токенов: 54 с латиницей, 78 с
  пунктуацией, заглавные, «ё»**. Развёрнут в `voice\models\gigaam-v3-ctc-punct\`, включается
  `node tools/voice-talk.mjs --ears punct`. **Замер A/B честный и не единогласный:** punct закрывает
  обе жалобы (`Привет, Николай. Как дела?` · `…называется Machine Lirning, и она…`), но на плотных
  ИТ-терминах валятся обе модели, а латиница выходит с опечатками. Фикстура была СИНТЕЗИРОВАНА —
  судить должен микрофон владельца. **Дефолт намеренно НЕ переключён:** модель ушей — элемент стека.
- **✅ `bugs/09` ЗАКРЫТ — стриминг больше не глохнет.** Нарезка брала САМУЮ РАННЮЮ границу
  предложения и выходила из цикла, если кусок короче порога: на ответе «Привет! У меня пока нет
  имени…» границей навсегда оставался «Привет!» (8 символов < 12), и весь ответ уезжал ОДНИМ куском.
  Фикс — `firstSentenceCut()` ищет первую ПОДХОДЯЩУЮ границу. Охранник **T0** в бенче проверяет
  нарезку детерминированно, на фикстуре строк, ДО обращения к ядру (прежний бенч этот класс поймать
  не мог: он зависел от того, что сегодня ответит модель). Проверен на старой логике: FAIL 2 против
  PASS. Наблюдение: «Привет! Как тебя зовут?» → было `1/1`, стало **`3/3`**, TTFA **6.2 с**.
  ⚠️ Попутный урок: первая версия фикстуры была НЕВЕРНОЙ (ждал 3 куска там, где контракт даёт 2) —
  охранник краснел бы на исправном коде. Поймано тем, что его прогнали на ОБЕИХ версиях до того,
  как ему поверили.

- **🏁 ВПЕРВЫЕ бенч прогнан В ОКРУЖЕНИИ ВЛАДЕЛЬЦА (без `PYTHONIOENCODING`) — 35/35.** Это и было
  лечение EXP-0018: до сегодня харнесс жил в привилегированной среде и потому был слеп.
  Все три сегодняшних фикса подтверждены на боевом конвейере; единицы звучат словами
  («около двадцати двух градусов Цельсия», «сорока пяти миллиметров»), смесь языков работает.
- **🐞 …и тот же прогон вскрыл `bugs/12`: одна из 35 галочек была ЛОЖНОЙ.** Кейс «единицы» получил
  **5/5** при ответе ядра `No response from OpenClaw.` — ход провалился, а бенч поставил полный балл:
  T1 судил по непустоте строки, T4 честно сверил звук с этой же заглушкой, T5 нашёл в ней кириллицу.
  **Весь набор проверок оставался зелёным на провалившемся ходе** — и это в инструменте, который
  существует ИМЕННО чтобы не отдавать владельцу сырое. Фикс: T1 отвергает строки-заглушки ядра
  (детектор проверен с обеих сторон: ловит 4 формы отказа, не трогает настоящие ответы).
  ⚠️ Честная граница: это ЧЁРНЫЙ список; правильное лечение — белый (`finish_reason: stop` из SSE),
  оно записано в документе бага как следующий шаг. **Перепрогон после фикса: 35/35, балл честный** —
  кейс «единицы» получил настоящий ответ. `voice-say.mjs` (путь TTS-провайдера ядра) отдельно
  проверен на регрессию после правки сайдкара — чист.
- **🐞 Смежная находка (НЕ чинена намеренно):** фикстура кейса «единицы» спрашивает про RTX 5070 Ti,
  а модель отвечает, что такой видеокарты не существует, — вопрос зависит от ЗНАНИЙ модели, а не от
  разворота единиц, который обязан проверять (класс EXP-0013). Требование владельца при этом покрыто
  кейсом «длинный ответ» («двадцати двух градусов Цельсия», «сорока пяти миллиметров»). Правильно —
  проверять разворот ДЕТЕРМИНИРОВАННО на уровне сайдкара. Не правил на финише сессии: правка теста
  без перепрогона и есть то непроверенное изменение, за которым охотится `/fable-judge`.
- **📚 `researches/14_voice_gold_standard.md` — КАРТА ПОЛЯ (заказ владельца).** Тракт до сих пор
  строился по дефектам; карты золотых стандартов не было. Собрано: вердикт индустрии 2026
  (**производственный выбор — каскад**, то есть наш путь верен; full-duplex Moshi ~200 мс — горизонт,
  не сегодня) · turn-taking как **ТРИ слоя** (акустический VAD → endpointing 800–1200 мс →
  семантический детектор конца хода) · бюджет **barge-in <150 мс** (flush TTS <60, отмена LLM <40,
  ложные перебивания <2%) · **AEC обязателен ПЕРЕД VAD** · постадийный бюджет латентности.
  **Главное — §4 инвентарь паритета (17 строк): 7 ✅ · 2 🟡 · 8 ❌.** Наш рот уже в норме эталона
  (первый байт 80–110 мс при норме 75–200), контроль по звуку — выше типовой практики; но нет НИ
  ОДНОГО слоя живого разговора, а **AEC — порядковый блокер** (без него VAD услышит голос самого
  ассистента). Предложенный порядок: AEC → Silero VAD + endpointing → barge-in; переписывать тракт
  на Pipecat/LiveKit НЕ предлагается (взять из них идеи и по одной OSS-детали). 4 вопроса владельцу.

**2026-07-29 (утро) — 🔴→✅ БОРМОТАНИЕ ГОЛОСА ПОБЕЖДЕНО: причина доказана ПОБАЙТОВО, и она была
в ОКРУЖЕНИИ — ровно там, куда указывал владелец:**

- **Владелец был прав, агент — нет.** Он написал: *«Я ДУМАЮ, ЧТО ЧТО-ТО В МОЁМ ОКРУЖЕНИИ!! РАЗ АГЕНТ
  НЕ МОЖЕТ ВОСПРОИЗВЕСТИ ЭТОТ БАГ!»* — и это был верный след. Диагноз прошлой сессии («две голосовые
  сессии говорят в одни колонки», `plans/13` «Инцидент») **оказался неверным**: замок полезен, но
  дефект был другой и остался жив.
- **🐞 `bugs/08` ЗАКРЫТ: моджибейк UTF-8 → cp1251 на стыке Node ↔ Python.** Node пишет в сайдкар
  UTF-8, а питон на Windows открывает stdin-**пайп** в кодировке ЛОКАЛИ (ANSI этой машины = **1251**).
  «Привет, Николай.» приезжает в Silero как `РџСЂРёРІРµС‚, РќРёРєРѕР»Р°Р№.` — и рот честно проговаривает
  «Р-џ-С-Ђ…». Коварство: моджибейк состоит из **настоящих кириллических букв**, поэтому проходит
  охранник `bugs/06` насквозь, не даёт ни исключения, ни лога — **дефект только ЗВУЧИТ**.
- **📏 Доказано побайтово, а не «похоже».** Улики нашлись на диске (`voice-talk` не удаляет wav
  ответов): в файлах сессии владельца от 0:22 уши KLAS услышали «сверс валера» и «разраз р р р раз».
  Точный текст того хода взят из лога ядра; синтез моджибейк-версии дал **те же sha1 у обоих файлов**.
- **✅ Лечение — контракт в самом сайдкаре, а не переменная у вызывающего:**
  `sys.std*.reconfigure(encoding="utf-8", errors="strict")` в `silero_daemon.py` и `silero_say.py`.
  Теперь тракт не зависит ни от локали машины, ни от того, кто и откуда его запустил.
- **🐤 Охранник — канарейка кодировки.** `TtsDaemon.ready()` гоняет `Проверка кодировки — ёжик, 42.`
  Node → питон → Node и сверяет побуквенно; новый статус **`encoding-broken`** заставляет `voice-talk`
  и `voice-bench` ОТКАЗАТЬСЯ стартовать. Проверен на сломанной версии: краснеет, печатая
  `enc.in = cp1251` и сам моджибейк.
- **🚨 Главный урок процесса (EXP-0018): харнесс жил в ПРИВИЛЕГИРОВАННОМ окружении.** Claude Code
  задаёт своим процессам `PYTHONIOENCODING=utf-8:surrogateescape`; у владельца её нет (пуста и в
  User-, и в Machine-области). Эта переменная молча лечила тракт в каждом прогоне агента — отсюда
  «35/35» при бормотании у человека. **Зелёный бенч не гарантировал ничего.** Опыт: EXP-0018 (разница
  окружений) · EXP-0019 (порча, которая только звучит → ищи артефакт на диске и доказывай байтами).
- **🐞 Попутно найден `bugs/09` (OPEN, не чинён):** нарезка ответа по предложениям глохнет навсегда,
  если ответ начинается с КОРОТКОГО предложения (`buf.match` возвращает первое совпадение, порог
  `MIN_CHUNK_CHARS` его не пропускает → `break` на каждой итерации). Звук не портит, но обнуляет
  стриминг: весь ответ уходит одним куском. Чинить с охранником-случаем в бенче.
- **Проверено наблюдением в окружении ВЛАДЕЛЬЦА** (`PYTHONIOENCODING` снята принудительно): канарейка
  зелёная (`enc utf-8/utf-8`), синтез «Привет, Николай.» sha1 = эталон, сквозной `voice-talk --text` —
  2/2 фразы, уши слышат верные слова.

**2026-07-29 (ночь) — 🗣 ТРАКТ ЗАГОВОРИЛ НА ДВУХ ЯЗЫКАХ + ПОЯВИЛСЯ БЕНЧ, СУДЯЩИЙ ПО ЗВУКУ:**

- **⚠️ Сначала о провале процесса, он важнее фич.** Перестроенный тракт был отдан владельцу как
  «проверенный»: файлы создавались, тайминги печатались, TTFA замерен. **Содержимое звука не
  проверялось ни разу.** Владелец получил сырую работу и сказал прямо: *«прежде чем мне сырое гавно
  отдавать в тесты»*. Лечение — не обещание быть внимательнее, а инструмент (ниже). Урок — EXP-0016.
- **🚨 «Сломанный звук» оказался НЕ дефектом кода.** Владелец слышал кашу («сррс рсрср срр») при верном
  тексте. Три гипотезы по коду не воспроизвелись; воспроизведение ЕГО сессии (агент сам жал Enter,
  играл вопрос в колонки, писал ответ микрофоном) показало **три всплеска речи вместо одного**. Причина:
  параллельно работали ДВЕ голосовые сессии — диалог владельца и звуковой бенч агента, — и обе говорили
  в одни колонки. Кашу устроил агент, гоняя `--audible` тесты на машине человека. Лечение —
  **замок единственной сессии** `tools/voice/single-instance.mjs`. Разбор — `plans/13` («Инцидент»),
  урок — EXP-0017.
- **🧪 `node tools/voice-bench.mjs` — бенч голосового тракта (заказ владельца).** Судит по ЗВУКУ:
  T1 ход завершён · T2 у каждой фразы свой файл · T3 файл не менялся при проигрывании (sha до/после —
  улика затирания) · T4 звук распознан ушами GigaAM и совпал с текстом · T5 кириллица озвучена.
  `--acoustic` замыкает петлю **через микрофон** (идея владельца): играет вслух и слышит комнату.
  Гоняет ТОТ ЖЕ конвейер (`tools/voice/pipeline.mjs`), что и живой диалог. **Норма — 35/35.**
  Охранник проверен на сломанной версии: T3 печатает «ЗАТЁРТ во время воспроизведения».
- **🗣 Двуязычность в одной реплике (требование владельца):** текст режется ПО АЛФАВИТУ, русские куски
  синтезирует Silero `v5_ru`, английские — `v3_en`, склейка в одну дорожку. Проверено: «Эта технология
  называется machine learning» звучит двумя моделями. ⚠️ Грабля: `v3_en.pt` питоном не скачать —
  у `models.silero.ai` истёк SSL-сертификат (Windows цепочку принимает, Python нет); файл добыт
  `Invoke-WebRequest`, лежит в `F:\KLAS\voice\models\`.
- **✅ `bugs/06` ЗАКРЫТ полностью** (`06_DONE_…`): числа произносятся словами (`56` → «пятьдесят
  шесть»), латиница — английской моделью, единицы разворачиваются в полные слова («115 кб/c» →
  «сто пятнадцать килобайт в секунду», требование владельца), markdown вычищается. Молчания больше нет.
- **⚠️ Честная граница:** КАЧЕСТВО английского произношения бенч не проверяет — уши GigaAM понимают
  только русский. Английский пока судит только владелец.

**2026-07-28 (ночь) — ⚡ ГОЛОСОВОЙ ТРАКТ УСКОРЕН ВТРОЕ: 24.5 → 7.6–8.0 с (жалоба владельца «всё
медленно» вылечена архитектурно):**

- **🔎 План ошибался в приоритете, и это поймал замер.** План 13 ставил первым стриминг TTS. Сложил
  стадии из его же замера: уши 1.5 + ядро 10.4 + рот 5.0 = **17 с при итоге 24.5 с**. Семь секунд не
  принадлежали ничему. За расхождением нашлись **~11 с перезапуска процессов**: `openclaw agent
  --local` поднимал агента заново КАЖДЫЙ ход (**8.4–10.6 с**), а питон-сайдкар Silero грузил модель
  заново на КАЖДУЮ фразу (**2.2 с**). Цель вдвое крупнее той, куда целился план.
- **📚 Разведка перед кодом (правило владельца): `researches/12_voice_latency_streaming.md`.** Золотой
  стандарт: метрика — **TTFA** (время до первого звука), бюджет облачного хода ~800 мс, но для
  локального CPU/скромной GPU индустрия сама называет 1.2–1.5 с; 90%+ времени петли сидит в
  `LLM TTFT + TTS TTFB`, стриминговый STT почти бесплатен. Приёмы: параллельный конвейер, нарезка по
  границам фраз, модель держать загруженной.
- **✅ Лечение — резидентность, без единой новой зависимости** (Оккам: всё собрано из уже развёрнутого):
  1. **Ядро резидентное.** Нашлось в первой строке справки CLI: `openclaw agent` **без `--local`
     исполняет ход через гейтвей**. Взят ещё более прямой путь — `POST /v1/chat/completions`
     (`stream: true`) прямо на гейтвей: тот же кодовый путь, ноль запусков процессов, SSE-дельты.
     Включается ключом `gateway.http.endpoints.chatCompletions` (внесён и в рабочий конфиг, и в
     git-шаблон `openclaw/openclaw.json.example`).
  2. **Рот резидентный** — новые `tools/voice/silero_daemon.py` + `tools/voice/tts-daemon.mjs`:
     модель Silero живёт в процессе, синтез фразы **3.7 → 0.08–0.11 с** (RTF 0.05).
  3. **Стриминг с нарезкой по предложениям** — сделан, но **честно: даёт лишь 0.1–1 с**. Замер
     объяснил почему: ядро молчит ~5 с (префилл), затем выдаёт весь ответ за полсекунды — перекрывать
     нечего. Оставлен как задел (длинные ответы, barge-in Г4/Г5).
- **📏 Результат наблюдением:** сквозной прогон с ушами — **8.0 и 7.6 с · TTFA 7.9 и 7.5 с**; без ушей
  6.0–7.0 с; живой смоук со звуком пройден; контекст беседы держится (на «а самое большое озеро там?»
  ядро верно поняло «там»). ⚠️ **Первый ход НОВОЙ сессии — 41.9 с** (полный префилл 20k промпта):
  сессию дня не пересоздавать, имеет смысл прогревать.
- **Следующий резерв назван:** остаток ядра ~5 с — это префилл **20-тысячного системного промпта**.
  Голый llama-server на коротком вопросе отвечает за **0.4–0.7 с**, то есть «модель поменьше»
  вычеркнута окончательно (уже второе независимое подтверждение).
- **Опыт:** EXP-0014 (расхождение суммы стадий с итогом — это и есть находка; план прошлой сессии —
  гипотеза, а не приказ) · EXP-0015 (запуск чужого CLI на каждый запрос — почти всегда главный
  источник латентности, и почти всегда у вендора есть резидентный режим).

**2026-07-28 (поздний вечер) — ✅ ПЛАН 16 ЗАКРЫТ + голосовой тракт перепроверен на b10167 (и это
принесло два дефекта):**

- **✅ Фаза 4 прогнана НЕ из-под администратора — план 16 получил `DONE`.** Лог доказывает права
  машинно: `IsAdmin = False`; ход — 23 тул-колла, **0 отказов**, `stopReason=stop`, 306.9 с; работали
  `App`, `Wait`, `Snapshot`, `Screenshot`, `Click`, `Type`. **EXP-0007 частично опровергнут:** понизить
  права изнутри админ-сессии МОЖНО — `runas /trustlevel:0x20000` (промпт передавать `--message-file`).
  Прогон писался экраном целиком (645 кадров, `screenrec\2026-07-28T19-00-19_rec`).
- **🐞 `bugs/07` — `Type` искажает символ.** Транскрипт сессии: модель послала `KLAS UIA test` ТРИЖДЫ,
  а в Блокноте осело `KLAS IIA test`. Подтверждено строкой ОС (заголовок окна), не картинкой, — поэтому
  вердикт EXP-0010 «дефекта не было» поправлен: дефект есть, но **перемежающийся** (в прогоне 22:00 тот
  же `Type` сработал верно). Корень не установлен, лечение не применялось.
- **🔧 Тест фазы 4 чинён: он мог проходить по НЕВЕРНОЙ причине.** Блокнот Windows 11 хранит
  несохранённую вкладку между запусками — текст от прошлого прогона уже лежал в редакторе, и критерий
  «текст виден» выполнялся сам собой. Теперь фикстура несёт **уникальный токен прогона**, а вердикт
  сверяется с ОС (`promptUi(token)`, `notepadTitles()`, вердикт `FAIL-UI-UNVERIFIED` в `llamacpp-probe`).
- **🎙 Голосовой тракт: аудио-половины чисты, ядро замедлилось.** `voice-roundtrip` — те же **97%**
  (59/61). А вот ход: **24.5 · 25.7 · 26.7 с** (ядро 10.1–10.8 · уши 1.3–1.7 · рот 4.4–5.3) против базы
  20–22 с (ядро ~7) → **критерий Г3 «<25 с» больше не держится**. Гипотеза «виноваты схемы Windows-MCP»
  ПРОВЕРЕНА И ОТВЕРГНУТА (1.8k токенов из 20.5k). Остались два подозреваемых, разделяемые только A/B на
  стенде: смена билда (b9538→b10167) и смена основной модели (базу мерили до перехода на qwen3.6).
  Важное для оптимизации: в ходе 20 560 токенов промпта (19 576 из кеша) при выходе **4 токена** —
  время уходит НЕ на генерацию, значит «модель поменьше» проблему не решит; бить надо в стриминг TTS.
- **🐞 `bugs/06` — реплика без русских букв роняла ВЕСЬ голосовой ход.** Ядро ответило `56.` → Silero
  кинул `ValueError` → каскад упал стеком. Перебор: `56`, `56.`, `OK.` падают, `Ответ: 56.`,
  `42 плюс 14.`, `Минск.` — нет. **Крах устранён** (охранник кириллицы в `voice-say.mjs`, код выхода 2;
  `voice-talk` переживает случай и продолжает диалог — проверено тем же падавшим прогоном). Полное
  лечение (числа→слова, произношение латиницы) — владельцу: это его же жалоба на двуязычность.
- **Опыт:** EXP-0011 (штатный `runas /trustlevel` против «средствами ОС нельзя») · EXP-0012 (тест,
  чей результат переживает прошлый прогон, проверяет историю → уникальный токен) · EXP-0013 (фикстура
  из десяти русских фраз не проверяет тракт, а проверяет себя).

**2026-07-28 (вечер) — 🏆 БЛОКЕР СНЯТ: ядро управляет Windows UI, движок обновлён, бисекция не
понадобилась:**

- **Корень «дефекта движка» оказался НАШИМ.** Встроенный тул OpenClaw `cron` объявляет
  `trigger.script` с `maxLength: 65536` → генератор GBNF выдаёт `char{1,65536}`, а llama.cpp режет
  всё выше `MAX_REPETITION_THRESHOLD = 2000` → `400 … failed to parse grammar` на ЛЮБОМ ходе, даже
  «привет», даже с выключенным MCP. Лечение — одна строка в конфиге ядра:
  `compat.unsupportedToolSchemaKeywords: ["pattern","maxLength","minLength","maxItems","minItems"]`
  (тем же приёмом сам OpenClaw лечит модели xAI). Внесено и в `~/.openclaw/openclaw.json`, и в
  git-шаблон `openclaw/openclaw.json.example`.
- **Движок обновлён: `llamacpp\` = b10167** (28.07.2026). Проверено стендом на обеих границах
  диапазона (b9760 и b10167 — оба PASS после фикса схем), затем в бою: `health-check` зелёный,
  `llama-server --version` = `10167 (ee3d1b54c)`, ход ядра через 8080 отвечает. Откат — `llamacpp-b9538\`.
- **✅ Фаза 4 плана 16 ВЗЯТА:** ядро живьём открыло Блокнот, выполнило `Click` и **напечатало текст**
  (`Type`) — то, что на b9538 убивало ход. Поправка к тесту: новый Блокнот Windows 11 не отдаёт
  область ввода в UIA, поэтому `Type` идёт по `loc` (координаты), а не по `label`.
- **Новые инструменты харнесса:** `tools/llamacpp-probe.mjs` (стенд смены билда: качает релиз, поднимает
  движок на 5900, гоняет НАСТОЯЩИЙ ход ядра; `--ui` — живой UI-тест) и `tools/screen-rec.mjs`
  («зрение» агента: запись экрана 1 кадр/с, идея владельца) — оба в таблице харнесса `AGENT_GUIDE.md`.
- **Канон пополнен правилом владельца:** перед крупной/эпик-фичей — обязательный гуглёж и документ
  разведки в `researches/` (состав документа и признаки эпика — `AGENT_GUIDE.md`).
- **Опыт:** EXP-0009 (первым подозреваемым при «сломалось после обновления» держи свои схемы; читай
  ошибку с НАЧАЛА вывода) · EXP-0010 (зрение врёт на ужатом кадре — для чтения текста только `--crop`
  в нативном масштабе; на этом я выдумал несуществующий дефект ввода, владелец поправил).

**2026-07-28 (день) — живой UI-тест прогнан + поиск корня дефекта тул-коллов:**

- **Фаза 4 плана 16 ПРОГНАНА по «go» владельца (4 прогона).** Ядро управления Windows UI доказано
  ЧАСТИЧНО: `App launch_executable` (Блокнот реально открылся — подтвердил владелец), `Wait`,
  `WaitFor`, `Snapshot` (UIA-дерево читается) работают, модель верно взяла label области
  редактирования (**475**). Ход умирает на первом ДЕЙСТВУЮЩЕМ тул-колле (`Type`/`Click`).
- **🔎 Корень: не модель и не Windows-MCP, а движок.** Апстрим
  [llama.cpp#24807](https://github.com/ggml-org/llama.cpp/issues/24807) — ровно наша модель и симптом
  (peg-native роняет XML-тул-колл Qwen3.6) — **закрыт 21.06.2026, рабочий билд после b9754**; наш
  движок собран 06.06.2026 = **b9538**, старше фикса. Это переворачивает диагноз EXP-0003: модель
  выдаёт свой штатный формат, парсер хоста его не умеет.
- **⚠️ Обновление на свежий b10167 НЕ подошло — откачено.** На нём падает ЛЮБОЙ ход ядра
  (`400 Failed to initialize samplers: failed to parse grammar`), даже «привет» и даже с выключенным
  Windows-MCP: генератор GBNF ссылается на неопределённые правила (`value ::= object | array | …`).
  Заведён **`bugs/05_llamacpp_b10167_toolcall_grammar.md`** (улика — грамматика из stderr движка).
  Боевой стек возвращён на b9538 и проверен (ядро отвечает, MCP 7 тулов, `/health` OK); сборка
  b10167 лежит в `F:\KLAS\llamacpp-b10167` для бисекции.
- **✅ Починен ретрай моделей (задание владельца).** `fallbacks` не срабатывали НИ РАЗУ, потому что
  **явный `--model` обнуляет цепочку** (`modelOverrideSource:"user"` → пустой список; прочитано в
  исходниках OpenClaw 2026.7.1 и подтверждено живым прогоном `qwen3.6 → qwen3.6-r → qwen3.5`).
  Правило вписано в `AGENT_GUIDE` и в команду фазы 4 плана 16.
- **Прочее, оплаченное прогоном:** одобрений на MCP-действия при `agent --local` НЕТ (Control UI не
  поднят); понизить права из административной сессии не удалось ни `explorer.exe`, ни COM — лечится
  средой (не держать VS Code от админа); фаззи-`App switch "Notepad"` на русской Windows уводит в
  **Notepad++**, запускать строго по пути .exe.
- **Опыт пополнен 4 записями:** EXP-0005 (`--model` выключает fallback) · EXP-0006 (обновление движка
  проверяй ходом ЯДРА, не curl-ом; держи откат папкой) · EXP-0007 (права/локализованный grep) ·
  EXP-0008 (стенд на отдельном порту + stderr движка называет виновника).

**2026-07-18 — наработки, которых не было в документах (внесены задним числом 2026-07-28):**

- **Android-нода OpenClaw подключена и approved** (телефон Krinik's Titan1, Android 16) — но **через
  USB**, не по tailnet: нода → `ws://127.0.0.1:18789` → `adb reverse tcp:18789 tcp:18789` → гейтвей.
  Пейринг `openclaw qr --url ws://127.0.0.1:18789 --setup-code-only`, одобрение `openclaw nodes approve`.
  **Мешали:** конфликт NordVPN ↔ Tailscale (хост offline в tailnet), расхождение часов 6 мин
  (`DEVICE_AUTH_SIGNATURE_EXPIRED`), bootstrap оператора → **`gateway.bind` переведён `tailnet → loopback`**
  (только true-loopback даёт операторский scope). Нода живёт, пока телефон на USB.
- **Windows-MCP v0.8.2 установлен и подключён** (`F:\KLAS\mcp\Windows-MCP-v0.8.2`, uv-бинарь в
  `F:\KLAS\mcp\uv\`, венв Python 3.13.14), allow-list в два слоя = **ровно 7 безопасных тулов**
  (`App, Snapshot, Screenshot, Click, Type, Wait, WaitFor`), опасных нет. План — `plans/16`.
- **⚠️ Токен гейтвея светился локально** (в session-логах и удалённом прокси) — по-хорошему ротировать.
  В git не утекал.

**2026-07-26 — 🔧 KAIF обновлён 1.2 → 1.6 «Homeostatic KAIF» ✅ (обёртка проекта, не продукт):**
- **Маршрут:** проект был на 1.2 = без машинерии, поэтому legacy-путь — тонкий `KAIF.md` →
  `KAIF-LOADER.mjs` → механический установщик как ОБНОВЛЕНИЕ. Механический проход чистый: из 64 файлов
  предварительной резервной копии изменились РОВНО 2 (`.kaif/kaif.json`, `package.json`) — контент
  владельца (`GOAL`/`STATUS`/`MASTER_PLAN`/карты/директории знаний) не тронут вообще.
- **Что нового в проекте:** `TESTING_FRAMEWORK.md` (7 принципов + маркеры доверия
  `[NOT-TESTED]`/`[TESTED: …]`), `EXPERIENCE.md` (журнал уроков, засеян 4 реальными уроками KLAS),
  **26 скиллов** (+5: `/experience` и семейство `/fable-method` `/fable-loop` `/fable-judge`
  `/fable-domain` — дисциплина исполнения), `.kaif/spheres/`, машинерия `.kaif/kaif-core.mjs`
  (`npm run kaif:version|check|update`). AGENT_GUIDE получил роутер контекста, артефакты разведки,
  гигиену git, write-гейт и метки `[AI]`; BUG_FIXING — гейт `INTENT:`, проверку `TWINS:`, охранников;
  PHILOSOPHY — «учись один раз», «наблюдение вместо домысла», «три двери».
- **🇷🇺 Решение владельца:** обёртка **остаётся русской** (в 1.6 апстрим держит агентские документы
  по-английски + русские триггер-алиасы). Смыслы 1.3–1.6 перенесены в русский текст вручную. **Цена
  зафиксирована:** 44 из 52 файлов слепка числятся «разошедшимися» → каждое будущее обновление KAIF
  требует РУЧНОГО переноса. Порядок работы — в скилле `/kaif-update` («Особенности ИМЕННО ЭТОГО
  проекта»). Намеренно оставлены английскими и потому обновляются механически: 3 файла
  `fable-method/references/*` и 5 неиспользуемых библиотек сфер (проверено: 8/8 байт-идентичны).
- **Решение владельца:** скиллы развёрнуты под **все пять** агентских систем (`.claude/` `.roo/`
  `.agents/` `.grok/` `.cline/` — 26 × 4 копии + канон). Копии раскладывает машинерия из канона
  `.claude/skills/` (`resyncCopies`), **руками их не правим**.
- **Доп. фиксы KLAS-стороны:** обёртка нормализована к LF (159/159; CRLF-эпоха 1.2 обнуляла бы
  sha-снимки); `.gitignore` расширен на новые артефакты KAIF (`.agents/` `.grok/` `.cline/`
  `.clinerules/` `EXPERIENCE.md` `TESTING_FRAMEWORK.md` + транзиентные) — KAIF по-прежнему вне git;
  **`tools/kaif.mjs` выведен из эксплуатации** (валидатор эпохи 1.2 печатал ложное «структура целостна:
  21 скиллов»); заглушка `<BUILD_COMMAND>` в сфере заполнена; карта проекта обновлена.
- **Проверка:** `npm run kaif:check` → 52 файла + 108 агентских артефактов ✅; `update-verify` пройден
  (113 копий пере-синхронизированы, установщик самоочистился); свой механический аудит — **30/30 ok, 0
  FAIL** (маркер, комплектность 26 скиллов, frontmatter, парность фенсов, литеральные токены
  `INTENT`/`AUTH`/`TWINS`/`PENDING`/`[TESTED:]`/`VERIFIED`, плейсхолдеры, синхронность копий, ссылки).
- **Новый инструмент:** `node tools/kaif-baseline-diff.mjs <версия> [--to <версия>]` — восстанавливает
  базовый слепок любой версии KAIF из релизного артефакта и диффает: показывает объём работы по
  переносу и наши кастомизации. Им же измеряли это обновление (15 изменено · 7 новых · 22 без изменений).
- **📄 Отчёт разработчику KAIF:** `researches/10_kaif_1.6_update_field_report.md` — 10 трудностей
  миграции с предложениями по механизму (главные: legacy-путь не даёт списка расхождений; `templateNotes`
  1.6 описывают 1.5 и не кумулятивны; «откатываемый дифф» через git не работает, когда обёртка
  gitignored; CRLF/LF обнуляет sha-снимки) + что сработало отлично.

**2026-07-17 — 🎉 OpenClaw ПРИНЯТ ЯДРОМ АССИСТЕНТА, выход из пилота — ступень 0 выполнена ✅:**
- **Решение владельца:** «принято, выходим из пилота» — Q1 интервью 004 закрыт окончательно.
  Зафиксировано: MASTER_PLAN (журнал + Фаза 8 шаг 1 ✅), research 09 §3.4, plans/08 (Блок 0),
  новый **план выката `plans/10_openclaw_core_rollout.md`** (ступени полномочий по Q4 инт. 004).
- **Ступень 0 развёрнута и проверена:** рабочий (дефолтный) профиль `~/.openclaw/openclaw.json` —
  провайдер klas, fallbacks `[qwen3.5-35b-r, gemma-4-12b]`, compat-фиксы; **exec зажат**
  (`tools.exec.security: allowlist`, `ask: off` — проверено отказом «allowlist miss» на reg query);
  **гейтвей/каналы/cron НЕ подняты**; дисциплина тул-коллов в `~/.openclaw/workspace/TOOLS.md`.
  Смоук: цепочка read→write честная (токен перенесён, 2 колла). Шаблон конфига — `openclaw/` в git
  (`openclaw.json.example` + README). Пилот-профиль оставлен песочницей для бенчей.
- **Ступень 1 выполнена ✅ (интервью 005 отвечено, все A):** гейтвей поднят — bind **только tailnet**
  (100.80.125.66:18789, auth token в `caddy/PASSWORD.local.txt`), Control UI проверен web-shot'ом
  (страница отдаётся, без токена не пускает); exec НЕ эскалирован (allowlist/deny); навыки ужаты до
  shortlist `[healthcheck, skill-creator]` (подтверждено системным промптом); гейтвей встроен в
  `tools/klas.ps1` up/down. Постоянный сервис — опция владельца: `openclaw gateway install`.
  **Видение владельца (Q4):** основной канал будущего — Android-нода с голосовым управлением,
  аутентификация — голос и паттерны владельца → голос-биометрия (plans/09, идея 14) = вход в канал;
  зафиксировано в plans/10 ступень 2. **Следующее по ядру — ступень 2** (Android-нода + Windows-MCP)
  — после голосового тракта (plans/07).

**2026-07-17 — мини-бенч цепочек OpenClaw ВЫПОЛНЕН ✅ (развилка «ядро» решена) + `/revision` ✅:**
- **`tools/openclaw-chain-bench.mjs`** — 6 градуированных задач-цепочек внутри агентного цикла
  OpenClaw (`agent --local --json`, профиль `pilot`, свежая сессия на задачу); фикстуры со случайными
  токенами — правильный выход ДОКАЗЫВАЕТ реальное чтение; + `meta.toolSummary`. Запуск:
  `node tools/openclaw-chain-bench.mjs <model>` (⚠️ только на свободном GPU — свапает модели).
- **Результаты (полная таблица и разбор — research 09 §3.4): qwen3.5-35b — 5.00/6.0** (цепочки
  честные; единственный дефект — многострочный write → срыв в XML-псевдо-формат тул-колла, ход
  падает, 2/2 воспроизводимо); **gemma-4-12b — 3.00/6.0** (в чужом цикле обрывает цепочки после
  первого шага, неудачные ходы 140–160 с — fallback в OpenClaw на ней не строить).
- **Вердикт Q1 инт. 004: ядро OpenClaw жизнеспособно с основной qwen3.5-35b.** До «ядра надолго»
  оставалось два условия; **первое закрыто (2026-07-17): write-дефект разобран и обойдён** — корень:
  qwen пишет фразу перед `<tool_call>`, peg-парсер llama.cpp этого не переносит (апстрим #20260,
  фикс не завершён). Меры: сэмплинг qwen прижат (temp 0.7/top-p 0.8/top-k 20/min-p 0 в
  `llama-swap/config.yaml`), дисциплина тул-коллов в TOOLS.md пилота, фейловер на ту же модель через
  алиас `qwen3.5-35b-r` (llama-swap aliases → fallbacks OpenClaw). Замер: 10/11 чистых (~9% флейка
  против 75% до) + сеть 5/5. Детали — research 09 §3.4 «Дожим». **Осталось второе условие:
  безопасность гейтвея (только tailnet, exec-approvals) + решение владельца о выходе из пилота.**
- **`/revision` мастер-плана выполнен:** фазы 1–6 закрыты релизом 1.0 и свёрнуты в историю; новые
  фазы — 7 (самораскрытие до идеала) и 8 (эпик ассистента конструктором из OSS); эволюция ядра LLM
  вынесена в «непрерывные процессы». Пункт «⚠️ /revision» снят.

**2026-07-16 — ревизия OSS-поля: «собирать KLAS конструктором из крупных готовых модулей»** (вопрос
владельца) — 🔬 исследование готово: `researches/09_oss_constructor_map.md`. Метод: снимок GitHub API
по ~60 репозиториям (точные звёзды/активность/лицензии) + веб-поиск (fetch-фаза упёрлась в лимит
сессии — сниппет-уровень помечен ⚠️). Ключевое: **OpenClaw** (экс-Clawdbot, 383k⭐, Node/TS, MIT,
навыки-SKILL.md, Android-нода, локальные LLM) — кандидат на готовое ядро ассистента; русский голосовой
тракт собирается из готового (**sherpa-onnx+GigaAM-v3** STT · **Silero v5** TTS-старт · клоны —
CosyVoice 3/Chatterbox V3/F5-TTS_RUSSIAN · каркас **Pipecat**); Windows-контроль — **Windows-MCP**
(6.4k⭐ MIT); Piper/Rhasspy заморожены (Piper → OHF-Voice/piper1-gpl, GPL). Ротацию памяти по лимиту
ГБ никто не даёт — пишем сами. **Интервью 004 отвечено ✅ (2026-07-16):** Q1 — короткий **смоук-пилот
OpenClaw** → выбор ядра на фактах; Q2 — клиент следует за ядром (OpenClaw-бекенд → его Android-нода);
Q3 — слепой A/B TTS-клонов (CosyVoice 3 / Chatterbox V3 / F5-TTS_RUSSIAN), до них Silero v5; Q4 —
полномочия над Windows ступенчато с allow-list. **+ Правило владельца: протухшие OSS не брать — только
живые** (зафиксировано в MASTER_PLAN, AGENT_GUIDE). Решения разнесены: plans/07 (новые дефолты:
Pipecat · sherpa-onnx+GigaAM-v3 · Silero v5), plans/08, idea 05.

**Смоук-пилот OpenClaw выполнен ✅ (2026-07-16, вердикт — research 09 §3.4):** v2026.7.1 поставлен
(`npm i -g openclaw`), изолирован в профиле `pilot` (`~/.openclaw-pilot`), llama-swap подключён
кастомным провайдером. Итог: **работает** — tool calling на основной qwen3.5-35b ✅ (после фикса
`compat.unsupportedToolSchemaKeywords:["pattern"]` — иначе llama.cpp 400 на схемах инструментов),
автофейловер моделей ✅, русский ✅, 53 навыка SKILL.md (18 ready), тёплый ход ~16 с / холодный
60–130 с. ⚠️ Модель срезает шаги цепочки (write без read) → перед решением «ядро надолго» нужен
мини-бенч цепочек в цикле OpenClaw + правила безопасности (гейтвей только tailnet, exec-approvals).
Голосовой realtime это не заменяет — Gateway-каскад (Pipecat) остаётся в плане 07.

**2026-07-07 — идея PAI (Психо-Акустический Интерфейс) + выбор личности Jarvis** — 🔬 исследование и
план (реализация НЕ начата, задел на будущее). Кластер документов в `ideas/psycho_acoustic_interface/`:
- **PAI** — подмодуль Jarvis (idea 05): по голосу формировать и играть в наушники психоакустические
  протоколы (сон/фокус/релакс — режим A) и звуковые уведомления (акустический CLI — режим B). Собран
  гуглёж (наука+безопасность+инструменты), причёсана идея, написаны 2 исследования и детальный план
  по 6 фазам (см. `ideas/psycho_acoustic_interface/README.md`). Ключевое: честная рамка «аудио-UX, не
  программирование мозга»; безопасность — обязательный слой; стек — свой генератор на Web Audio/Tone.js.
- **Jarvis (idea 05) дополнен:** **выбор личности** — Jarvis (муж., как у Тони Старка) ↔ **Joi** (жен.,
  по «Бегущему по лезвию 2049»: мягкая, добрая, услужливая; голос девичий, женственный, бархатный).
  Перед реализацией персон/голосов — `/interview`.
- **Связка хост ↔ Android-клиент ↔ носимое устройство** (вопрос владельца): 🔬 исследование
  `researches/07_jarvis_wearable_transport.md` + план `plans/07_jarvis_gateway_and_client.md`.
  Вывод: НЕ «обширный кастомный API», а **тонкий Jarvis Gateway** (realtime-голос WS/WebRTC + маленький
  control-API) поверх **уже развёрнутого Tailscale** и llama-server `/v1`; каскад STT(Whisper)→LLM→TTS(Piper)
  со стримингом+barge-in; on-device wake word на Android; голос только в tailnet (приватность). 5 фаз.
- **Интервью 003 отвечено ✅ (2026-07-07)** (`interviews/interview_003_jarvis_pai_android.md`): решения по
  эпику ассистента. **Q1: фокус на ЯДРЕ, но закладываемся под Jarvis** (в homepage — отдельный web-тул
  управления ассистентом, только для Криника под паролем). **Q2:** дефолт Jarvis, обе персоны на оба
  имени («коллега отдыхает»). **Q3:** PAI — только режим A + модуль **PAI-тренажёр** (калибровка
  «воздействие→отклик», PID-аналогия). **Q4:** Android-клиент — форк готовой базы (HA Companion App).
  **Q5:** голос — только tailnet. **⚠️ Принцип стека:** выбор языка по поддержке/сообществу/скорости/
  простоте (НЕ «Node по умолчанию»): realtime-аудио/ML — вероятно Python, control-API — Node.
- **Эпик-роадмап `plans/08_assistant_epic_roadmap.md`** — высокоуровневый секвенс всего эпика
  (Jarvis/Joi + PAI + носимый доступ) + «закладки в ядро» уже сейчас. Реализация — задел (Q1=A).

**2026-07-08 — идея 14: идентичность/доверие/защита владельца** (`ideas/14_...md`) — 🔬 гуглёж-исследование
+ план (задел). Профилирование собеседников, непрерывная модель доверия, идентификация/аутентификация,
**распознавание принуждения (duress)** — Jarvis ставит защиту владельца выше буквального исполнения.
Знания — `researches/08_jarvis_identity_trust.md` (голосовая биометрия ECAPA/SpeechBrain, anti-spoofing
ASVspoof, стилометрия, adaptive risk-based auth, duress/panic-коды). План — `plans/09_jarvis_identity_trust.md`
(6 фаз; каркас доверия можно начать рано). Вписано в эпик `plans/08` (Блок 8). Честная рамка: голос ≠
доказательство (подделывается) → многофакторность + step-up; решения вероятностны; всё локально/зашифрованно.



**2026-07-04 — баг 04: Open WebUI «под ключ» + веб llama-swap** (`bugs/04_openWEBUI_FCK_UP.md`) —
🔧 фикс применён и проверен браузером, ждёт подтверждения владельца:
- **Open WebUI turnkey** — родные регистрируются и СРАЗУ чатятся со всеми моделями, без ручных групп/прав.
  В `docker-compose.yml` (сервис `open-webui`): `ENABLE_PERSISTENT_CONFIG=false` (env — источник правды,
  лечит «БД перекрывает env»), `ENABLE_SIGNUP=true`, `DEFAULT_USER_ROLE=user` (без ожидания одобрения),
  `BYPASS_MODEL_ACCESS_CONTROL=true` (все видят все модели), `USER_PERMISSIONS_WORKSPACE_*_ACCESS=true`.
  Проверено playwright: новый юзер → сразу в чат, видит все 5 моделей (тестовый аккаунт удалён из БД).
- **llama-swap Playground 404** — в `caddy/Caddyfile` матчер `@swap_api` расширен на `/v1/*` и
  `/api/captures*` (Playground шлёт на корень `/v1/chat/completions`, раньше проваливался на homepage →
  404). Проверено: ответ модели 200.
- **llama-swap боковая панель** — не баг KLAS: залипший cookie `sidebar_state=false` (shadcn Sidebar).
  Лечение — очистить куки сайта / кликнуть гамбургер. Владелец подтвердил: почистил куки — панель есть.
- **Новый харнесс** `tools/web-shot.mjs` (playwright/Chromium): скриншот + ошибки консоли/сети — «глаза»
  агента в вебе; первый кирпич «зрения» Jarvis (идея 05). В таблице харнесса `AGENT_GUIDE.md`.

**2026-07-04 — баг 02 + идея 10 (управление KLAS):**
- **Баг 02** (`bugs/02_remote_access_via_funnel.md`) — фикс применён + полный homepage-over-funnel:
  **единый вход через Caddy** — локальный `:80` (docker `127.0.0.1:80`, БЕЗ пароля, пульт на
  `http://localhost/`) и публичный `:443` (funnel, basicauth). Общий сниппет `(klas_web)`: `/wiki/`
  (Kiwix), `/ui/` (llama-swap UI, нативный префикс), корень (пульт). Ссылки-виджеты ОТНОСИТЕЛЬНЫЕ —
  работают и локально, и через funnel. **Open WebUI** — root-приложение → отдельный порт funnel
  **:8443** напрямую (своя авторизация). llama-swap тихий фон. Funnel-лимит: только 443/8443/10000,
  но Caddy на 443 разводит сколько угодно сервисов по путям. Всё проверено (200 локально и через funnel).
- **Идея 10** (`ideas/10_start_stop_control.md`) — ✅ реализовано: трей-контроллер `tools/klas.ps1`
  (иконка «Кот Криник», меню Open/Stop, пуш при старте, up/down всего стека), скрытый лаунчер
  `klas-control.vbs`, ярлык `install-klas-shortcut.ps1`, ссылка на репозиторий в homepage
  (anonymize её убирает). **Автозапуск ставит владелец:** `powershell -File tools\install-autostart.ps1`.

**2026-07-03 — идея 07 выполнена ПОЛНОСТЬЮ + релиз MVP 0.1** (`ideas/07_DONE_remote_api_access.md`,
`plans/03_remote_and_mvp.md`):
- **Удалённый доступ** к LLM-API через интернет: `https://krinikspc.forest-ratio.ts.net/llm/v1` +
  Bearer-ключ (Caddy `/llm` → llama-swap; ключ в `caddy/PASSWORD.local.txt`; Zoo Code — `homeworks/01`).
- **Контекст расширен** (главный крик владельца): **Qwythos-9B — основная, 256K** (needle 148K ✅,
  agent-bench 6/6); qwen3.6 400 исправлен (→64K); все модели KV q8_0. Модели в `llama-swap/config.yaml`.
- **KAIF 1.2**: маркер, скиллы `/fix-vision` `/what-next`, Zoo Code-адаптер (`.roo/rules/kaif.md`
  авто-подгружает канон — лечит «модели забывают KAIF»).
- **KLAS ≠ KAIF**: KAIF вынесен из git-трекинга (3rd-party dev-фреймворк; файлы на диске, деплой
  умеет `--with-kaif`). См. принцип в `GOAL.md`.
- **Инструменты**: `tools/anonymize.mjs` (анонимный деплой), `tools/leaderboard.mjs` (свежие модели
  из HF API), `tools/sync-zoo-code.mjs`, автостарт llama-swap (`tools/install-autostart.ps1`).
- Исследования: `researches/03` (кванты GGUF), `04` (архитектуры), `05` (датасеты/лидерборды).

Прошлый контекст (Фаза 2 исследование) — в `researches/02_optimal_stack_2026.md`.

**Видение оформлено идеями владельца:**
- `ideas/03_klas_single_unit_self_deploy.md` — KLAS = одна законченная единица; репозиторий —
  обёртка/каркас/фрейм; развёртывание на пустой ПК = `git clone` + одна команда deploy, которая
  **сама скачивает** тяжёлое (движок, модели, docker-образы, .zim).
- `ideas/05_Jarvis.md` — 💡 на будущее (после основы): голосовой человекоподобный ИИ-ассистент
  (навыки-CRUD, управление Windows 11, зрение экрана, память с ротацией, голос Вихрова).

✅ **Опубликовано (2026-07-03):** первый push в origin сделан — https://github.com/MikalaiKryvusha/KLAS
(README с логотипом виден). Установлен GitHub CLI (`gh`), владелец авторизован (аккаунт
MikalaiKryvusha, keyring); git-email = noreply-адрес GitHub (защита приватной почты). Лицензия —
**MIT** (выбор владельца при создании репо; Q2 интервью 001 закрыт).

✅ `/revision` мастер-плана выполнен (2026-07-17) — фаза самораскрытия и горизонт ассистента вписаны.

> `F:\LOCAL_NOMAD` удалён владельцем (2026-07-04) — наследие забыто, вопросов по нему нет.

**2026-07-03 — дневной цикл (итерации 1–4):**
- **Баг 01: фикс применён** 🟡 — истинные причины: конфликт порта с kiwix + два дефекта запуска
  bat (полный путь к exe обязателен: `NoDefaultCurrentDirectoryInExePath=1`; CRLF). Модель
  реабилитирована: `n_ctx_train=262144`, стек здоров (68 t/s; 25K промпт за 7.8 с). Профиль:
  `-c 131072`, явный порт 8080; kiwix → host-8081. Ждёт подтверждения владельца в Zoo Code.
- **llama-swap развёрнут** (Фаза 4 частично): прокси на 8080, автозапуск модели по запросу
  (холодный старт 5.5 с), конфиг `llama-swap/config.yaml`. ⚠️ ttl-автовыгрузка ПОКА не
  подтверждена наблюдением — проверяется; автостарт llama-swap при загрузке ПК не настроен.
- **Харнесс:** `tools/health-check.ps1` (одной командой: порт/процессы/health/props/VRAM).
- **План 02** (`plans/02_self_deploy.md`): архитектура «манифест + deploy.mjs», шаги. SHA256
  модели снят: `EE33AB5B…7020` (для манифеста).

| Фаза (по `MASTER_PLAN.md` после `/revision` 2026-07-17) | Статус |
|------|--------|
| 0–6 Фундамент → рабочий KLAS-сервер (= релиз 1.0) | ✅ |
| 7 Самораскрытие «clone + deploy» до идеала | 🔧 хвосты в автономном пуле |
| 8 Эпик ассистента (конструктор из OSS) | 🔬 шаг 1: выбор ядра (мини-бенч OpenClaw) |
| Непрерывные процессы: эволюция ядра LLM · ревизия OSS · гигиена беклога | ♻️ |

---

## 🎯 ЯДРО — следующее по порядку (приоритет сейчас, Q1 интервью 003 = фокус на ядре)

1. ✅ **agent-bench v3 сделан (2026-07-08):** `tools/agent-bench-v3.mjs`, 8 градуированных задач.
   Лидер — **gemma-4-12b 7.5/8** (единственная взяла tool-chain с ветвлением на 1.0); рекомендация по
   ядру: gemma-4-12b — дейли-драйвер, Qwythos-9B — спец long-context; ornith исключить (медленный pp).
   Таблица и выводы — `researches/02` (§ «Агентный бенч v3»). Идеи v4 там же.
2. **Мини-бенч цепочек в цикле OpenClaw** (следствие смоука, см. автономный пул ниже) — решает
   развилку «OpenClaw как ядро ассистента» (Q1 интервью 004).
3. **Добить бенч новых кандидатов** — скачать и прогнать `bench-model` + `agent-bench-v2/v3`:
   **Qwen3-Coder-30B-A3B UD-Q5**, **GPT-OSS 20B** (чек-лист «Следующие шаги» в `researches/02`).
   Требует скачивания (~15–20 ГБ) → делать, когда есть место/время; результаты в таблицы `researches/02`.
4. **`/revision` мастер-плана** — MASTER_PLAN отстаёт от реальности: вписать релиз 1.0, фазу
   самораскрытия (идеи 03 + 12), горизонт ассистента (эпик `plans/08`), стратегию «конструктор из
   OSS-модулей» (`researches/09`, частично уже в принципах). Документ, без железа.

## 🤖 Пул автономного беклога (не нужен человек / особое железо)

- [x] **Мини-бенч цепочек в цикле OpenClaw (2026-07-17):** qwen3.5-35b **5.00/6.0** (цепочки
      честные; дефект: многострочный write → XML-псевдо-формат тул-колла, воспроизводимо),
      gemma-4-12b 3.00/6.0 (обрывает цепочки в чужом цикле). Вердикт: ядро жизнеспособно с
      qwen3.5-35b. Таблица и разбор — research 09 §3.4. Скрипт: `tools/openclaw-chain-bench.mjs`
      (⚠️ гонять только на свободном GPU — свапает модели).
- [ ] **Поиск свежих моделей (регулярно, идея 06):** раз в 1–2 недели — `node tools/leaderboard.mjs`
      (HF-API лидерборды, подключён в цикл 2026-07-17) + веб-поиск (r/LocalLLaMA, HF trending);
      кандидатов — через `node tools/try-model.mjs <repo> <файл>` и `agent-bench-v3`; результаты в
      таблицу `researches/02`. Последний раз: 2026-07-17 (кандидаты: Qwen3.6-35B-A3B, GPT-OSS 20B).
- [x] **agent-bench v2 (2026-07-07):** `tools/agent-bench-v2.mjs` — 9 дифференцирующих задач.
      Де-сатурировал (разброс 6–7/9 vs 6/6 у v1). Лидеры gemma-4-12b и Qwythos-9B (7/9) сильны в
      РАЗНОМ (gemma — инструкции/JSON; Qwythos — длинный контекст); qwen3.5/qwen3.6/ornith — 6/9;
      ornith медленный (long-ctx 106 с). ВСЕ валят multistep-math (десятичный %). Таблица+выводы —
      `researches/02` (§ «Агентный бенч v2»). Идеи v3 там же. Многофайловая правка/длинная сессия — в v3.
- [ ] **Фаза 2b — локальная проверка кандидатов:** по чек-листу «Следующие шаги» из
      `researches/02_optimal_stack_2026.md` (обновить llama.cpp под sm_120/CUDA 13, бенч
      Qwen3.5-35B-A3B UD-IQ3_S / Qwen3-Coder-30B UD-Q5 / GPT-OSS 20B, поставить llama-swap,
      сравнить Cline vs Zoo Code).
- [x] **deploy.mjs — добит (2026-07-17, Фаза 7):** манифест дополнен новыми компонентами
      (основная model-qwen3.5-35b — её не было!, openclaw npm, voice: venv+Silero v5+GigaAM);
      новые kinds `npm`/`commands`; финальный health-check в конце `--apply` + напоминания
      владельцу. Проверено на живой системе: dry-run — все 11 элементов ✓ (идемпотентность),
      `--apply` — no-op + зелёный health-check. `homepage/config/*.yaml` оказались уже в git
      (хвост закрылся раньше). Развёртывание KLAS = clone + `node tools/deploy.mjs --apply` ✅.
- [x] **Тихий фон + автозапуск через ТРЕЙ (баг 02 + идея 10, 2026-07-04):** llama-swap запускается
      без окна консоли (`llama-swap\start-hidden.vbs`, проверено MainWindowHandle=0, /health 200).
      Автозапуск теперь ставит ВЛАДЕЛЕЦ одной командой `powershell -File tools\install-autostart.ps1`
      — задача Планировщика «KLAS» при входе тихо поднимает весь стек + трей «Кот Криник» + пуш
      (агенту classifier запрещает создавать persistence). ⚠️ Пока владелец не запустил — остаётся
      старая задача «KLAS llama-swap» (только llama-swap, без трея); новый скрипт её снимает.

## ❓ Ждёт ревью человека

- ✅ **Интервью 006 отвечено и ИСПОЛНЕНО (2026-07-17): основная модель теперь `qwen3.6-35b-a3b`**
  (qwen3.5 — fallback до обкатки; gpt-oss удалён по критерию «≥ gemma»). Проверено смоуками: ядро
  на новой основной (fallback не понадобился), голосовой каскад работает. Видение владельца
  зафиксировано: скорости достаточно — покупаем УМ, эталон Claude Code; gemma «болезненная».
- 🏠 **Домашка ОБНОВЛЕНА: Zoo Code переключить на НОВУЮ основную** — model = `qwen3.6-35b-a3b`
  (API тот же `http://127.0.0.1:8080`, контекст профиля ≤98304). Запасные: `qwen3.5-35b`, `gemma-4-12b`.
- 🏠 **ЕДИНЫЙ вечерний тест-план: `homeworks/02_voice_and_android_tests.md`** — выбор голоса (2 мин)
  + живой микрофонный диалог (3 мин) + Android-нода (вместе с агентом). Результаты — в домашку или чат.
- 🏠 **ВЕЧЕРОМ (владелец, 2026-07-17): голосовые тесты** — оставлено на вечер по слову владельца:
  1) живой микрофонный диалог Г3: `node tools/voice-talk.mjs` (Enter старт/стоп, выход — пустая
  реплика; впечатление → `plans/13`); 2) выбор дефолтного голоса Г1:
  `node tools/voice-say.mjs "Привет, Криник!" --play --voice aidar|baya|kseniya|xenia|eugene`
  (ответ → `plans/11`). До проверки Г1/Г3 стоят в статусе 🟡.

- ✅ Интервью 005 отвечено (2026-07-17, все A): гейтвей в tailnet поднят, exec не эскалирован,
  навыки shortlist, канал — Control UI; видение Q4 (Android-нода + голосовая аутентификация)
  разнесено в plans/10/09. 🏠 **Домашка (по желанию): постоянный сервис гейтвея** —
  `openclaw gateway install` (Scheduled Task от имени владельца; иначе гейтвей поднимает klas.ps1).

- ✅ Интервью 004 отвечено (2026-07-16): смоук-пилот OpenClaw / клиент за ядром / A/B TTS-клонов /
  ступенчатые полномочия Windows + правило живости OSS. Решения разнесены по документам.
- 🏠 **Домашка (появится после пилота): референс-сэмплы голосов** — для A/B-теста клонов (Q3 инт. 004)
  владелец собирает/одобряет сэмплы Вихрова и кандидаток Joi.
- 🏠 **Домашка: переключить Zoo Code на основную модель** — в настройках профиля Zoo Code указать
  model = `qwen3.5-35b` (API остался тот же `http://127.0.0.1:8080`); контекст профиля ≤98304.
  Запасные там же: `gemma-4-12b` (128K, зрение в будущем), `qwen3.6-27b` (32K).
- ✅ Интервью 002 отвечено (2026-07-03): A/A/A — основная Qwen3.5-35B (контекст расширен до 98K
  через KV q8_0), старый квант 16.7 ГБ удалён, MTP-вариант замеряется.

- ✅ **SECURITY закрыто (2026-07-04):** секреты ротированы (новый basicauth-пароль + новый Bearer-ключ,
  проверено: старые → 401, новые → 200); git-история вычищена от утёкшего хеша и force-push в origin.
  Caddyfile вне git (шаблон `Caddyfile.example`); секреты — в `caddy/PASSWORD.local.txt` + `LINKS.local.md`.
- ❓ Интервью 001 — границы автономии (Q3), docker-сервисы (Q4). Q1 и Q2 (MIT) закрыты.

---

## Где продолжить в следующей сессии

> Сессия 2026-07-28: живой UI-тест (plans/16 фаза 4) → корень дефекта тул-коллов найден в ДВИЖКЕ
> (b9538 старше апстрим-фикса #24807) → обновление на b10167 откачено как ломающее (`bugs/05`) →
> ретрай моделей починен (не передавать `--model`) → документы приведены к правде.

### 🤝 ПЕРЕДАЧА ЭСТАФЕТЫ (сессия 2026-07-29, утро — закрыта в 10:00 по слову владельца)

**Что произошло за сессию.** Владелец вернулся с живым микрофоном и принёс три дефекта. Все три
разобраны; два закрыты, один вылечен опцией и ждёт его ушей.

| # | Дефект | Состояние |
|---|---|---|
| `bugs/08` | голос бормотал (моджибейк UTF-8→cp1251) | ✅ **ЗАКРЫТ**, подтверждён владельцем: «заработало» |
| `bugs/11` | голос менялся посреди реплики (муж→жен) | ✅ **ЗАКРЫТ**, ждёт вкусового вердикта |
| `bugs/09` | стриминг глох на коротком первом предложении | ✅ **ЗАКРЫТ** (1/1 → 3/3 фраз, TTFA 6.2 с) |
| `bugs/10` | уши без латиницы и пунктуации | 🟡 лечение развёрнуто **опцией** `--ears punct`, дефолт не тронут |
| `bugs/12` | бенч ставил 5/5 провалившемуся ходу ядра | 🔧 фикс применён (чёрный список заглушек), белый список — следующий шаг |

**Артефакты знания:** `researches/13` (уши и языки) · `researches/14` (**карта золотых стандартов +
инвентарь паритета 17 строк**) · EXP-0018…EXP-0021.

### 🔥 Первое дело следующей сессии — СПРОСИТЬ ВЛАДЕЛЬЦА, а не кодить

Три вердикта, которые может дать только он, и без которых следующий шаг — угадывание:

1. **Уши `punct`** — `node tools/voice-talk.mjs --ears punct` своим микрофоном. Вопрос: стоят ли
   запятые и латиница той цены, что покажет живая речь? Замер на синтетике — в `bugs/10`, он
   НЕ единогласен. Переключать дефолт только по его слову (модель ушей = элемент стека).
2. **Английский голос** — `--voice-en en_23` (сейчас) против `en_46` / `en_112`. И более крупная
   развилка из `bugs/11`: вариант B (транслитерация → буквально ОДИН голос, но русский акцент) и
   вариант C (кросс-язычный клон, эпик). Это выбор ГОЛОСА ассистента ⇒ `/interview`.
3. **Три вопроса из `researches/13`** (чем платим за латиницу; можно ли занимать GPU под уши;
   насколько священно качество чистого русского) **и четыре из `researches/14`** — главный:
   **убирать ли Enter из разговора?** VAD + endpointing делают диалог живым, но означают, что
   микрофон слушает ПОСТОЯННО; для системы, чей принцип — приватность, это решение владельца.

### 🧭 Куда двигать тракт дальше (порядок выведен инвентарём паритета, `researches/14` §4–5)

Не по вкусу, а по зависимостям: **AEC → Silero VAD + endpointing (800–1200 мс) → barge-in (<150 мс)**.
AEC первым, потому что без эхоподавления VAD слышит голос самого ассистента из динамиков владельца и
принимает его за перебивание — следующие два слоя без него не заработают в принципе, сколько их ни
пиши. Переписывать тракт на Pipecat/LiveKit НЕ предлагается: взять из них идеи и по одной OSS-детали.
Резерв латентности — префилл 20-тысячного системного промпта (строка 4 инвентаря), а НЕ «модель
поменьше»: это подтверждено замерами дважды.

### ⚠️ Что следующий агент обязан знать, чтобы не повторить провал этой сессии

- **Терминал агента ≠ терминал владельца.** Claude Code задаёт своим процессам
  `PYTHONIOENCODING=utf-8:surrogateescape`; у владельца её нет. Месяц эта переменная молча лечила
  голосовой тракт, бенч показывал 35/35, а человек слышал кашу. **Любой голосовой фикс проверяй
  прогоном со снятой переменной** (`Remove-Item Env:PYTHONIOENCODING`). Уроки — EXP-0018, EXP-0019.
- **Дефект, который только ЗВУЧИТ, не ищется чтением кода.** Ищи артефакт, который система уже
  сохранила на диск (`voice\out\*.wav` переживают сессию), слушай его ушами KLAS и доказывай причину
  побайтовым воспроизведением, а не похожестью симптома.
- **Прошлый диагноз может быть неверен.** Вечер 28-го списал бормотание на «две сессии в одни
  колонки» и закрыл симптом замком — настоящий дефект остался жив, и владелец услышал его снова.
  Замок при этом полезен; неверен был вывод, что он всё вылечил.

### 🧰 Новое в харнессе за сессию

- `python tools/voice/pitch-check.py [--match]` — охранник единства голоса (F0 обоих языков).
- `node tools/voice-hear.mjs <wav> --model ru|punct` — уши переключаемы между двумя алфавитами.
- `node tools/voice-talk.mjs --ears punct --voice-en en_46` — живой диалог с любой комбинацией.
- Канарейка кодировки в `TtsDaemon.ready()` → статус `encoding-broken` (`voice-talk` и `voice-bench`
  на нём отказываются стартовать).

> ⚠️ **ПРАВИЛО, ОПЛАЧЕННОЕ ВЕЧЕРОМ 28.07:** перед тем как отдать тракт владельцу — прогнать
> `node tools/voice-bench.mjs` (норма 35/35). Метрика рядом с артефактом не заменяет проверки
> артефакта. И НИКОГДА не гонять звуковые тесты (`--audible`/`--acoustic`), пока человек может быть
> в колонках, — замок теперь не даст, но лучше не подходить к границе.

1. **🏠 Живой микрофонный прогон:** `node tools/voice-talk.mjs` (гейтвей должен быть поднят —
   `powershell -File tools\klas.ps1 -Action up`). Впечатление → `plans/13`. Что спросить владельца:
   (а) достаточно ли быстро (критерий «≤8 с TTFA» придуман агентом из возможностей железа, а не задан
   им); (б) **как звучит английский** — это единственное, что бенч проверить не может, уши GigaAM
   русские. Если медленно — следующий шаг назван и оценён: **урезать 20-тысячный системный промпт для
   голосовых сессий** (остаток ~5 с ядра — это его префилл), затем нарезка по запятым с упреждением
   25 символов.
2. **Атрибуция регресса ядра (10.4 с против ~7 с базы 07-17)** — стендом, без риска для боевого стека:
   `node tools/llamacpp-probe.mjs b9538` против `b10167` на ОДНОЙ модели. Разделит «цену нового билда»
   и «цену перехода на qwen3.6». ⚠️ Замеры латентности теперь берутся с резидентного тракта — старые
   числа с ним не сравнимы напрямую.
3. ✅ **`bugs/06` закрыт** (двуязычность сделана 29.07) — пункт снят.
4. **`bugs/07` — воспроизвести искажение `Type` изолированно** (20–50 прогонов одной строкой, без
   ядра) и с цифрой идти в апстрим-тикет `CursorTouch/Windows-MCP`.
5. **Проверить наблюдением риск одобрений MCP в голосовом канале** (`researches/12` §5 «б»): через
   гейтвей действия инструментов могут требовать подтверждения оператора, чего `--local` не спрашивал.
   В прогонах этой сессии тул-коллов не было — риск НЕ проверен.

> Нормы, закреплённые прогоном 28.07 (соблюдать в живых UI-тестах): запись экрана обязательна; текст
> читать `--crop` в нативном масштабе, а спорную букву — вообще спрашивать у ОС строкой, не глазами
> (EXP-0010); фикстура несёт уникальный токен прогона, иначе тест проходит на остатках (EXP-0012);
> непривилегированный прогон — `runas /trustlevel:0x20000` (EXP-0011).

### Дальше по ценности

- **🏠 Обратная связь владельца по голосу (домашка 02) разнесена ЧАСТИЧНО** (2026-07-28, ночь):
  ✅ **«всё медленно»** — вылечено резидентной архитектурой (24.5 → 7.6–8.0 с, `researches/12`);
  ✅ **голос дефолтом `eugene`** — вписан в `voice-talk` (раньше код молча брал `aidar`);
  ❌ **«голоса уёбищные»** — ждёт Г5 (клоны, `plans/15`, нужны 🏠 сэмплы);
  ❌ **«TTS не произносит английские слова, STT пишет английское кириллицей»** — двуязычность тракта,
  два реальных дефекта, упираются в решение владельца (`bugs/06`, `/interview`).
- **Беспроводная нода** (видение владельца — основной канал): разрулить NordVPN ↔ Tailscale, вернуть
  `gateway.bind=tailnet` + `openclaw gateway --tailscale serve` (wss), перепейринг. Сейчас нода живёт
  только на USB-кабеле.
- **Ротация токена гейтвея** (светился в локальных логах).
- **Поиск свежих моделей** (идея 06) — просрочен с 2026-07-17. ✅ Теперь можно: движок чинён, тул-коллы
  парсятся, сравнение моделей по цепочкам снова достоверно (и цифры на b10167 будут честнее прежних).
- **🔬 Зрение Jarvis/Joi — задание на разведку (`plans/17_jarvis_vision_research.md`, заказ владельца
  2026-07-28):** гуглёж «быстрое и надёжное зрение экрана» (OpenCV и другие подходы) → документ
  `researches/11_agent_vision.md` → только потом код. Первый потребитель — план 16.

1. **Голосовой тракт — фазы Г1→Г5** (зонтик `plans/07`): **Г1 🟡 · Г2 ✅ · Г3 🟡 — KLAS ГОВОРИТ,
   СЛЫШИТ И ВЕДЁТ ДИАЛОГ** (2026-07-17, всё локально/офлайн): Silero v5 (RTF 0.19) + GigaAM-v3
   (RTF 0.032, round-trip 97%) + каскад `voice-talk` с ядром OpenClaw — тёплый ход 20–22 с,
   контекст сессии держится. 🏠 Владельцу: живой микрофон (`node tools/voice-talk.mjs`) и выбор
   голоса (`plans/11`). **Г4 начата (хостовая половина ✅):** аудио-архитектура ноды исследована
   (research 09 §3.4: STT на устройстве, TTS хоста через talk.speak; Voice Wake на Android пока
   отключён апстримом) и **наш Silero подключён TTS-провайдером ядра** (`tts-local-cli` →
   `voice-say.mjs`, проверено тулом tts) — нода из коробки заговорит нашим голосом. Осталась
   телефонная половина Г4 (🏠 с владельцем) → Г5 клоны (`plans/15`, ждёт 🏠 сэмплы).
2. ✅ Хвосты Фазы 7 закрыты (2026-07-17): deploy.mjs добит (см. пул), манифест знает openclaw и
   голосовой тракт. Открытым из старого: ответы владельца по интервью 001 (Q3–Q4) — если появятся.
   Следующее автономное — поиск свежих моделей (идея 06, просрочен с 03.07, пункт пула).
3. Если владельца нет и вопросов нет — `/autoloop` по пулу выше. Помни: порт 8080 делят
   llama-server и kiwix; docker после перезагрузки контейнеры НЕ поднимает (проверено 2026-07-02);
   GPU может быть занят задачей владельца — проверяй перед бенчами.

---

## Баги

- ✅ `bugs/06_DONE_voice_tts_needs_cyrillic.md` — **ЗАКРЫТ 2026-07-29.** Реплика без русских букв
  роняла голосовой ход, потом молчала. Теперь: числа словами, английский — своей моделью, единицы
  полными словами. Развилка «как читать латиницу» решена словом владельца в чате, а не интервью.
- 🔬 `bugs/07_windows_mcp_type_char_corruption.md` — `Type` Windows-MCP перемежающеся искажает символ
  (послано `KLAS UIA test`, на экране `KLAS IIA test`). Улики собраны, корень не установлен, лечение
  не применялось. Опасен тем, что молча искажает ввод в чужие приложения.
- ✅ `bugs/05_DONE_llamacpp_toolcall_grammar.md` — **ЗАКРЫТ 2026-07-28.** «Движок ломает грамматику»
  оказалось нашей схемой: тул `cron` просил `char{1,65536}` при пороге llama.cpp 2000. Лечение —
  `compat.unsupportedToolSchemaKeywords`; движок обновлён до b10167, оба критерия годности взяты.
  Апстрим-тикет НЕ заведён (дефекта llama.cpp здесь нет; кандидат — тикет в OpenClaw, по слову владельца).
- 🔧 `bugs/04_openWEBUI_FCK_UP.md` — Open WebUI не «под ключ» + веб llama-swap (панель, Playground 404).
  Фикс применён и проверен браузером (см. «Где мы сейчас»); ждёт подтверждения владельца.

### Ранее закрытые ✅ (2026-07-04)

- ✅ `bugs/01_DONE_context_overflow_zoo_code.md` — переполнение контекста в Zoo Code. Подтверждено
  владельцем: ошибок нет.
- ✅ `bugs/02_DONE_remote_access_via_funnel.md` — удалённый доступ через funnel + краснеющий пульт +
  окно консоли при старте. Подтверждено владельцем: funnel работает, пульт не краснеет, окна нет.
- ✅ `bugs/03_DONE_homepage_links_fck_up.md` — ссылки пульта + Kiwix в чатах. **Закрыто, подтверждено
  владельцем (2026-07-04).** Корень багов 1/3/4 — host-валидация homepage (`HOMEPAGE_ALLOWED_HOSTS`
  без голого `localhost`); фикс — env в `docker-compose.yml`. Баг 2 (чат зашит на funnel) — ссылка
  `/chat`, Caddy редиректит локально→`:3080`, funnel→`:8443`. Баг 5 (llama-swap UI красный) — его
  live-API на КОРНЕ (`/api/events`…) проксируется на llama-swap матчером `@swap_api` в Caddyfile.
- ✅ **Идея 11** (`ideas/11_DONE_kiwix_in_openwebui.md`) — знания Kiwix в чатах Open WebUI. Готово и
  работает: тул `open-webui/tools/kiwix_search.py` (+ импортируемый `kiwix_search.tool.json`), LLM
  сам вызывает `search_local_knowledge` и отвечает по локальной Википедии (проверено). После импорта
  тул надо ВКЛЮЧИТЬ (в чате или привязать к модели: Workspace → Models → Tools).
