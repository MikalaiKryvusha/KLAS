# KLAS — Krinik Local Agent System — Летопись проекта

> **Append-only летопись** того, как проект жил и рос: закрытые сессии, пройденные фазы, релизы,
> крупные решения в порядке, в котором они случались. Сюда `STATUS.md` сбрасывает своё прошлое —
> STATUS остаётся короткой живой сводкой НАСТОЯЩЕГО, а всё завершённое переезжает СЮДА
> (шаг «бонсай-подрезка» в `/end-chat`).
>
> **Читать не обязательно.** Этот файл НЕ входит в канон-набор `/resume` и не входит в обязательный
> минимум перед задачей — открывай его, только когда действительно нужна археология: как родилось
> решение, что содержала старая фаза, когда что вышло.
>
> **Правила летописи (дисциплина ADR):**
> - **Append-only, новое сверху.** Записанную запись никогда не переписывают, чтобы она говорила
>   другое, — история, которую можно переписать, не история. Поправки приходят НОВЫМИ записями,
>   которые ссылаются на старую и отменяют её.
> - Запись переезжает сюда из `STATUS.md` **ДОСЛОВНО**, когда её работа закрылась, — переносим, а не
>   пересказываем: в записи уже есть её даты, цифры и ссылки на файлы.
> - Записи свободно называют версии и даты — летопись имеет полное право говорить о старых версиях,
>   и сканер устаревших утверждений в машинерии обновления знает, что этот файл трогать не надо.
> - Когда файл станет неподъёмным — резать по эпохам: свежая эпоха остаётся здесь, старые уезжают в
>   `PROJECT_HISTORY_<эпоха>.md`, а наверху этого файла остаётся однострочный индекс (так делают
>   большие changelog'и).
>
> Живой документ — тегом `DONE` не помечается.
>
> ⚠️ **Происхождение файла:** заведён 2026-07-31 при обновлении KAIF 1.6 → 2.1. Записи ниже перенесены
> из `STATUS.md`, который к тому моменту разросся до **1928 строк** и перестал быть сводкой. Перенос
> дословный: ни одна строка не переписана, изменён только порядок склейки (внутри — от новых к старым).

---

## Записи (новые сверху)

<!-- Перенесено из STATUS.md 2026-08-02 (бонсай-подрезка при закрытии чата о контуре вычитки).
     Работа этих записей закрылась; их указатели остались в STATUS там, где ещё нужны. -->

- **2026-08-01 (ночь) — 🚪 НАПРАВЛЕНИЕ: у ядра KLAS должна быть ДВЕРЬ** (`ideas/20`). Владелец:
  *«запуск в терминале — временный костыль… нужно оформлять в виде веб-панели»* и *«контрольная
  прослушка должна быть в конечном инструменте поставки»*. Вечер это доказал сам: четыре сбоя были
  не про запись голоса, а про СПОСОБ доступа (кодировка запускателя, пробелы в аргументах, «у тебя
  же есть зрение», прослушка в песочнице). Разведка НЕ проводилась; `D:\work\ndim` найден, не изучен.
- **2026-08-01 (ночь) — 🛡 ОХРАННИК КОДИРОВОК** `npm run guard:encoding` (8/8): `.ps1` с не-ASCII
  обязан иметь BOM, `.json` — обязан быть БЕЗ него, `.bat`/`.cmd` — CRLF. Правила противоположны, и
  оба уже кусали проект. Правило про `.ps1` **в проекте было** — в сноске «Не для:» у EXP-0002, где
  его никто не ищет; стоило владельцу захода к микрофону (**EXP-0057**).
- **2026-08-01 (вечер) — 🏠 ДОМАШКА 04 ПРОЙДЕНА: «вроде работает», два замечания.** Разговор по
  имени принят владельцем на живом микрофоне. Замечание 1 — **не перебить, пока ассистент говорит**;
  причина ИЗМЕРЕНА (`bugs/25`): поверх собственной речи оценка активатора падает с 0.93/0.97 до
  0.478/0.012 даже когда имя вчетверо громче, при пороге 0.5 ⇒ перебить голосом физически нельзя,
  AEC у нас нет. Ложных самоперебиваний при этом НЕТ (контроль 0.001). Попутно починены два
  дефекта: бип молчал ровно на перебивании, а отчёт печатал TTFA у хода, не издавшего ни звука.
  **Лечение — развилка владельца** (переобучение на конкурирующей речи · AEC · наушники), см.
  `bugs/25`. Замечание 2 — **голоса не Вихрова и не Гороховой**: обе персоны говорят заглушками
  Silero, одобренные клоны в резидентный рот не встроены — это трек `plans/18`.
- **2026-08-01 (ночь) — 🗣 РАЗГОВОР СТАЛ РАЗГОВОРОМ.** Живой тест владельца дал волну находок, и
  все закрыты: **Джарвис перебивается** (0.890 при равной громкости против 0.685 у предшественника,
  а тот ещё и зажигался от собственной речи — 0.666 при пороге 0.5); **перебивание больше не
  записывает речь ассистента** (`bugs/26`); **окно продолжения** — после ответа ассистент сам
  слушает 5 с без имени (`plans/21`); **сигналы вместо «пика»** — `bell` на открытие, `bell-deep`
  на закрытие, выбраны владельцем на выслушке (`homeworks/05`).
  ⚠️ Два урока сессии: план написан ЗАДНИМ ЧИСЛОМ (EXP-0054) и прогоны стенда нельзя сравнивать
  без закреплённого стимула (EXP-0053).
- **2026-08-01 (ночь) — 🏗 ЛЕСТНИЦА ПОД «ПОСТОЯННУЮ ЖИЗНЬ»** (`researches/24` → `plans/22` →
  `interviews/interview_010`). Главное: **микрофон в Docker на Windows недостижим** — у WSL2 нет
  звуковых устройств вовсе. Задача переформулирована в четыре свойства «постоянства», и первой
  фазой идёт диагноз `bugs/15`, иначе вечная жизнь превращается в вечные перезапуски.
- **2026-08-01 (вечер) — 🕵 АНОНИМНАЯ КОПИЯ РАБОТАЕТ И БОЛЬШЕ НЕ ВРЁТ** (`bugs/23` + `bugs/24`):
  мастер звал `anonymize.mjs` без `--apply` — тот вежливо репетировал и выходил кодом 0, а мастер
  рисовал экран успеха над копией с **41 файлом** личности автора. Дальше выяснилось, что и сам
  валидатор анонимности фильтровал дерево тем же списком расширений, что и чинилка, то есть умел
  лишь подтверждать себя. Теперь: мастер зовёт с `--apply` и останавливается на провале, валидатор
  обходит всё дерево по байтам, и независимая сверка десятью зондами на свежем клоне оставляет
  **только `LICENSE`** — объявленное исключение, которое печатается вслух.
  Два охранника: `npm run guard:dry-run` · `npm run guard:anonymize` (10/10).
  ❓ Владельцу — `interviews/interview_008` (политика по `LICENSE` и `README.pdf`).
  → **Закрыто 2026-08-02:** владелец ответил через контур вычитки (Q1=A `LICENSE` не трогаем,
  Q2=A `README.pdf` пересобирать); разнесено, самопроверка выросла до 11/11.
- **2026-08-01 (день) — 🗣 ИМЯ ОТКРЫВАЕТ РАЗГОВОР ВМЕСТО ENTER** (шаг 5 плана 19 собран):
  `node tools/voice-wake.mjs` — сказал «Джарвис» или «Джой», и имя выбирает персону целиком (голос,
  характер, своя беседа у ядра). Проверено на фикстурах без человека, бенч тракта 35/35.
  **Ждёт живого микрофона владельца — `homeworks/04`.** Подробности ниже.
- **2026-08-01 (ночь) — 🎙 АКТИВАТОРЫ «ДЖАРВИС» И «ДЖОЙ» ГОТОВЫ И ПРИНЯТЫ ВЛАДЕЛЬЦЕМ**
  на живом микрофоне («работает отлично»). Обе модели обучены, измерены, цена ожидания снята.

**🎤 2026-08-01 ночь. АКТИВАТОР УЧИТСЯ НА ГОЛОСЕ ВЛАДЕЛЬЦА + направление на админскую панель.**

Сессия началась с вопроса владельца *«Может обучим на моём голосе? Сколько раз я должен имя
произнести?»* и закончилась двумя вещами: **50 личных записей имени «Джарвис» сняты** и записано
направление, меняющее способ доступа ко всем инструментам KLAS.

**Что появилось.** `tools/voice/wakeword-enroll.py` (стенд записи: подсказки по сценарию, проверка
каждой записи ушами на месте, разбор выбросов `--audit`, пересборка из оригиналов `--retrim`,
контрольная прослушка `--audition` с раскладкой по секундам; самопроверка 9/9) ·
`tools/voice/voice-reading.py` + `docs/voice_reading_ru.md` (длинная начитка для биометрии и
клонирования, 7 блоков, ≈2.8 мин чистой речи; разбор текста 3/3) · `tools/voice/record-window.ps1`
(видимое окно для человека — агентский вывод копится и человеку не показывается) ·
`tools/encoding-guard.mjs` (`npm run guard:encoding`, 8/8) · `ideas/20` (админская панель) ·
`homeworks/06` (вердикт владельца на слух).

**Что вскрыла живая сессия.** Три дефекта СТЕНДА, невидимые на фикстурах: начало имени падало в дыру
между подсказкой и стартом записи (человек говорит быстрее, чем прибор готовится) · резка принимала
щелчок в тихой комнате за начало слова (пол шума ≈ 0, порог от него бессмыслен; различает не
громкость, а длительность) · разбора выбросов не существовало, и клип на 2.95 с прошёл приёмку.
Все три вылечены, и **ни один не потребовал звать владельца заново** — учебные клипы пересобраны из
оригиналов 48 кГц (изменилось 39 из 50, медиана 0.87 → 0.76 с). Урок — EXP-0059.

**Чем обошёлся терминальный доступ.** Владелец: *«запуск в терминале — это конечно временный
костыль»*. Вечер это подтвердил числом сбоев, не относящихся к задаче: `.ps1` без BOM не разобрался
(правило В ПРОЕКТЕ БЫЛО — в сноске «Не для:» у EXP-0002, где его не ищут; EXP-0057) · аргументы с
пробелами через `Start-Process` потерялись дважды · агент попросил владельца описать экран, имея
собственное зрение `screen-rec.mjs` (*«у тебя же должно быть зрение из KLAS»*, EXP-0058) · прослушка
была собрана временным скриптом в песочнице (*«должна быть в админской панели»*).

**Открытым осталось:** вердикт владельца по записям (`homeworks/06`), длинная начитка (текст готов,
не записана), обучение на личных клипах (`plans/23` шаги 4–6).

**🗣 2026-08-01 день. ИМЯ ОТКРЫВАЕТ РАЗГОВОР ВМЕСТО ENTER — шаг 5 плана 19.** Короткая сессия, 2 коммита.

Продолжение сессии выше: модели активаторов были приняты владельцем, но диалог по-прежнему начинался
нажатием Enter. Сделана связка «услышал имя → открылась беседа, и имя выбрало персону».

**Что появилось.** `tools/voice/personas.mjs` (канон двух персон: имя → детектор → голос → промпт;
ключ персоны СОВПАДАЕТ с именем файла модели — контракт, а не совпадение) · `tools/voice/wakeword-listen.py`
(резидентный слушатель: непрерывный поток, обе модели, захват фразы после имени, события строками
JSON) · `tools/voice-wake.mjs` (дирижёр) · `tools/voice/wake-talk-fixture.py` (воспроизводимая
фикстура «имя + вопрос») · `tools/voice/ears.mjs` (распознавание вынесено из `voice-talk.mjs`, у него
появился второй потребитель).

**⛔ Захват фразы живёт в слушателе, а не в Node, по ФИЗИКЕ:** dshow-устройство эксклюзивно — второй
ffmpeg на тот же микрофон не откроется, значит «слышать имя» и «писать вопрос» обязаны читать один
поток. Побочная выгода: запись стартует в миг срабатывания, начало фразы не срезается.

**Конец фразы — по числу, а не по вкусу.** Пороги выставлены замером: тишина комнаты через шумодав
NVIDIA Broadcast — средний модуль отсчёта **0.1** (99 кадров подряд), пик живой речи владельца в
прогоне 90 с — **1272**; отсюда нижняя граница 40, сам порог адаптивный (шум × 4). Первый замер, к
слову, был сделан при ВЫКЛЮЧЕННОМ микрофоне и дал те же числа — шумодав давит комнату в тот же ноль,
поэтому «мёртвый мик» и «тихая комната» по уровню неразличимы. Автомат отделён от захвата и проверен
числами: `--selftest` **6/6**, среди случаев «шумная комната: 500 это не речь» (ловит вбитую
константу) и «провал громкости внутри фразы не обрывает человека». Отдельной VAD-модели нет
намеренно: `silero_vad.onnx` лежит и понадобится на слое turn-taking, а здесь хватило автомата.

**⭐ Два решения владельца (спрошены ДО кода; оба висели открытыми в `researches/16` §5).**
1. **«Слушать всегда, имя перебивает»** — barge-in, а не глушение. Агент рекомендовал обратное
   (дешевле и честнее без AEC) и назвал цену ДО выбора: детектор будет слышать собственный голос
   ассистента из колонок (EXP-0017). Владелец подтвердил. Смягчение: каждое перебивание печатается
   с оценкой; обратный режим оставлен флагом `--mute-while-speaking` (не проверен наблюдением).
2. **Короткий бип как сигнал «услышал»** (варианты «голосом» и «ничего» отклонены). Замер: **0.36 с**
   накладных на запуск проигрывателя + 0.15 с тона. Тон генерируется кодом, файла в git нет.

**Проверка наблюдением, без человека.** Каждое имя зажигает свой детектор и молчит на чужом
(jarvis 0.556 · joy 0.851) · фраза отрезана верно (3.28 с = преролл + пауза + вопрос + хвост 0.88 с) ·
имя не затекло в вопрос (захваченный wav распознан дословно) · персона выбрана именем — Джарвис
ответил «Два плюс два, **сэр**, — четыре» · перебивание в темпе живой речи оборвало проигрывание
ответа, ходы не столкнулись · микрофонный путь 12 с без ложных срабатываний · **регрессия всего
голосового тракта — бенч 35/35**. Тайминги: уши 1.6–1.7 с · ядро 4.4–5.0 с · **TTFA 6.6–7.7 с**, то
есть активатор своей латентности не добавил. Первый ход НОВОЙ беседы персоны — 50.6 с (префилл).

**Урок сессии — EXP-0048:** харнесс, который сжимает время, проверяет только то, что от времени не
зависит. Прогон из файла на максимальной скорости тихо ломал две вещи: антидребезг на настенных часах
глотал все срабатывания после первого, а поведение во времени (перебивание, глушение) было нечем
проверить. Лечение: время считается по ЗВУКУ, плюс режим `--realtime`.

**Осталось:** живой микрофон владельца — `homeworks/04` (пять сценариев, три вопроса).
Попутное наблюдение, не баг: во время прогонов держалось 20 процессов Windows-MCP на 1.3 ГБ — по паре
на новую беседу ядра; старые отмирают, роста без предела нет.

**🎙 2026-07-31 вечер → 2026-08-01 ночь. АКТИВАТОРЫ «ДЖАРВИС» И «ДЖОЙ» ОБУЧЕНЫ, ИЗМЕРЕНЫ И ПРИНЯТЫ
ВЛАДЕЛЬЦЕМ НА ЖИВОМ МИКРОФОНЕ.** Одна сессия, 26 коммитов.

Задача владельца дословно: *«сейчас важно сделать запуск по активационным фразам-именам: Джарвис и
Джой»*. До этой сессии оба готовых пути были закрыты замерами (`researches/16`): sherpa-onnx
`KeywordSpotter` — потолок 3/6 на русском произношении, готовая `hey_jarvis` из openWakeWord — 0/10.
Оставалось обучить свои две модели, и это было сделано целиком.

**Результат.** `voice\wakeword\training\jarvis.onnx` и `joy.onnx`, по 13.8 КБ, вход `[1, 16, 96]`.
На неизменном оценочном наборе (по 300 клипов: свои · ловушки · чужое имя):
«Джой» — полнота **94.3%**, ложных **0.0%**, на чужом имени **0.3%**;
«Джарвис» — **83.3%** / **0.0%** / **0.0%**. Отраслевой ориентир (>90% при <1 ложном срабатывании в
час) «Джой» берёт. Живой прогон микрофоном владельца: 10 срабатываний за 90 с (6 «Джарвис» 0.658–0.960,
4 «Джой» 0.637–0.931), ни разу два детектора одновременно. **Вердикт владельца: «работает отлично».**

**Цена круглосуточного ожидания** (`tools/voice/wakeword-cost.py`): 3.56 мс счёта на кадр 80 мс ⇒
**4.4% одного ядра из 16 = 0.28% машины**, плюс ffmpeg ~0.5% ядра; память 224 МБ процесс (из них
37.8 МБ модели) + 35 МБ ffmpeg; **видеокарта не используется вовсе**; запуск 0.09 с. Требование
`GOAL.md` «система спит и не ест ресурсы, пока не позвали» выполнено с запасом.

**Разведка `researches/22`** (гуглёж до кода): 30 000 часов негативов качать не надо — они
поставляются предвычисленными фичами. Реальная загрузка 16.3 ГБ, из них 16.09 — один файл ACAV100M;
импульсные характеристики комнат MIT всего 30.5 МБ. Научная база — Ko и др., ICASSP 2017. Новинки:
у openWakeWord появился более живой конкурент `OHF-Voice/micro-wake-word` (push 3 недели против 7
месяцев), но остались на openWakeWord — он отдаёт ONNX, а весь наш звуковой стек ONNX-овый, тогда как
micro-wake-word отдаёт TFLite и вернул бы TensorFlow. Обоснование против правила живости записано явно.
⛔ **Поправка к самой себе в тот же вечер:** рецепт фонового шума из ноутбука апстрима МЁРТВ (AudioSet
переехал на parquet, FMA стал датасетом-скриптом, выпиленным в `datasets` 5.x). Спас запасной вариант,
вписанный в разведку ДО того, как понадобился, — `rirs_noises.zip` с openslr.

**СЕМЬ НЕСОВМЕСТИМОСТЕЙ АПСТРИМА между готовым конфигом и работающим обучением.** Путь обучения
openWakeWord на Windows со свежими зависимостями, судя по всему, не проходил никто. Каждая закрыта
прокладкой с собственной проверкой ЧИСЛАМИ; **в чужом коде не изменено ни строки**
(`tools/voice/wakeword-train-shim.py`):

1. `train.py:635` читает конфиг `open(path,'r')` без кодировки ⇒ cp1251 на нашей кириллице. Лечение — `-X utf8`.
2. `train.py:638` БЕЗУСЛОВНО импортирует `generate_samples` стадии, которую мы не запускаем. Честная заглушка, ПАДАЮЩАЯ при вызове.
3. `torchaudio.load` ушёл в `torchcodec`, тому нужна shared-сборка FFmpeg. Подмена на `soundfile` — лечение уже было записано в `plans/18` по CosyVoice.
4. `torchaudio.info` снята в 2.13, зовётся из пяти мест. Своя поверх `soundfile`.
5. `trim_mmap` удаляет файл, который САМ ЖЕ держит отображённым (на Linux законно, на Windows нет) — и держит его ВЫЗЫВАЮЩИЙ (`utils.py:571`), не сама функция.
6. **Виндовый баг speechbrain:** защита от обращений `inspect` есть (`importutils.py:89`), но разделитель пути зашит ПРЯМЫМ слэшем — на Windows не срабатывает никогда.
7. `DataLoader(num_workers=N)` поверх датасета-генератора: на Windows только spawn, генераторы не сериализуются. Загрузчик переведён в один процесс.

Плюс: `onnxscript` нужен `torch.onnx.export` — не проверил заранее и потерял 18 минут обучения; проверка вынесена в предполёт. И `total_length`: скачанные негативы ЖЁСТКО задают окно 16 кадров = 32 000 отсчётов; заодно вскрылось, что форма входа модели считается целочисленным `//16000` и расходится с фичами при любой длине, не кратной 16 000.

**ГЛАВНЫЙ УРОК СЕССИИ — трижды подряд врал ПРИБОР, а не предмет (EXP-0047).**
Потоковый стенд давал **0.52 / 0.052 / 0.084** на одном клипе одной модели, и по его показаниям
владельцу был доложен ЛОЖНЫЙ вывод о двух ложных срабатываниях; прямой опыт с детерминированным
выравниванием показал 0.000 и 0.001 — ложных не было. Оценка на отложенных фичах выглядела честной,
но фичи ПЕРЕСЧИТЫВАЮТСЯ при каждом обучении со случайной аугментацией, то есть каждое поколение
судилось своим набором. И сама фикстура замера лежала вне git, была вычищена, а контрольные клипы не
имели генератора вовсе — замер, на который опирался канон, был невоспроизводим.
Правило: **число, из которого делается вывод, снимается ДВАЖДЫ и сравнивается.**

**Вторая ошибка того же рода — домысел вместо наблюдения.** Длинные обрамления в корпус я добавил,
РАССУЖДАЯ про «устойчивость к выравниванию», вместо того чтобы прочитать, как `create_fixed_size_clip`
раскладывает данные (выравнивание по ПРАВОМУ краю окна). Треть корпуса оказалась непригодной. Верный
признак — слово обязано ЗАКАНЧИВАТЬ фразу; боевой сценарий это не обедняет, ибо в живой речи детектор
срабатывает СРАЗУ ПОСЛЕ ИМЕНИ, когда окно как раз кончается на слове.

**Контрольный опыт, поставленный намеренно.** «Джой» оставили нелечёным, пока «Джарвис» получал
перекрёстные негативы. На одном наборе: нелечёный путал чужое имя в 99.7% случаев, лечёный — в 0.0%.
Разница видна не в рассуждении, а в двух числах на одном материале.

Уроки: **EXP-0044** (STT-судья предвзят к словам вне словаря — имя ассистента вне словаря всегда),
**EXP-0045** (huggingface_hub 1.26 копит большой файл в ОЗУ; у долгой загрузки два датчика — результат
на диске и расход памяти), **EXP-0046** (прокладка совместимости проверяется ЧИСЛОМ, а не фактом
импорта), **EXP-0047** (двигающаяся линейка). Артефакты: `plans/19_wake_word_activators.md`,
`researches/22_wake_word_training_data.md`, десять инструментов `tools/voice/wakeword-*`.

---

**📖 2026-07-31 (день). README ПЕРЕСТРОЕН В РУКОВОДСТВО ПОЛЬЗОВАТЕЛЯ — по образцу KPOT и
стилометрическому портрету владельца.**

Заказ владельца: перенести на KLAS удавшийся жанр README KPOT («руководство пользователя» вместо
витрины проекта; вердикт старому README KPOT — «устаревший фрод»). Образец: STATUS-запись
KPOT-сессии 2026-07-30 + `nikolai_stylometry.md` (портрет из Unliminium) + README KPOT.

- **Новый `README.md`:** 10 нумерованных разделов × 2 языка (Назначение · Состав · Установка ·
  Запуск и остановка · Пульт · Чат · База знаний · Удалённый доступ и API · Голосовой ассистент ·
  Ограничения текущей версии), 5 таблиц с подписями «Таблица N — …», регистр кодекса (безличный
  залог, «N.1. Основные положения»), шапка-витрина сохранена. Истории проекта и роадмапа в README
  больше нет; незавершённое живёт в §9.3 «Что в работе» и §10 «Ограничения».
- **Каждое утверждение перевыведено из кода** (5 агентов разведки → текст → 4 состязательных
  проверяющих; ~65 утверждений checked_ok). Пойманы и исправлены наследственные неточности:
  EN-строка про «переключение RU↔EN одним голосом» (снятая схема двух моделей — в бою
  транслитерация), «клоны в подборе» (выбраны 2026-07-30), «~8 с на ход» без оговорки о холодном
  старте (первый ход новой сессии до ~50 с), «мастер = те же скачивания» (мастер ставит
  Qwythos/Gemma, основную qwen3.6-35b — только полный deploy), «все модели Таблицы 2 деплоем»
  (в манифесте 4 из 6).
- **🐞 `bugs/23` заведён:** «анонимная установка» в мастере зовёт `anonymize.mjs` БЕЗ `--apply` —
  т.е. dry-run, ничего не обезличивает (`install.mjs:229` против `anonymize.mjs:22`). README подаёт
  анонимизацию только ручными командами (§3.5), ограничение названо в §10.3.
- **Отчёт стилометристу Unliminium** — `researches/21` + копия в
  `D:\work\unliminiumProject\ideas\AI_agents_reports\02_klas_readme_stylometry_field_report.md`
  (заказ владельца; в чужой git не коммитим). KLAS — четвёртый проект переноса портрета (после
  Unliminium, KPOT, NDim; отчёт NDim там же под номером 01).
- README.pdf перегенерён (`node tools/render-pdf.mjs --all`).

---

**🔍 2026-07-31 (ночь). БОЛЬШАЯ РЕВИЗИЯ КОДА ПОСЛЕ СЛАБЫХ МОДЕЛЕЙ — 40+ дыр закрыто за ночь.**

Заказ владельца: проверить код на адекватность, позакрыть дыры, оставить руководства/баги/идеи
«в назидание слабым моделям». Метод: 5 параллельных ревизоров по зонам + личная верификация
каждой находки (репро/чтение кода) + фиксы. Счёт: **~55 находок → ~45 подтверждено → 40+
закрыто** тремя коммитами (`d815684` голос · `b46e137` инфраструктура · `28c00f6` гигиена).

Самое важное из закрытого (полный разбор — `researches/19`, детали — `bugs/19`–`21`):
- **«1024.5 МБ» роняло normalize() IndexError'ом** — живой рот молча пропускал реплику; даты
  читались «шестьминус семьминус»; воскрес `bugs/06` в voice-say (боевой TTS-путь ядра глушил
  ответы-числа).
- **Сторож диска обучения был бумажным**: умирал от унаследованного `set -e` при первом сбое
  `df` — защита от повторения инцидента ext4 не работала. Плюс его текст помощи советовал
  запрещённый каноном `--set-sparse`.
- **Свежий клон получал НЕРАБОЧЕЕ ядро**: `deploy.manifest.json` пинил движок b9538 (не умеет
  тул-коллы Qwen3.6, `bugs/05`) при боевом b10167.
- **Установщик и трей врали об успехе** (экран «работает!» после провала, health-check всегда
  exit 0), SSE-парсер терял разрезанные чанки (дыры в речи), TOCTOU замка динамиков, битые
  недокачанные модели навсегда, транскрипт Гороховой в публичном git, docker.sock у homepage.

**Оставлено после ревизии:** баги `19`–`21` (DONE) + **`22` ОТКРЫТ** (судьи бенчей мягче, чем
заявляют — чинить одним коммитом С ПЕРЕМЕРОМ) · идеи `15` (дедуп общих модулей) и `16` (сторож
гигиены) ждут одобрения · уроки EXP-0041 (метод ревизии), EXP-0042 (три смертных греха
охранника) · `researches/19` (полный разбор + вопросы владельцу: funnel-хост в git, чистка
истории от транскрипта) · **`researches/20` — отчёт для ИИ-агента-разработчика KAIF** (заказ
владельца: как фреймворку делать слабые модели умнее — 7 механизмов по уликам этой ревизии).

---

**🎙 2026-07-30 (поздний вечер). ГОЛОС ВИХРОВА ОБУЧЕН, ЭКСПОРТИРОВАН В ONNX И ЖДЁТ УХА ВЛАДЕЛЬЦА
ДВУМЯ ВАРИАНТАМИ. По дороге найден и починен `bugs/18`: кавычка в корпусе склеила 104 реплики,
и обучение падало «нехваткой VRAM».**

### ⛔ ВЕРДИКТ ВЛАДЕЛЬЦА 2026-07-30 (канон): ОБА ВАРИАНТА PIPER ЗАБРАКОВАНЫ

Дословно: *«piper_vihrov.mp3 — **отвратительно**»*, *«piper_vihrov_temp14.mp3 — **аналогично**»*,
*«звучит плохо, если можно попробовать обучить как-то иначе — попробуй. **Я подозреваю, что
Piper как класс**»*.

**Гипотеза владельца подтверждается замером, а не мнением:**

| движок | параметры генератора | вес на диске |
|---|---|---|
| **Piper VITS medium** (наш) | **23.7 М** (из лога обучения) | 63.5 МБ ONNX |
| **F5-TTS / ESpeech** (одобренные владельцем) | **~330 М** (1.3 ГБ fp32 / 4 байта) | 1.3 ГБ |
| CosyVoice 3 | — | 9.1 ГБ |

**В 14 раз меньше параметров, 22.05 кГц против 24.** Piper medium спроектирован для Raspberry Pi;
ESpeech и F5 — современные flow-matching модели. Это ПОТОЛОК АРХИТЕКТУРЫ, а не недообучение:
сколько ни обучай 23.7 М параметров, звучание 330 М из них не появится.

⚠️ **Контроль поставлен, ждёт уха владельца** — `candidates/JARVIS/piper_ruslan_ETALON.mp3`:
те же пять текстов, озвученные ЧЕКПОЙНТОМ-ДОНОРОМ `ru_RU/ruslan/medium` (обучен его авторами
полностью, 1 724 372 шага, наше дообучение его не касалось). Логика контроля — EXP-0027:
**донор плох ⇒ виноват класс Piper, доучивать бессмысленно · донор хорош ⇒ виновато наше
обучение или корпус.** Пока владелец не сказал — вопрос открыт формально, но числа выше уже
называют ответ.

### 📉 Что нашлось в корпусе (объясняет темп, но не тембр)

| | |
|---|---|
| объём | 1185 реплик, **59.7 минуты** |
| медианный темп корпуса | **16.3 симв/с** |
| очень коротких реплик (<2.5 с) | **428 = 36%** |

Первое число закрывает вопрос, откуда у обученного голоса 16.9 симв/с: **модель честно выучила
темп начитки**, а не «ускорилась». Коридор владельца 12–14 симв/с — это МЕДЛЕННЕЕ, чем читают
аудиокниги. Второе число — реальная слабость корпуса: на репликах короче 2.5 с VITS учится хуже
всего, а их больше трети.

### 🔴 ФАЙЛЫ НА ВЫСЛУШКУ (два забракованы, третий — контроль)

`voice/out/candidates/JARVIS/` — рядом с уже одобренным `espeech_bystree.mp3`:

| файл | темп | что это |
|---|---|---|
| `piper_vihrov.mp3` | **16.9 симв/с** | голос ровно как обучился, `length_scale=1.0` |
| `piper_vihrov_temp14.mp3` | **13.9 симв/с** | `length_scale=1.35` — возврат к темпу САМОГО Вихрова |

⭐ **Почему второй вариант вообще существует, а не является «докруткой ручкой».** Коридор
владельца, выведенный из его же вердиктов, — 12–14 симв/с (17 у него уже «тараторит»). Модель
после обучения заговорила на **16.9**, при том что ИСХОДНАЯ запись Вихрова измерена в **14.1**.
То есть 1.35 не подгоняет темп под вкус, а **возвращает диктору его собственную скорость**,
которую модель ускорила. Плюс механизм другой, чем у забракованного `speed` в клоне F5:
`length_scale` масштабирует предсказанные длительности фонем ДО синтеза, и декодер порождает
речь заново, — это не растягивание готового звука. **Решает всё равно владелец.**

### ✅ Голос обучен — проверено, а не заявлено

| | |
|---|---|
| обучение | `global_step` **2904**, 22 эпохи, 784 тензора · ~20 минут |
| ⚠️ сколько это батчей | **1452**, а не 3000: у VITS ДВА оптимизатора, а `max_steps` считает шаги оптимизатора (EXP-0039) |
| метрики | `val_mel` 0.4812 → **0.4409** · `val_mos` 2.79 → **3.06** (лучшее на 9-й эпохе, дальше колеблется) |
| корпус | обучатель видит **1185 из 1185** реплик (до починки `bugs/18` видел 1082) |
| артефакт | `voice/out/piper_vihrov/vihrov.onnx` — **63.5 МБ, CPU, 0 ГБ VRAM** |
| звук проверен | нашими ушами: первый текст распознан почти дословно в обоих вариантах |

⭐ **Главное для архитектуры: 0 ГБ VRAM.** Это ровно то, что снимало противоречие «с загруженной
моделью свободно ~950 МБ, а клон-движкам нужно 3150 МБ». Лёгкий голос рядом с LLM помещается.

### ⚠️ Честная граница — что уши услышали и чего они НЕ доказывают

Обычную русскую прозу голос произносит чисто (текст 1 распознан дословно). А на текстах с
транслитерацией расшифровка разваливается: «си-плюс-плюс», «эн-пи-эм», «оупен веб-ю-ай»
теряются в обоих вариантах.

Но **винить в этом голос нельзя**: уши GigaAM `ru` — 34 токена чистого русского, и на
транслитерированных ПСЕВДОСЛОВАХ они слепы по построению (EXP-0022: молчание прибора, который
не умеет выразить искомое, — не улика). Что можно сказать честно: корпус — художественная
начитка, модель никогда не слышала «эн-пи-эм», и это **подтверждает вердикт владельца**
(«транслитерация — вынужденный костыль, лечение — мультиязычный движок»). Piper `ru` мультиязычным
не станет никогда — это его конструкция.

### ▶️ ПОВТОРИТЬ / ПРОДОЛЖИТЬ — команды

```powershell
# обучение (только из PowerShell и только фоновой задачей харнесса — см. грабли 1 и 4)
wsl -u root -e bash /mnt/f/KLAS/tools/voice/train_vihrov.sh 3000 16 6

# обученный голос -> звук на выслушку (экспорт + пять текстов + склейка + печать темпа)
wsl -u root -e /opt/piper1-gpl/.venv/bin/python /mnt/f/KLAS/tools/voice/piper_audition.py --length-scale 1.35 --suffix _temp14
node tools/voice/normalize-loudness.mjs F:\KLAS\voice\out\piper_vihrov\piper_vihrov_temp14.wav --out F:\KLAS\voice\out\candidates\JARVIS --lufs -14
```

⚠️ **Граната апстрима, уже обезврежена в скрипте:** `piper.train.export_onnx` зовёт
`torch.onnx.export` БЕЗ параметра `dynamo`, а в torch 2.13 умолчание уже `dynamo=True` — новый
экспортёр падает на сплайнах VITS. Скрипт возвращает `dynamo=False` поверх вызова, апстрим не
форкаем (EXP-0038). Ещё нужен `onnxscript`: venv собран через `uv`, **pip внутри нет** —
`uv pip install --python /opt/piper1-gpl/.venv/bin/python onnxscript`.

Готово на диске: корпус **1185 реплик (~60 мин)** в `/opt/dataset_vihrov` (и копия в
`voice/dataset_vihrov`; у обеих рядом лежит `metadata.csv.before-guard` — состояние до лечения),
чекпойнт-донор `ru_RU/ruslan/medium`, собранный `espeakbridge.so`, окружение
`/opt/piper1-gpl/.venv` (Python 3.11, torch 2.13.0+cu130, CUDA видна).

### 🐞 `bugs/18` — «CUDA out of memory» оказался ДЕФЕКТОМ ДАННЫХ, а не железа

Первый запуск упал через 37 шагов: `Tried to allocate 49.93 GiB` на карте с 16 ГБ. Очевидное
лечение «уменьшить батч» было бы лечением симптома — батч ни при чём.

Причина: Piper читает корпус `csv.reader(f, delimiter='|')` — разделитель мы задали, а
**`quotechar` остался дефолтным `"`**. Кавычка в НАЧАЛЕ текста строки 1082 открыла закавыченное
поле, закрывающей дальше в файле не нашлось — парсер склеил в одну реплику **весь остаток
корпуса, 104 реплики**. Реплика получила звук на 4 секунды и текст на 104 реплики.

Три числа сошлись в одну точку: 1082 + 103 поглощённых = **1185** строк файла · фонемная строка
**20 465** при медиане 117 · 16 × 2 × 20465² × 4 байта = **49.93 ГиБ**, байт в байт из ошибки
torch. В фонемах монстра прямо читаются ИМЕНА ФАЙЛОВ соседних реплик.

**Лечение — не разбор кавычек, а ПРЕДУСЛОВИЕ:** охранник
`node tools/voice/piper-dataset-guard.mjs <csv> [--fix]` снимает `"`, `|` и переводы строки
(ёлочки «» сохраняет — парсеру они не мешают, а речи нужны). Сборщик корпуса теперь прогоняет
через него расшифровку ушей и **отказывается отдать корпус**, не прошедший проверку. Охранник
проверен на НАСТОЯЩЕЙ сломанной версии: exit 1 и все 4 строки поимённо, после `--fix` парсер
Piper видит 1185 из 1185.

⚠️ **Почему это не поймал дымовой прогон:** битая реплика одна на 1082, при батче 16 её очередь
наступает в среднем на 68-м шаге — прогон в 3 шага физически не мог её встретить. **Смоук
доказывает, что собран КОНВЕЙЕР, и молчит про ДАННЫЕ.**

### ⛔ ГРАБЛИ — не наступать заново (три с прошлого вечера + три новые)

1. **`setsid nohup … &` в WSL НЕ переживает выхода из `wsl`.** Дистрибутив сворачивается и уносит
   отсоединённый процесс: лог остался нулевого размера, обучение не стартовало ни разу.
   Держать процесс задачей харнесса (`run_in_background`) или окном.
2. **Три тяжёлые операции разом уронили файл подкачки Windows** («The paging file is too small»):
   копирование 151 МБ через `/mnt`, обучение и опрос WMI одновременно. PowerShell упал, следом
   упал и сам процесс Claude Code (код 3221226505). Тяжёлое — **по одному**.
3. **`--ckpt_path` это НЕ «взять веса».** Lightning восстанавливает состояние обучателя и счётчик
   шагов (у донора 1 724 372), поэтому `max_steps 3000` завершил бы обучение мгновенно, изобразив
   успех. Правильный ключ — **`--model.warmstart_ckpt`**, он грузит только веса. Всё это уже
   зашито в `tools/voice/train_piper.py`, но знать надо.
4. 🆕 **Инструмент `Bash` агента — это Git Bash, и он съедает `/mnt`-пути.** Запуск
   `wsl -e bash /mnt/f/KLAS/...` из него даёт `exit 127` и
   `bash: C:/Program Files/Git/mnt/f/KLAS/...` — MSYS-слой переписал unix-путь в Windows-путь.
   **Команды в WSL запускай из инструмента `PowerShell`.** Аргумент был чистым ASCII ⇒ этот класс
   бьёт и без кириллицы (симптом 9.5 в `AGENT_GUIDE.md`, EXP-0035).
5. 🆕 **`.sh` в git на этой машине превращался в CRLF** (`core.autocrlf=true`), а bash с CRLF
   падает на `$'\r': command not found` — то есть свежий клон получил бы неработающий скрипт.
   Закрыто новым `.gitattributes`: `*.sh` → LF, `*.bat` → CRLF (парсеру cmd наоборот нужен CRLF,
   баг 01).
7. 🆕🔥 **ОБУЧЕНИЕ ВЫЕДАЕТ СИСТЕМНЫЙ ДИСК И ЛОМАЕТ ВЕСЬ ДИСТРИБУТИВ WSL.** Piper держит ДВА
   монитора качества (`val_mel` и `val_mos`), у каждого `save_top_k=5`, плюс `last` ⇒ до
   **11 чекпойнтов по 846 МБ за прогон (~9.3 ГБ)**, и прогоны копятся. `ext4.vhdx` дорос до
   83.4 ГБ, на `C:` осталось **7.49 ГБ из 465** — запись оборвалась, ext4 ушёл в аварийный режим.
   Симптомы разные, причина одна: сначала `Код ошибки: 6, шаг сбоя: 2` ·
   `Wsl/Service/CreateInstance/E_FAIL`, потом `getpwnam(root) failed 5` · `I/O error` на всё.
   ⛔ **ИЗНУТРИ WSL ЭТОГО НЕ ВИДНО:** виртуальный диск объявлен на 1 ТБ, и `df /` показывал
   **874 ГБ свободных**, когда на хосте было 7. Смотреть надо `df /mnt/c`.
   ✅ **Охранник поставлен и проверен на красном:** `train_vihrov.sh` смотрит на `/mnt/c` и
   отказывается стартовать при <25 ГБ (выход 2, с командами лечения).
   ✅ **Лечение инцидента:** `wsl --shutdown` (ext4 проигрывает журнал) → удалить чекпойнты
   (`find /opt/piper_train/vihrov/lightning_logs -name '*.ckpt' -delete`) → стек вернётся сам.
   ⚠️ Место Windows НЕ вернёт, пока диск не сжат, а сжать нельзя, пока запущен Docker Desktop:
   он держит `vmmemWSL` и файл занят. `wsl --manage --set-sparse` Microsoft отключила из-за
   риска повреждения данных (предлагает `--allow-unsafe` — **не применять**, тем более сразу
   после аварии с ФС). ⇒ сжатие только при полном выходе из Docker Desktop.
   ✅ **Сжатие выполнено 2026-07-31 (владелец остановил Docker):** `wsl --shutdown` → дождаться
   гибели `vmmemWSL` → `diskpart` с монтированием **только на чтение**:
   ```
   select vdisk file="C:\Users\krinik\AppData\Local\wsl\{cb9e52e7-…}\ext4.vhdx"
   attach vdisk readonly · compact vdisk · detach vdisk
   ```
   **45 секунд работы: диск 83.4 → 55.1 ГБ, свободно на C: 6.18 → 50.75 ГБ.**
   Целостность после сжатия проверена ДЕЛОМ, а не метаданными: `/etc/passwd` и locale читаются,
   корень смонтирован `rw` (не в аварийном режиме), чекпойнт 846 МБ прочитан целиком через
   `md5sum` без ошибок, все голосовые активы на месте (корпус 1185, ESpeech, F5, CosyVoice 3
   9.1 ГБ), CUDA видна. Стек поднят и отдаёт 200 на пульте, вики и homepage.
   ⚠️ `wsl --shutdown` роняет и `docker-desktop` ⇒ стек владельца уходит в даун. Цена считается
   ЗАРАНЕЕ: у всех наших контейнеров `unless-stopped`, они вернулись сами за 8 секунд, но это
   **outward-facing действие — спрашивать владельца** (спросил, разрешил).
   ⚠️ Модалка Docker Desktop «WSL integration with distro Ubuntu unexpectedly stopped» — это
   СЛЕДСТВИЕ. Docker был жертвой; кнопка «Restart the WSL integration» не лечит. Владельцу в
   такой момент правильно нажать «Skip WSL distro integration» — контейнеры в другом
   дистрибутиве и не страдают.
   📌 **Мой первый диагноз этого инцидента был НЕВЕРЕН** («виноват `TaskStop`») — я проверил
   оперативную память и не посмотрел диск. Разбор ошибки диагностики — EXP-0040, пункт 2.
6. 🆕 **Код и текст в WSL — только ФАЙЛАМИ.** За эту сессию я трижды наступил на одно: `awk` с
   `<` развалил парсер PowerShell, питон-однострочник со скобками — тоже, многострочное сообщение
   коммита с бэктиками разлетелось на `pathspec`. Канон это правило содержит; лечение каждый раз
   одно — `Write` файла и запуск файла (`git commit -F <файл>`).

### 🧭 Дизайн бенча «грамотность пользования ПК» — согласован

`researches/18_windows_pc_bench.md`. Метод взят у **WindowsAgentArena**: проверка ИСПОЛНЕНИЕМ.
Ответы владельца записаны каноном: окна открывать во время бенча можно · **GUI и CLI меряются
РАЗДЕЛЬНО, консоль в GUI-задаче = читерство и ошибка модели** · порога нет, смотрим абсолютные
цифры. Восемь задач расписаны. **Кода ещё нет** — следующий шаг после обучения.

### 📊 Модели перемерены (ответ на вопрос владельца про gemma)

| | цепочки | время/задача | VRAM |
|---|---|---|---|
| `qwen3.6-35b-a3b` | **6.00/6.0** | **11 с** | 15 356 МБ |
| `gemma-4-12b-norep` | **6.00/6.0** | 22 с | 12 093 МБ |
| `gemma-4-12b` (боевой, со штрафами) | 4.60/6.0 | — | — |

**«Гемма болезненная» было НАШЕЙ настройкой:** снятие штрафов за повтор подняло 4.60 → 6.00
(`copy-2` с 0.00 до 1.00). Ставка владельца «гемма будет быстрее» **не сыграла** — она плотная 12B
против MoE с 3 млрд активных. Основную модель НЕ менял: это решение владельца.

⛔ **Тупик, проверенный замером:** урезание контекста qwen с 98K до 32K освободило всего **330 МБ**
(память ест не KV-кеш, а веса) — значит голос рядом с qwen не поместится никак.

### 🐞 `bugs/17` — песочница бенчей тихо портила замеры

В `~/.openclaw-pilot` список лечения `bugs/05` был наполовину (`pattern` вместо пяти ключей),
и qwen выдавала честный ноль по всем задачам, который читался бы как приговор модели. Починено,
копия сохранена. **Историческая цифра «qwen 5/6» из `researches/09` теперь под сомнением** —
она мерилась там же.

---

**⏸ ПАУЗА 2026-07-30. Голос ЗАМОРОЖЕН на достигнутом по решению владельца: «голос можно будет
доработать до идеала позже». СЛЕДУЮЩАЯ ЗАДАЧА — АКТИВАЦИОННЫЕ СЛОВА «ДЖАРВИС» И «ДЖОЙ».**

### 🏁 ЭСТАФЕТА СЛЕДУЮЩЕМУ АГЕНТУ — начни отсюда

Каркас цел: `npm run kaif:check` → **52 файла + 108 агентских артефактов на месте**.
Дерево чистое, `main` == `origin/main`.

**Твоя задача одной строкой:** запуск ассистента по имени — «Джарвис» и «Джой».

**Определение готовности** (не «работает у меня», а измеримо): обе модели детектируют своё имя
на **живом микрофоне владельца** с долей срабатываний, сравнимой с контролем, и НЕ зажигают
чужой детектор. Стенд с положительным контролем уже написан — `tools/voice/wakeword-probe.py`;
контроль там обязателен, иначе ноль неотличим от сломанного стенда (EXP-0027).

**Первые три шага:**
1. Прочитай `researches/16_wake_word.md` §3.1–3.2 — **оба готовых пути уже проверены и отвергнуты
   замером.** Не переоткрывай: `KeywordSpotter` дал 3/6, готовая `hey_jarvis` — 0/10.
2. Разверни обучение openWakeWord **в WSL2** (только Linux; на Windows не заведётся). Окружение
   готово: CUDA видна, `uv` стоит, Python 3.10 ставится одной командой — всё сделано в голосовой
   ветке 2026-07-30.
3. Сгенерируй корпус на «Джарвис» и «Джой». ⭐ **Генератор у тебя уже есть:** рабочие клоны обоих
   голосов (`tools/voice/clone_espeech.py`, эталоны в `voice/sources/`) плюс слой нормализации.
   Это дешевле и ближе к цели, чем синтетика чужим голосом.

**Открытый беклог, чтобы ты знал поле:** баги `04` (Open WebUI), `07` (Windows-MCP порча символов),
`10` (уши без латиницы — ждёт вердикта владельца), `15` (гейтвей умирает молча, ловушка стоит),
`16` (разобран, охранник готов, врезать при следующем прогоне клонирования).
Планы `11`–`15` — голосовые фазы Г1–Г5; активатор относится к `15` (клоны и персоны) и `08` (эпик).

**Чего НЕ делать:** не трогать голос дальше без просьбы владельца — он заморожен осознанно ·
не «чинить» `bugs/15` автоперезапуском (уничтожит единственный путь к причине) · не докручивать
темп клона ручкой `speed` (владелец слышит манипуляцию).

### ▶️ ГДЕ ПРОДОЛЖИТЬ — запуск по слову-активатору

**Задача владельца дословно:** *«сейчас важно сделать запуск по активационным фразам-именам:
Джарвис и Джой»*.

**Что уже известно и НЕ надо переоткрывать** (`researches/16_wake_word.md`, замеры 2026-07-29):

| путь | вердикт |
|---|---|
| sherpa-onnx `KeywordSpotter` (без обучения, списком слов) | ⛔ потолок **3/6** на русском произношении |
| готовая `hey_jarvis` из openWakeWord | ⛔ **0/10**; она обучена на английской ФРАЗЕ «hey jarvis», голого «Джарвис» нет |
| **обучить свои две модели** | ✅ единственный оставшийся путь. Ресурсы владелец выдал: «обучение — хоть всю видюху» |

**Нулю можно верить:** рядом стоял положительный контроль — клип «Alexa.» настоящей английской
моделью зажёг свой детектор на 1.000, не тронув чужой ⇒ стенд исправен (EXP-0027).

**Стенды готовы:** `node tools/voice/kws-probe.mjs` · `voice\venv-wakeword\Scripts\python.exe
tools/voice/wakeword-probe.py` (второй несёт тот самый контроль).

⚠️ **Логистическая грабля:** автообучение openWakeWord работает **только на Linux** (зависимость
Piper) ⇒ WSL2 или Colab, не Windows напрямую. WSL с CUDA у нас **уже поднят и проверен**
(`torch 2.13.0+cu130`, RTX 5070 Ti видна, capability 12.0) — это сделано в голосовой ветке
и теперь работает на активатор.

🎁 **Голосовая работа отдаёт активатору готовый актив.** Обучение wake word идёт на СИНТЕТИЧЕСКИХ
сэмплах (`rhasspy/piper-sample-generator`), а у нас теперь есть **работающий клон обоих голосов**
и слой нормализации: «Джарвис» и «Джой» можно наговорить сотнями вариантов, в том числе голосами
самих персон. Это ровно тот материал, которого требует конвейер.

**Первый шаг следующей сессии:** развернуть обучение openWakeWord в WSL2 (там же, где стоит
CosyVoice — `uv` уже установлен, Python 3.10 ставится одной командой), сгенерировать корпус
на «Джарвис» и «Джой», обучить, прогнать через существующий стенд с контролем.

### ✅ ГОЛОСА ВЫБРАНЫ ВЛАДЕЛЬЦЕМ 2026-07-30 (канон, не пересматривать без его слова)

| персона | файл | чем порождён |
|---|---|---|
| **JARVIS** | `candidates/JARVIS/espeech_bystree.mp3` | движок **ESpeech-TTS-1 RL-V2** · эталон `voice/sources/jarvis_vihrov/ref_vihrov_12s.wav` (11.77 с, 14.1 симв/с) · `speed=1.06` · `TRANSLIT=full` |
| **JOI** | `candidates/JOI/gorokhova_112cps.mp3` | движок **F5-TTS_RUSSIAN** v4_winter · эталон `voice/sources/joi_gorokhova/joi_v_112.wav` (8.79 с, 11.2 симв/с, цельный дубль из русской озвучки Cyberpunk 2077) · `speed=1.0` · `TRANSLIT=full` |

Дословно: *«как будто бы этот больше понравился… и этот… можем пока на них зафиксироваться»*.
Оба воспроизводятся одной командой каждый — параметры выше полные, ничего доугадывать не нужно.

### 🔴 ПОГОВОРИТЬ С НИМИ ПО МИКРОФОНУ СЕЙЧАС НЕЛЬЗЯ — две преграды, обе измерены

**1. VRAM.** Замер 2026-07-30: с загруженной основной моделью занято **15 356 МБ из 16 303**,
свободно **~950 МБ**, а обоим выбранным движкам нужно **3 150 МБ**. Не влезают. Это ровно то
противоречие, которое владелец назвал сам: «при инференсе голос должен быть ресурсо лёгким».

**2. Тракт.** Выбранные голоса — пакетные скрипты в WSL (загрузка модели ~10 с, синтез на GPU).
В живом разговоре работает Silero: резидентный, CPU, 0.1 с на фразу. Чтобы говорить выбранными
голосами, нужен резидентный демон F5 + мост Windows↔WSL, по образцу `silero_daemon.py`.

**Три пути, честно с ценой:**

| путь | что даёт | цена |
|---|---|---|
| ужать контекст основной модели или взять меньшую на время голосового сеанса | говорить выбранными голосами **сегодня** | меньше контекста или меньше ума; плюс работа по демону и мосту |
| **обучить лёгкий голос (Piper VITS) на выбранных эталонах** | CPU, **0 ГБ VRAM**, реальное время, ничего не конкурирует с LLM | часы обучения — но это ровно архитектура владельца «тяжёлое один раз офлайн, лёгкое каждый раз» |
| оставить Silero в разговоре, выбранные голоса — для подготовленной речи | ничего не ломается | владелец Silero забраковал |

⭐ **Рекомендация агента:** второй путь. Материал под него уже собран и лежит в `voice/sources/`
(Вихров 2 ч 29 мин; Горохова — 8 359 реплик восстанавливаются одной командой), а конвейер обучения
Piper — тот же, что понадобится для активатора. **Решение за владельцем.**

### 🧹 Уборка выполнена по указанию владельца

Освобождено **1.77 ГБ**. Удалены: сырьё Cyberpunk (8 359 `.wem` + 200 wav + карта), все
промежуточные итерации клонов (v1–v5, bug16, probe/kws/multi/v55), папки `refs_*`.
**Исходники голосов собраны в одну папку `voice/sources/`** — оригиналы владельца, одобренные
эталоны, разбор 143 реплик и пути всех 8 359 (чтобы вернуть сырьё одной командой, см. README там же).
⚠️ Пути по умолчанию в `clone_espeech.py` / `clone_qwen.py` переведены на `voice/sources/`.

---

**2026-07-30 (день) — 🎙 НАЙДЕН НАСТОЯЩИЙ КОРПУС ГОЛОСА JOI: 8 359 реплик живой Гороховой
из русской озвучки Cyberpunk 2077. Цепочка извлечения собрана и работает.**

### Почему это меняет задачу целиком

До этого материал Joi был лоскутным: 6-секундный рекламный клип, 19-секундное интервью,
40-секундный демо-рил. Ни один не давал одновременно нужный темп, спокойную манеру и чистоту —
и владелец справедливо сказал «нужно искать другой оригинал».

Оригинал нашёлся там, где он и должен был быть: **основную кампанию Cyberpunk 2077 официально
дублировала Юлия Горохова**, и это часы студийной записи одного диктора во всех регистрах.

⚠️ **Отличать от нейроклона.** Владелец качал `Rus_Dub_by_DreamVoice_v5.3.1` — это ИИ-озвучка
дополнения Phantom Liberty (у него официальной русской озвучки не было). Клонировать клон значит
выучить и голос, и артефакты чужой нейросети как одно целое. **Проверено датами файлов, не на
слово:** `content\lang_ru_voice.archive` (4.99 ГБ) имеет ту же метку `05.12.2023 14:02`, что ВСЕ
официальные архивы игры ⇒ не тронут; `ep1\lang_ru_voice.archive` (4.16 ГБ) — `30.12.2023 02:35`,
единственный файл в папке с другой датой ⇒ это и есть нейродубляж.

### 🔧 Цепочка извлечения (работает, воспроизводима)

| шаг | чем |
|---|---|
| 1. распаковать архив | `voice/tools/wolvenkit/WolvenKit.CLI.exe unbundle <archive> -o <dir> -r "vo\\v_.*\.wem"` |
| 2. найти реплики ИГРОКА | `voiceovermap.json` из того же архива → `convert serialize` → у реплик Ви **женский и мужской путь РАЗНЫЕ** (их писали два актёра), у NPC одинаковые |
| 3. `.wem` → `wav` | **`voice/tools/vgmstream/vgmstream-cli.exe`** — 48 кГц стерео |
| 4. отобрать эталон | `node tools/voice/ref-scout.mjs` либо ранжирование готовых реплик по симв/с |

⛔ **Тупики, не повторять:** CLI-команда `wwise` в WolvenKit 8.19 падает («Type System.IO.FileInfo
cannot be created without a custom binder») · ffmpeg `.wem` не декодирует вовсе · `archiveinfo`
списка файлов не даёт. Зато **шаблон работает как счётчик**: `unbundle -w "<шаблон>"` печатает
«Found N/90700», ничего не извлекая, — так и подбирался тег.

### 📊 Что получилось

| | |
|---|---|
| всего реплик в русской озвучке | 32 767 |
| с разными женским/мужским вариантом (реплики игрока) | 9 945 |
| **женских реплик Ви (тег `v_`)** | **8 359, извлечены** |
| из них длиной 8–12 с — готовый размер эталона, ЦЕЛЬНЫЙ дубль | **143** |

🎁 **Побочно решён вопрос обучения.** У каждой реплики есть `stringId`, то есть привязка к тексту
субтитра. Пара «звук + текст» на тысячах реплик одного диктора — это уже полноценный обучающий
корпус, а не 19 секунд, на которых владелец предлагал обучать.

### 🎯 Целевая манера Joi — уточнена владельцем

**«бодрый, чуть-чуть озорной, ровный и чёткий»** (как голос Вихрова). Это согласуется с измеренным
коридором: 12–14 симв/с и есть «ровно и чётко»; 10 — томная медлительность, 17 — тараторенье.

⚠️ **Но cps предсказывает ТЕМП, а не МАНЕРУ.** Проверено дорого: окно из демо-рила легло ровно
в 13.0 симв/с, а вердикт — **«отвратительно, кричит»**: материал был восклицательной рекламой.
Манера переносится в клон так же сильно, как тембр, и числом пока не отбирается.

---

**2026-07-30 (утро, автономное окно 11:00–12:00) — 🔌 БОЕВОЙ РОТ ПОДКЛЮЧЁН К ОБЩЕМУ СЛОЮ (там был
ДУБЛЬ), 📥 ДВА НЕПРОБОВАННЫХ ДВИЖКА СКАЧАНЫ, F5-TTS_RUSSIAN УЖЕ ОЗВУЧЕН.**

### 🚨 Главное: правки произношения не доезжали до живого ассистента

`silero_daemon.py` **не импортировал** `text_norm.py`. Слой был «вынесен», но тракт на него не
переключили — в файле осталась ВТОРАЯ копия. Полтора дня правок (дроби, код, транслитерация,
«класс», «Ти-Ай», снятое двоеточие) жили только в модуле.

Вопрос задал сам владелец: *«может я бракую то, что звучало бы хорошо с этим импрувментом?»* —
и это тот же класс, что `bugs/16`, где ESpeech судили по испорченному выходу. **Вывод для канона:
вынести реализацию мало — надо СНЯТЬ старую, иначе «вынес» означает «сделал вторую».**

Сделано: импорт `normalize`, `TRANSLIT_MODE='full'` (Silero одноязычный ⇒ латиница уходила ВТОРОЙ
модели, то есть другому диктору — это и есть «разные голоса… НЕПРИЕМЛИМО»), дубли удалены.
**Бенч в окружении владельца: 10/10.** Прогнан ДО и ПОСЛЕ удаления дубля; охранники не ослаблялись.

### 🗣 Вердикты владельца этого окна (канон)

| Что | Дословно |
|---|---|
| `Ti` | **«Ти — Ти Ай должно быть»** |
| `KLAS` | **«KLAS плохо произносит»** → отдавали ЗАГЛАВНЫМИ, для синтеза это сигнал читать по буквам; пишем словарным «класс» |
| правило аббревиатур | ⭐ **«некоторые аббревиатуры можно читать, как знакомые слова — класс, ты верно понял»** ⇒ складывается слово → читаем словом; не складывается → по буквам |
| пауза | **«не делаем паузы между „работает“ и „пайтон скрипт“»** → там ДВОЕТОЧИЕ, движок читает его разделителем; снято |
| темп | **«хорошо, можно чуть-чуть ускорить»** (оба ESpeech-фаворита) |
| ⭐ транслитерация | **«решение с транслитерацией — это вынужденный костыль»** ⇒ настоящее лечение — МУЛЬТИЯЗЫЧНЫЙ движок |

### 🧭 Развилка, которую вскрыл вопрос про мультиязычность

> **Фаворит по тембру (ESpeech) — русская модель, английский честно произнести не может НИКОГДА.
> Фаворит по английскому (Qwen) — мультиязычный, но у него тоньше верх (нейрокодек 12 Гц).**

Транслитерация — не выбор, а следствие выбора одноязычного движка. Мультиязычные в пуле:
**Qwen3-TTS** (пробован) и **CosyVoice 3** (скачан, не пробован).

### 📦 Скачано в это окно (вне git)

| Путь | Что |
|---|---|
| `/opt/f5ru` | **1.3 ГБ, F5-TTS_RUSSIAN v4_winter** (Misha24-10, 5000+ ч). ⛔ одноязычный |
| `/opt/cosyvoice3` | **9.1 ГБ, Fun-CosyVoice3-0.5B-2512** — ✅ **МУЛЬТИЯЗЫЧНЫЙ**, Apache-2.0, кросс-язычный клон |

🎁 **`hf_transfer` установлен** — канал дал **10–12 МБ/с** вместо 1.2. Загрузка 9 ГБ заняла ~13 минут.
⚠️ **Поправка к прежней записи:** CosyVoice 3 существует ТОЛЬКО в 0.5B — старшей версии нет.
Прошлая сессия зря корила себя за «взял младшую»: выбора тут не было. Есть и GGUF-сборка
(`cstr/cosyvoice3-0.5b-2512-GGUF`) — прямо в требование лёгкого инференса.

### 🎧 Что ждёт ушей владельца

- **`voice/out/clone/v2/`** — 9 файлов: транслитерация, «класс»/«Ти-Ай», снятое двоеточие,
  Qwen по предложениям и с `temperature=0.6`, пары на A/B по темпу.
- **`voice/out/clone/v3/`** — 2 файла: **F5-TTS_RUSSIAN** на тех же эталонах и текстах.
  Запустился УЖЕ НАПИСАННЫМ `clone_espeech.py` — правка ради него заняла две строки (`F5_MODEL`/`F5_VOCAB`).

### ▶️ Следующий шаг

**Поднять CosyVoice 3** (скачан, есть `cosyvoice3.yaml`, `llm.pt`, `flow.pt`, `hift.pt`,
`speech_tokenizer_v3.onnx`, `campplus.onnx`) и озвучить те же пять текстов на тех же эталонах.
Это единственный непробованный МУЛЬТИЯЗЫЧНЫЙ кандидат, и именно он может снять развилку
«тембр против честного английского».
⚠️ Нужен рантайм `cosyvoice` (репозиторий FunAudioLLM), в venv его пока нет — ставить с
`--resume-retries`, туннель рвётся.

---

**2026-07-30 (ночь, живая работа с владельцем) — 🎙 ГОЛОСА КЛОНИРОВАНЫ И ОТСЛУШАНЫ ВЛАДЕЛЬЦЕМ.
Эталон Jarvis ОДОБРЕН. По Joi идёт живая настройка темпа и тона.**

### ✅ Вердикты владельца этой сессии (канон)

| Что | Дословно / решение |
|---|---|
| эталон Jarvis | ✅ **`ref_3_52m14s`** — чистое повествование, без отыгрыша персонажей |
| Joi — материал | владелец сам собрал `Юлия Горохова Cyberpunk 2077.mp3` + дал `Recsquare_Горохова_Киберпанк.mp3` и `1669_gorohova-yuliya_demo.mp3` |
| Joi — «Киберпанк» | 🟡 **«голос ОЧЕНЬ приятный, но медленно»** → ускорить на 20%, чуть поднять тон |
| Joi — «Туапсе» | 🟡 **«слишком бодрый, нужно более томным»** → замедлить |
| Joi — Ви | 🟡 **«тараторит»** + **«тихо»** → замедлить и выровнять громкость |
| Joi — склейка | 🟡 **«тут приятнее голос»** |
| ⭐ Форма выдачи | **«по одному файлу на выслушку по варианту · не делай копии вав и мп3 · клей кусочки воедино, кусочки удаляй · папки делай по кандидатам · нормализуешь — тянешь до высокой громкости»** |

### 🎧 Выдача владельцу — `voice/out/clone/` (индекс: `README.md` там же)

Пять папок по кандидатам, **10 файлов, по одному на вариант**: в каждом все пять текстов кастинга
подряд, громкость всех выровнена к **−14 LUFS**. Кусочки удалены, пар wav+mp3 нет.

### 🔬 Два механизма, найденные замером (оба меняют архитектуру голоса)

1. **Клон копирует из эталона ТЕМП.** `duration = ref_audio_len/ref_text_len × gen_text_len / speed`
   (F5, `utils_infer.py:505`). «Символов в секунду» у эталона = будущая скорость речи. Замерено:
   Киберпанк 10.1 · склейка 12.9 · Вихров 14.1 · Туапсе 16.1 · Ви 17.2 — и вердикты владельца
   выстроились по этой оси ровно («приятный, но медленно» → «приятнее» → «слишком бодрый» →
   «тараторит»). **Гипотеза: его коридор 12–14 симв/с.** Дважды угадала направление правки.
   Лечение — штатная ручка `speed`, выведена наружу в `clone_espeech.py`.
2. **Клон копирует из эталона УРОВЕНЬ ЗАПИСИ.** Эталон −13.7 дБ → клон −12.6; эталон −23.0 дБ →
   клон −24.3…−29.8. Владелец услышал это как «тихо». ⇒ громкость ассистента зависела бы от
   того, с какой записи склонирован голос, а на прослушивании человек сравнивал бы громкость
   вместо голоса. Лечение — `tools/voice/normalize-loudness.mjs` (EBU R128, два прохода).

### 🧰 Скрипты клонирования переехали В РЕПОЗИТОРИЙ

`tools/voice/clone_espeech.py` и `clone_qwen.py` (запускаются из WSL, лежат в git). Прошлая сессия
держала их в `/opt` — вне git, и свежая сессия о них не узнавала. Оба параметрические: эталон,
текст, префикс, темп — аргументами, а не копией скрипта под каждый голос.

⚠️ **Грабля сессии:** `.ps1` с кириллицей, записанный инструментом `Write`, идёт **без BOM**, и
PowerShell 5.1 читает его как ANSI → `The string is missing the terminator`. Канон это правило
содержит, я всё равно наступил. Лечение — дописать BOM байтами перед запуском.

---

**2026-07-29 (поздний вечер) — 🎧 ЭТАЛОНЫ ВИХРОВА НАРЕЗАНЫ И ЖДУТ УХА ВЛАДЕЛЬЦА. Попутно найден
механизм дефекта ESpeech — и виноваты были МЫ, а не модель.**

### ⏳ ЕДИНСТВЕННОЕ, ЧТО СЕЙЧАС БЛОКИРУЕТ ГОЛОСОВУЮ ВЕТКУ

**Владелец должен назвать номер куска** в `homeworks/03_voice_quality_audition.md`. Клонировать без
одобренного эталона запрещено его прямым словом. Пока ответа нет — за голос не браться, брать
задачи из автономного пула.

### ✅ Что произведено

**`F:\KLAS\voice\refs_vihrov\`** (вне git) — шесть кусков по 60 с из записи Вихрова, нарезаны
`ffmpeg -c copy` = **побитовая копия без перекодирования**, качество источника (44.1 кГц, стерео,
160 кбит/с). Рядом с каждым mp3 — `.txt` с текстом эталона (GigaAM `punct`): **оба движка клонируют
по ПАРЕ «звук + текст»**, одного звука мало. Индекс и правила нарезки — `voice/refs_vihrov/README.md`.

Проверено ДО того, как звать владельца (прошлый провал был ровно в обратном порядке):

- **ушами по всем шести целиком** — сплошная связная речь 110–120 слов/мин, ни музыки, ни провалов;
- **профиль F0 всей записи** (окно 10 с каждые 5 мин, 29 точек по 2 ч 29 мин): **76–127 Гц, медиана
  ~100** ⇒ один начитчик с широким драматическим диапазоном. Повторение провала «шесть эталонов
  оказались одним диктором» здесь невозможно по построению — тут заведомо один человек;
- **однородность каждого куска** (6 окон по 10 с): `ref_4` самый ровный (95–101 Гц), `ref_3` весь
  лежит выше остальных (112–139 Гц), `ref_6` начинается разрежённо. Это инженерия эталона
  (разнорегистровый кусок даёт мутный клон), **а не оценка красоты** — красоту судит владелец
  (EXP-0030).

### 🐞 `bugs/16` — вклейка эталона у ESpeech РАЗОБРАНА И ДОКАЗАНА ЗАМЕРОМ. Виноваты были МЫ

`f5_tts/infer/utils_infer.py:344` — **F5 режет эталонный ЗВУК до 12 000 мс, а ТЕКСТ эталона не
трогает.** Прошлый эталон `cut_rassadin_15s` = 15 с звука → обрезан до 12 с, текст подан на все 15.

**Замер (не рассуждение):** один и тот же эталонный звук **9.27 с** во всех трёх вариантах,
менялась ТОЛЬКО длина текста эталона. Пара сходится (133 симв.) → **5 из 5 синтезов чистые**.
Текста вдвое больше (272 симв.) → **10 из 10 испорчены**, и в расшифровке дословно всплывают слова
эталона: «**Пересели** в добрый вечер…», «**И скрылись.** Добрый вечер…».
Стенд и улики — `voice/out/bug16/` (`probe.log`, `hear.txt`).

**Виновата НЕ обрезка, а РАССИНХРОН пары:** у контрольного варианта обрезка не срабатывала вовсе
(звук 10 с при пороге 12) — и он вклеил эталон так же. Обрезка лишь самый частый способ получить
рассинхрон.

**Вторая, более крупная половина дефекта:** ломается ТЕМП. На одном и том же звуке реплика про
видеокарту звучит **15.87 с** при сходящейся паре и **7.87 с** при рассогласованной — ровно вдвое
быстрее. То есть это не «иногда вклеивает фразу», а порча КАЖДОГО синтеза.

⚠️ **Следствие для прежних выводов: ESpeech судили по испорченному выходу** — макет снимался с
`cut_rassadin_15s`, то есть с рассогласованной пары. Его настоящее звучание ещё не слышал никто;
строка «качество» в сравнении движков недействительна. RTF почти не поехал (0.218 против 0.208).

⚠️ **Прежняя гипотеза в этом файле («лечение — нарезка синтеза на короткие фразы, `bugs/09`»)
промахивалась мимо причины:** она про длину СИНТЕЗА, а дело в длине ЭТАЛОНА; такое «лечение»
замаскировало бы симптом.

✅ **Лечение развёрнуто: `tools/voice/ref_pair.py`** — охранник пары, бросает `ValueError` ДО
синтеза, лежит рядом с `text_norm.py` (оба скрипта синтеза уже импортируют этот путь). Самопроверка
`python tools/voice/ref_pair.py` — 4/4 PASS, среди них отвергается **эталон 15 с, ровно та
конфигурация, что породила дефект**, и проходит обратный случай «те же 15 с для Qwen».
Осталось врезать вызов в скрипты синтеза при следующем прогоне клонирования.

**Следствие для нарезки:** ESpeech — пара **≤12 с звука + ровно её текст**; Qwen ограничения длины
не имеет (искал в `qwen_tts` — ни `max_ref`, ни обрезки нет), берёт минуту целиком.

---

**2026-07-29 (19:00–20:00, автономное окно по слову владельца) — 🏆 НАЙДЕНА ОТКРЫТАЯ МОДЕЛЬ, КОТОРАЯ
ОБГОНЯЕТ ELEVENLABS НА РУССКОМ. Противоречие «качество против ресурсов» может оказаться ложным.**

### ⭐ Главное: разведка вчерашнего дня устарела за сутки

`researches/15` §7.3 ставил в проверку CosyVoice 3 **0.5B** — и это была та же ошибка, за которую
владелец уже поправлял агента (EXP-0031): взята МЛАДШАЯ модель, то есть снова сэкономлено там, где
просили качество. Свежий поиск дал кандидата, которого в документе нет вовсе:

| модель | WER на **русском** (ниже = лучше) |
|---|---|
| **Qwen3-TTS-12Hz-1.7B** | **3.212** |
| ElevenLabs | 3.878 |
| MiniMax | 4.281 |

**Apache-2.0 · 2.5 млн скачиваний · репозиторий живой (12 672★, не архив) · русский И английский
в ОДНОЙ модели · клон голоса с 3 с · родной стриминг.** Тезис владельца «11 лабс делает гораздо
лучше» по русскому языку на сегодня **неверен**.

⚠️ Прежний вердикт `researches/15` «Qwen3-TTS выбывает из роли рта» касался **0.6B и роли рантайма**
под ограничение §0.1. Он НЕ отменён, но и НЕ применим к вопросу «какое качество вообще достижимо»:
сначала владелец слушает лучшее, потом считаем цену (EXP-0031, пункт 3).

### 🎁 Побочная находка, способная снять противоречие целиком

У Qwen3-TTS 1.7B на HuggingFace лежат **готовые GGUF- и ONNX-сборки** (`cstr/qwen3-tts-1.7b-base-GGUF`,
`xkos/Qwen3-TTS-12Hz-1.7B-ONNX`). ~~GGUF — это наш llama.cpp, ONNX — это sherpa-onnx, на котором уже
работают уши. Если хоть одна поедет, «качество требует GPU» перестаёт быть правдой.~~

> ⛔ **ПОПРАВКА 2026-07-30 — это допущение ПРОВЕРЕНО И ОКАЗАЛОСЬ НЕВЕРНЫМ.** `llama-tts.exe --help`
> показывает поддержку **только OuteTTS** (`--tts-oute-default`, вокодер через `-mv`); архитектур
> Qwen3-TTS и CosyVoice3 наш llama.cpp не знает. `OfflineTts` из sherpa-onnx требует «exactly one
> tts model» своих семейств (VITS/Matcha/Kokoro), а ONNX-сборка Qwen3 — это конвейер из энкодера,
> декодера и предсказателя кодов, который в эту форму не ложится. **Сборки существуют и все
> Apache-2.0, но каждой нужен СВОЙ рантайм — «воткнуть в то, что уже стоит» не получится.**
> Разбор и таблица живости — `plans/18_light_voice_engine.md`, трек В.

### ⛔ Отсеяно проверкой (сэкономило гигабайты загрузки)

- **Voxtral TTS (Mistral, март 2026)** — гремит в обзорах как «победил ElevenLabs в 68.4%», но
  **славянских языков в нём нет вовсе**. Русского не будет. Проверено до загрузки.
- **Fish-Speech** — лицензия NOASSERTION, отложен.
- **Sesame CSM** — лидер MOS, но 14 месяцев без push (правило живости владельца).

### ⛔⛔ СНАЧАЛА ПРОЧТИ ЭТО: ВСЁ ГОЛОСОВОЕ АУДИО УДАЛЕНО ПО ПРЯМОМУ ПРИКАЗУ ВЛАДЕЛЬЦА

**Не ищи файлы, о которых говорят старые записи ниже и в `homeworks/03` — их больше нет.**
Удалено вечером 2026-07-29 по дословным указаниям владельца («удаляй нахуй этот мусор»):

| Что удалено | Почему |
|---|---|
| `voice\out\casting\` (64 файла) | кастинг Silero/Piper — владелец отверг ВСЕ эти голоса |
| `voice\out\sravnenie\` | сравнение 4 способов, тоже на отвергнутом Silero |
| `voice\out\qwen\`, `voice\out\espeech\` | макеты и A/B — сняты с эталонов, которые владелец забраковал |
| `voice\refs\`, `voice\refs2\`, `voice\refs_vihrov\`, `voice\vihrov\` | все мои эталоны, см. разбор провалов ниже |

**Модели и окружение НЕ трогали** — они целы и стоили часа загрузки (таблица ниже).

### 🚨 ВЕРДИКТЫ ВЛАДЕЛЬЦА (канон, не переоткрывать)

| Что | Дословно |
|---|---|
| Silero целиком | ⛔ **«отвратительно. разные голоса. НЕПРИЕМЛИМО»** — вопрос закрыт |
| клон `eugene` через Qwen | 🟡 «единицы измерения плохо читает. Код тоже. В остальном неплохо, но **не супер круто**» |
| родные дикторы Qwen (`ryan`, `eric`) | ⛔ **«ДИБИЛЬНЫЕ. НЕ ДВОРЕЦКИЕ»** — международные пресеты в роль дворецкого не годятся |
| длительность сэмплов и обучения | ⭐ **«не скупимся: между 10 секундами и 1 минутой — берём длиннее; между часом обучения и пятью — пять часов»** |
| процесс | ⭐ **эталон согласует ВЛАДЕЛЕЦ до клонирования**: «принеси мне их на отслушивание — я согласую» |

### 🎙 ГЛАВНЫЙ АКТИВ, С КОТОРОГО НАЧИНАТЬ СЛЕДУЮЩУЮ СЕССИЮ

**`F:\KLAS\Vladimir_Vihrov_-_Smert_ranenogo_zverya_s_tonkoj_kozhej_2_2005_(SkySound.cc).mp3`**
— скачал САМ ВЛАДЕЛЕЦ, потому что агент не справился. Проверено замером:

- **2 часа 29 минут**, 44.1 кГц, стерео, 160 кбит/с — качество нормальное;
- это **чистая дикторская начитка Вихрова**, проверено ушами в ШЕСТИ точках по всей длине
  (07:27 · 29:50 · 52:14 · 74:37 · 97:00 · 119:23) — везде 37–52 слова за 25 секунд, музыки нет;
- ✅ **закрыт в `.gitignore`** правилом `/*.mp3` (`.gitignore:105`, с объяснением, почему правило
  снимать нельзя). `git check-ignore -v` подтверждает — случайный `git add -A` его не утащит.

### 💀 РАЗБОР ПРОВАЛОВ ВЕЧЕРА — читать, чтобы не повторить

За вечер агент трижды сжёг время владельца, и каждый раз владелец находил ошибку быстрее агента:

1. **Шесть «разных» эталонов оказались ОДНИМ диктором.** Взял шесть изданий из одной номерной
   серии archive.org, не посмотрев поле «Исполнитель» в тех же метаданных, которые сам и качал:
   все читала Саша Жукова (коллекция `studiosimsim`). **Свидетельство было:** F0 всех шести совпал
   (167–179 Гц). Агент объяснил это «удвоением октавы на сжатом звуке» и даже вписал ложное
   ограничение в шапку `tools/voice/f0-of-wav.py` — вместо простейшей версии «это один человек».
   Ложная граница удалена, история сохранена в шапке инструмента как урок.
   → **Правило: когда замер даёт «невозможно одинаковый» результат, первая гипотеза — объекты
   ОДИНАКОВЫ, и только вторая — инструмент врёт** (лошади, а не зебры).
2. **Скачал 12 песен вместо голоса Вихрова.** Собрал со страницы ВСЕ ссылки на mp3 подряд, не
   сопоставив их с названиями треков; нужен был один файл «Диктор Вихров Владимир», 0:28.
   Уши потом показали припевы. → **Правило: скачал материал — докажи ушами, ЧТО это, до того как
   называть это эталоном.**
3. **Отдал владельцу файлы 16 кГц моно.** Эти обрезки делались для РАСПОЗНАВАНИЯ (роботу), а не для
   слуха, но остались лежать в `voice\` под именем эталонов. → **Правило: рабочие файлы для машины
   держать ВНЕ `voice\` и подчищать; эталон для уха резать в качестве ИСТОЧНИКА, а не в 16 кГц.**
4. **Искал не там.** Полез в HuggingFace-датасеты и речевые корпуса, тогда как владелец нашёл нужный
   сайт за три минуты обычным поиском «имя + скачать». → **Правило: сначала прямой веб-поиск по
   имени, и только потом корпуса.**

### 🔧 Что из сделанного ЖИВО и полезно (не удалено)

- **`tools/voice/text_norm.py`** — слой нормализации вынесен из `silero_daemon.py` отдельным модулем
  и расширен под ОБЕ жалобы владельца: дроби («11.4» → «одиннадцать целых четыре десятых», раньше
  резалось на два целых) и код (`f(x)` → «эф от икс», `=` → «равно», `-m` → «ключ m», кавычки
  снимаются). Самопроверка внутри модуля: `python tools/voice/text_norm.py` → PASS на трёх случаях,
  инвариант «в речи не осталось ни цифры, ни сокращения». **Работает с ЛЮБЫМ движком.**
- **`tools/voice/f0-of-wav.py`** — F0 произвольного wav (`pitch-check.py` умеет только собственный
  синтез Silero). [TESTED: контроль eugene 97.0/эталон 96, baya 246.2/246; на живых записях
  102 / 124 / 171 / 195 Гц — различает дикторов уверенно.]
- **Оба движка подняты и замерены** (числа ниже) — переустанавливать не нужно.

### ⚖️ ЗАМЕРЫ ДВИЖКОВ на RTX 5070 Ti (получены до удаления аудио, цифры в силе)

| | **ESpeech-TTS-1 RL-V2** | **Qwen3-TTS 1.7B** |
|---|---|---|
| RTF | **0.167–0.197** (быстрее реального времени) | 2.62–3.40 (**медленнее втрое**) |
| пик VRAM | **3.15 ГБ** | 4.44 ГБ |
| ударения | ✅ RUAccent | ❌ нет |
| стриминг | ❌ нет (flow-matching) | ✅ есть по устройству |
| лицензия | Apache-2.0 | Apache-2.0 |

🐞 **Дефект ESpeech, найденный НАШИМИ УШАМИ:** он **вклеивает куски ЭТАЛОНА в синтез**. На тексте
про видеокарту всплыла фраза «словно бы загибал» из референса — дважды. Ассистент произнесёт то,
чего не говорил. Опаснее медленности; вероятное лечение — нарезка на короткие фразы (`bugs/09`),
но это надо ПРОВЕРИТЬ, а не предположить. У Qwen такого не наблюдалось.

⚠️ RTF Qwen — цена ИМЕННО нашей сборки: `flash-attn` не собран, работает запасной путь PyTorch;
вендорские 97 мс относятся к оптимизированной сборке.

### 📦 Что появилось НА ДИСКЕ (вне git, свежая сессия иначе не узнает)

| Путь | Что |
|---|---|
| `/opt/qwentts/tokenizer` (WSL) | 651 МБ, аудио-кодек Qwen3-TTS-Tokenizer-12Hz |
| `/opt/qwentts/customvoice` | **4.3 ГБ, ГОТОВ** — 1.7B с пресет-дикторами |
| `/opt/qwentts/base` | ~3.5/3.8 ГБ — 1.7B для клона голоса |
| `/opt/voicetrain/.venv` | **torch 2.13.0+cu130, CUDA доступна** (RTX 5070 Ti видна из WSL) |
| `/opt/qwentts/*.sh, synth_qwen.py` | цепочка: сторож застоя → синтез → сборка mp3 |
| `/opt/espeech/rl_v2` | **2.6 ГБ, ГОТОВ** — ESpeech-TTS-1 RL-V2 (`espeech_tts_rlv2.pt` + `vocab.txt`) |
| `/opt/espeech/synth_espeech.py` | рабочий прогон ESpeech (без gradio, с нормализацией и RUAccent) |
| `/opt/cosyvoice/weights` | 1.3 ГБ из ~2 — CosyVoice 3 0.5B, **докачан не полностью** |
| в venv стоят | `qwen-tts 0.1.1`, `f5-tts 1.1.22`, `ruaccent 1.5.8.3`, torch 2.13.0+cu130 |

⚠️ **Скрипты синтеза ссылаются на УДАЛЁННЫЕ эталоны** (`voice/refs/cut_rassadin_15s.wav` и
`voice/out/casting/silero_eugene_*`). Перед запуском подставь новый эталон из файла Вихрова —
иначе упадут на отсутствующем файле.

---

## ▶️ ГДЕ ПРОДОЛЖИТЬ — план следующей сессии (по порядку)

1. ~~**Обезопасить исходник Вихрова.**~~ ✅ закрыто `/*.mp3` в `.gitignore:105`.
2. ~~**Нарезать эталоны В КАЧЕСТВЕ ИСТОЧНИКА.**~~ ✅ шесть кусков по 60 с в `voice\refs_vihrov\`,
   `-c copy` (без перекодирования), с текстами и индексом.
3. ⏳ **ЖДЁМ ВЛАДЕЛЬЦА: какой кусок.** `homeworks/03`. Это его прямое требование, а не формальность:
   клонировать без одобренного эталона запрещено. **Пока ответа нет — голосовую ветку не двигать.**
4. **Клонировать одобренный эталон ОБОИМИ движками** на пяти текстах кастинга (тексты — в
   `homeworks/03`, менять нельзя) с обязательным `text_norm.normalize()`, и дать A/B.
   ⚠️ Для ESpeech эталон резать **≤12 с звука + ровно его текст** (`bugs/16`), для Qwen — минута целиком.
   ⚠️ Скрипты в WSL всё ещё ссылаются на удалённый `voice/refs/cut_rassadin_15s.wav` — подставить новый путь.
5. **Проверить `bugs/16` замером** по таблице A/B/C из документа (контроль обязателен). Критерий
   машинный: расшифровать синтез нашими ушами и искать слова эталона, которых нет в задании.
   Не переходить на ESpeech, пока не проверено.
6. **Только после вердикта по звуку** — считать цену: GGUF/ONNX-сборки Qwen на llama.cpp и
   sherpa-onnx (`cstr/qwen3-tts-1.7b-*-GGUF`, `xkos/Qwen3-TTS-12Hz-1.7B-ONNX`).

**Чего НЕ делать:** не предлагать Silero ни в каком виде · не тащить пресет-дикторов Qwen в роль
дворецкого · не резать эталоны короче минуты, если есть выбор · не приносить владельцу ссылку
вместо артефакта (EXP-0031) · не называть материал эталоном, не доказав ушами, что это.

### 🌐 Настоящий блокер вечера оказался НЕ в коде — он в канале

Полтора часа работы съела сеть, и это стоит знать следующей сессии:

- Весь трафик машины идёт через **NordVPN**; на прежнем сервере канал давал **28 КБ/с**
  (7.5 ГБ весов = полтора суток). Владелец переподключился на Кипр — стало **1.2 МБ/с** одним
  потоком, **7.9 МБ/с** через `hf_transfer` (многопоточная качка одного файла).
- ⚠️ **Туннель тихо роняет соединения.** И `pip`, и `hf_transfer` периодически виснут на мёртвых
  сокетах: прирост байт 0, процесс жив, `--timeout` НЕ срабатывает. Лечение — не «ждать дольше»,
  а сторож застоя `/opt/qwentts/watchdog.sh`: замечает нулевой прирост БАЙТ за 70 с и перезапускает.
  Перезапуск почти бесплатен — кеш pip и частичные файлы HF целы.

### Уроки этого окна

- **EXP-кандидат: короткий замер скорости — это замер рукопожатия, а не канала.** Я объявил канал
  мёртвым («1.2 КБ/с») по 10–15-секундным пробам, в которых доминировали TLS и медленный старт.
  Установившаяся скорость была в тысячу раз выше. Мерить пропускную способность — только на
  десятках секунд И по приросту байт на диске, а не по одному `curl`.
- **`pkill -f <шаблон>` убивает СОБСТВЕННУЮ команду запуска**, если её строка содержит этот шаблон
  (получил `Exit code 15` на ровном месте). Гасить фоновое — только из файла-скрипта.
- **Папка есть ≠ веса скачаны.** Первая версия скрипта синтеза проверяла `isdir` и загрузила бы
  пустой каталог `base`. Судить по ГЛАВНОМУ файлу (`model.safetensors`).



### Что решено владельцем (канон, не пересматривать)

| Решение | Дословно |
|---|---|
| Способ произносить английское | **транслитерация кириллицей** — «второй способ самый хороший» |
| Качество Silero | ⛔ **«звучит как дерьмо, 11 лабс гораздо лучше»** |
| Роль агента | **«искать в сети лучшее, быстро макетировать и приносить мне на тесты то, что сам оценить не можешь»** |
| Голоса | ✅ `eugene`, `baya`, `xenia` · ⛔ все 4 Piper, `aidar`, `kseniya` |
| Активаторы | «Джарвис» и «Джои» — **два** |
| Ресурсы | обучение — вся GPU; инференс — лёгкий (⚠️ **конфликтует с требованием качества**, см. ниже) |

### 🔥 ГЛАВНОЕ ПРОТИВОРЕЧИЕ, которое предстоит решить владельцу

Качество уровня ElevenLabs в открытых моделях **существует** (разрыв 0.1–0.3 MOS), но **все такие
модели — GPU**. Это прямо конфликтует с правилом «при инференсе голос должен быть ресурсо лёгким».
Пока конфликт не решён, любой выбор голоса — угадывание. Нужны цифры: VRAM и RTF лучшего кандидата
на нашей карте → и слово владельца.

### Что проверено наблюдением (детали — `researches/15` §7)

- **Silero `multi_v2`** — одна модель, оба языка, РАБОТАЕТ (уши записали «Machine larning»
  латиницей), но потолок **8/16 кГц**, дикторы поколения v2, без `eugene`/`xenia`. По официальному
  `models.yml` это **единственная** мультиязычная модель Silero.
- **Мы крутим устаревшую `v5_ru`**, линейка ушла до **`v5_5_ru`** (8/24/48 кГц) — скачана в
  `voice/models/`, в тракт НЕ подключена.
- **Свежие русские модели латиницу молча ВЫРЕЗАЮТ** — «Эта технология называется, и она уже
  работает» (класс тихой пропажи, как цифры в `bugs/13`).
- **Транслитерация работает**: «мащин лёрнинг» уши распознали обратно как «Machine lurning».
  Инженерная часть — транслитератор на espeak-ng (английский G2P уже лежит внутри моделей Piper).

### ⚠️ Незавершённое — состояние на момент паузы (проверено, не предположено)

| Что | Состояние |
|---|---|
| Корпус текстов для обучения | ✅ **3000 фраз** в WSL `/opt/corpus/sentences.txt` (из наших же zim) |
| PyTorch + CUDA в WSL | ❌ **НЕ встал** — обрыв на `nvidia_cublas` (423 МБ), затем процесс убит при моих манипуляциях. `/opt/voicetrain/.venv` есть, torch нет |
| Веса CosyVoice 3 0.5B | ⚠️ **126 МБ из ~2 ГБ**, процесс не идёт. `/opt/cosyvoice/weights` |
| WSL Ubuntu 26.04 + CUDA | ✅ работает, `nvidia-smi` видит RTX 5070 Ti |

**Как продолжить загрузку** (WSL, от root):
`setsid nohup /opt/cosyvoice/dl.sh > /opt/cosyvoice/dl4.log 2>&1 &`
**torch:** `cd /opt/voicetrain && . .venv/bin/activate && pip install --resume-retries 30 torch torchaudio`

### 🏠 Домашка владельцу — `homeworks/03_voice_quality_audition.md`

Слушать: `voice\out\sravnenie\sravnenie.mp3` (4 способа, 40 с) · `voice\out\casting\` (9 голосов).
Ждёт: голос для Jarvis и Joi + решение по противоречию «качество против ресурсов».

### 🎫 Тикет разработчику KAIF — `researches/17_kaif_tandem_homework_report.md`

Найдена дыра во фреймворке: канон покрывает измеримые критерии (`TESTING_FRAMEWORK`) и развилки
видения (`/interview`), но **молчит про критерий, который агент не может ИЗМЕРИТЬ** (красиво,
естественно, приятно). Из-за этого агент трижды подряд решал сам вместо того, чтобы принести макет.
Предложение: третий класс задач + цикл «найти → макетировать → отдать артефакт → зафиксировать
вердикт» + правило «артефакт, а не ссылка». Урок — EXP-0031.

---

**2026-07-29 (день, автономный цикл 14:10–16:00) — 🔬 ЧЕТЫРЕ ЗАМЕРА: рантайм голоса НАЙДЕН, оба
пути к активатору ОТВЕРГНУТЫ, зрение разведано, баг гейтвея пойман с уликами:**

- **🎁 Находка, которая удешевила половину плана: нужный рантайм УЖЕ СТОЯЛ.** Пакет `sherpa-onnx-node`,
  установленный ради ушей, экспортирует ещё и **`OfflineTts`, `Vad`, `KeywordSpotter`,
  `OfflinePunctuation`, `SpeakerEmbeddingExtractor`, `OfflineSpeakerDiarization`**. Пять секунд на
  `Object.keys` — и четыре предполагаемые зависимости схлопнулись в одну уже имеющуюся (EXP-0026).
- **✅ ГЕЙТ РАНТАЙМА ГОЛОСА ПРОЙДЕН** (`tools/voice/tts-sherpa-probe.mjs`, оба рта на одних фразах в
  одном прогоне): sherpa-onnx с Piper VITS на CPU — **медиана синтеза 0.194 с / RTF 0.058** против
  Silero **0.206 с / 0.045**. Кандидат не хуже, и у него нет пиков Silero (первая фраза 1.088 с,
  латиница 1.938 с из-за ленивой загрузки английской модели). Уши подтвердили разборчивость всех пяти
  фраз. **Установка не потребовалась — нужен был только файл голоса (60 МБ).**
  🎁 **Побочно: Piper читает ЦИФРЫ САМ** (espeak-ng), правильно и с согласованием ⇒ при переходе наш
  самодельный слой нормализации из `bugs/13` становится НЕНУЖНЫМ.
  ⚠️ **Цена названа честно:** один русский голос произносит латиницу русскими фонемами
  («machine learning» → «машин лони») — это вариант B из `bugs/11`, который владелец отверг. Решается
  вторым голосом Piper в том же движке либо обученным клоном.
- **⛔ ОБА пути к готовому активатору проверены и ОТВЕРГНУТЫ** (`researches/16` §3.1–3.2):
  sherpa `KeywordSpotter` (ключевые слова списком, без обучения) — потолок **3/6** при развёртке по
  16 рабочим точкам; готовая `hey_jarvis` из openWakeWord — **0/10**. Поправка: готовая модель
  обучена на ФРАЗЕ «hey jarvis», голого «Джарвис» среди штатных слов нет вовсе.
  **Нулю можно верить, потому что рядом стоял КОНТРОЛЬ:** клип «Alexa.» настоящей английской моделью
  зажёг свой детектор на **1.000**, не тронув чужой ⇒ стенд исправен, а ноль — свойство модели
  (EXP-0027). **Вывод: русские активаторы придётся ОБУЧАТЬ — оба.** Ресурсы владелец уже выдал.
  ⚠️ Грабля: автообучение openWakeWord только на Linux (WSL2/Colab), инференс кроссплатформенный.
- **✅ Просроченный заказ от 28.07 исполнен: `researches/11_agent_vision.md`** (план 17 → **DONE**).
  Зрение — ТРИ слоя (захват → локализация → чтение); вердикт индустрии — **каскад**: UIA-дерево
  первым (0 ГБ VRAM), пиксели закрывают его дыры, VLM редко и осознанно. 🐞 **Найдено слепое пятно
  нашего же инструмента:** `screen-rec.mjs` снимает через `gdigrab`, а он **не видит
  аппаратно-ускоренные окна** — агент честно смотрит на чёрный прямоугольник и домысливает.
  Записано в шапку инструмента.
- **✅ `bugs/12` добит: T1 получил БЕЛЫЙ список.** `runTurn` протягивает `finish_reason` из SSE
  (забирать ОБЯЗАТЕЛЬНО до проверки на контент: финальный чанк несёт причину при ПУСТОЙ дельте —
  проверено наблюдением). Логика вынесена в `tools/voice/turn-verdict.mjs`, охранник **T0d** проверен
  красно-зелёной таблицей: **старая логика 5/8** (молча пропускала `finish_reason` = null / length /
  tool_calls), **новая 8/8**. Чёрный список НЕ убран: `stop` значит «договорила», а не «сказала
  осмысленное». Бенч после правки — 10/10 (`--quick`) и 34/35 (полный).
- **🐞 `bugs/15` ВОСПРОИЗВЁЛСЯ в 14:45:56 — и утренняя ловушка сработала.** Последняя строка нашего
  перехваченного лога: `model-fetch start` к llama-server, **дальше тишина** — ни ответа, ни
  исключения. Проверено: **llama-server жив**, журнал ОС пуст, памяти 8.7 ГБ свободно, stderr пуст,
  `liveness warning` в этот раз НЕ было (утренняя гипотеза как предвестник ослаблена).
  Построен минимальный воспроизводитель **`tools/gateway-churn-probe.mjs`**, и им сняты **ТРИ
  гипотезы подряд**: (1) быстрая смена сессий — 12 новых сессий пережил · (2) полный голосовой прогон
  — пережил, 34/35 · (3) **голодание CPU** (`--load`, все ядра заняты) — пережил **~26 минут
  стопроцентной загрузки и 29 ходов**, все с `finish_reason=stop` (ходы замедлились с 0.14 с до
  14–25 с на запрос к модели, но ни один не сорвался). **Дефект перемежающийся, по требованию не
  воспроизводится**; это сужение поля, а не провал — три правдоподобных подозреваемых сняты замером,
  а не рассуждением. Ловушка стоит, следующее падение снова оставит улику.
  🚫 **Не «чинить» автоперезапуском** — он замаскирует симптом и уничтожит единственный путь к причине.
  ⚠️ **Побочно найдено:** у запроса гейтвея к llama-server стоит **`timeoutMs=undefined`** — таймаута
  НЕТ. Не причина смерти (процесс исчезал, а не висел), но зависший llama-server повесит голосовой
  ход навсегда. Отдельный кандидат в дефекты, не смешивать с `bugs/15`.
- **Уроки:** EXP-0026 (перечисли API уже установленного до выбора зависимости) · EXP-0027
  (отрицательный результат стоит столько, сколько положительный контроль рядом) · EXP-0028 (импорт
  из ИСПОЛНЯЕМОГО скрипта запускает его целиком — логику под проверку выноси в модуль) ·
  **EXP-0029 (время изменения файла НЕ свидетельство о живости процесса: на Windows метка
  активно пишущегося файла отставала на 15 минут, и я объявил владельцу ложное «гейтвей завис» —
  свежесть определяй содержимым, а не метаданными; оговорка внесена в канон)**.
- **📦 Что появилось НА ДИСКЕ вне git** (свежая сессия иначе не узнает; всё под `/voice/` в
  `.gitignore`): `voice\models\vits-piper-ru_RU-ruslan-medium\` (60 МБ, русский голос Piper для
  замера рта) · `voice\models\sherpa-onnx-kws-zipformer-gigaspeech-3.3M-2024-01-01\` (17 МБ, KWS) ·
  `voice\models\silero_vad.onnx` (0.6 МБ, **пригодится** на слое endpointing) · **отдельный venv
  `voice\venv-wakeword\`** с openWakeWord — заведён специально, чтобы НЕ трогать рабочий
  `voice\venv` с Silero и GigaAM. ⚠️ **В манифест деплоя намеренно НЕ вносил:** это материал
  замеров, а не принятые компоненты стека — тот же принцип, по которому туда не попала модель ушей
  `punct` (решение владельца о стеке ещё не принято).

---

**2026-07-29 (день, ~13:00–14:00) — ✅ ИНТЕРВЬЮ 007 ОТВЕЧЕНО: порядок работ по голосу ПЕРЕПИСАН
ответами владельца + разведка клона + гейтвей перестал терять улики:**

- **🗣 Владелец ответил на все четыре развилки** (`interviews/interview_007`, ответы дословно):
  **Q1** — судить будет ХАРНЕСС, не его микрофон («не за компом, нужно через твои тесты») ·
  **Q2** — вариант C, кросс-язычный клон, мандат «ищи, тяни, тестируй»; некоммерческая лицензия
  допускается · **Q3** — **только по слову-активатору** (не постоянное прослушивание) ·
  **Q4** — скорость приемлема, вкладываться в живость.
- **♻️ Следствие: порядок из `researches/14` §5 больше не действует.** Было: AEC → VAD → barge-in.
  **Стало:** ① клон голоса → ② wake word «Джарвис» → ③ VAD+endpointing внутри открытой сессии →
  ④ AEC (нужен, но НЕ блокер №1: непрерывно работает лишь keyword-детектор). **Урезание системного
  промпта ради секунд СНЯТО с повестки** (Q4=B) — 6–9 с сквозного хода теперь НОРМА, а не дефект.
- **🔬 `researches/15_crosslingual_voice_cloning.md` — разведка по заказу Q2.** Три находки, каждая
  меняет план: **(1) XTTS-v2 ПРОТУХШИЙ** — `coqui-ai/TTS` последний push **2024-08-16**, по правилу
  живости владельца не берём; разрешение на некоммерческую лицензию **не понадобилось**, все реальные
  кандидаты под Apache-2.0/MIT. **(2) Chatterbox V3 вычеркнут** — источник, меривший РУССКИЙ, называет
  проблемы с ударениями и интонацией (он стоял в шорт-листе с `researches/09`). **(3) Кандидат №1 —
  Qwen3-TTS 0.6B**: русский и английский В ОДНОЙ модели (снимает `bugs/11` по сути, а не подбором
  диктора), родной стриминг (заявлено 97 мс), Apache-2.0, ~2 ГБ VRAM, клон с 3 секунд.
- **⚠️ Настоящее ограничение — не лицензия и не качество, а ГДЕ ЭТО СЧИТАЕТСЯ.** Наш рот живёт на
  **CPU** (0.08–0.11 с/фраза, первый байт 80–110 мс = ✅-строка паритета, 0 ГБ VRAM). **Все клоны
  требуют GPU**: на CPU Qwen3-TTS даёт RTF 3–5×, F5-TTS/ESpeech — ~2 минуты на фразу. Цена «хорошего
  голоса» = постоянные ~2 ГБ VRAM в конкуренции с основной LLM на 16 ГБ ⇒ **развилка уровня стека,
  вопрос владельцу** (§6 разведки; тот же вопрос открыт по ушам с `researches/13`).
- **🐞 `bugs/15` ЗАВЕДЁН — гейтвей умирает молча. Корень НЕ установлен, наблюдаемость ПОЧИНЕНА.**
  Почему улик не было: `klas.ps1` поднимал гейтвей `-WindowStyle Hidden` **без перехвата вывода** —
  предсмертные слова уходили в закрывшееся окно. Теперь вывод пишется в
  `F:\KLAS\logs\gateway-<метка>.out/err.log` (метка времени обязательна: перенаправление открывает
  файл НА ПЕРЕЗАПИСЬ, без неё «фикс» затирал бы ровно ту улику, ради которой делается).
  **Проверено на живом гейтвее:** порт 18789 LISTENING · `down`-матчер находит процесс · окон 0 ·
  в логе полный старт до `[gateway] ready`. **Попутно найдено главное: собственный лог OpenClaw
  существовал всегда** — в `%TEMP%\openclaw\openclaw-ГГГГ-ММ-ДД.log` (574 КБ за сегодня), а не в
  `~/.openclaw/logs\`, где его искали. Формат — **JSONL**, поэтому греп по `fatal|error` даёт ноль:
  разбирать надо как JSON по `subsystem`. Читать только с `-Encoding UTF8` (иначе моджибейк, `bugs/08`).
- **🔬 `researches/16_wake_word.md` — разведка по ответу Q3, и она удешевила эпик.** У **openWakeWord
  уже есть ГОТОВАЯ предобученная модель `jarvis`** (одно из шести штатных слов) — имя персоны из
  идеи 05 совпало, обучать своё слово пока не нужно. Заявлено 97%+ срабатываний при <1 ложном в час;
  модель ~200 КБ, инференс 5–15 мс на CPU, GPU не трогает. **Открытый вопрос решается замером нашим
  же ртом:** сработает ли английская модель на русском произношении «Джа́рвис» — синтез Silero →
  детектор → доля срабатываний (приём из `bugs/11`: выбирать измерением). Porcupine отвергнут не по
  качеству, а потому что требует **AccessKey от облака Picovoice** — плохой размен для системы,
  чей принцип приватность и офлайн; оставлен запасным. ⚠️ Честно: openWakeWord 7 месяцев без push
  (не архив, но жёлтый флаг по правилу живости); автообучение только на Linux, инференс — везде.
- **⚡ ВЛАДЕЛЕЦ ОТВЕТИЛ В ТОТ ЖЕ ДЕНЬ — и первый ответ ПЕРЕПИСАЛ решение по голосу:**
  **(а) Ресурсы:** *«обучение — хоть всю видюху на сколько хочешь часов; инференс при работе KLAS —
  НЕТ, голос должен быть ресурсо лёгким»*. ⇒ **Архитектура: тяжёлое ОДИН РАЗ офлайн на GPU → лёгкое
  КАЖДЫЙ РАЗ на CPU.** Qwen3-TTS выбывает из роли рта и переезжает в ОБУЧАЮЩИЙ конвейер (генератор
  корпуса). **Рантаймом становится `sherpa-onnx` — тот самый, на котором УЖЕ работают наши уши**
  (13 843★, push сегодня, Apache-2.0): он умеет TTS семейств VITS/Piper на CPU, для реального
  времени источник рекомендует именно Piper VITS. **Новых сущностей в рантайме ноль.** Голос — свой,
  обученный Piper VITS (инструмент `OHF-Voice/piper1-gpl`, GPL, применяется ТОЛЬКО офлайн и в
  поставку не идёт; ⚠️ `rhasspy/piper` **АРХИВИРОВАН**). Один диктор = один `.onnx` ⇒ Jarvis и Joi —
  два файла, а не два движка. ⚠️ **Первый шаг — самый дешёвый и может отменить всё:** поднять TTS
  sherpa-onnx на готовом русском голосе и сравнить RTF/качество с Silero.
  **(б) Голоса:** *«Про голоса я говорил, какие хочу»* — и это правда: `ideas/05_Jarvis.md` называет
  **Владимира Вихрова** (Jarvis) и девичий/бархатный тембр с референс-пулом дубляжа Аны де Армас
  (Joi). Вопрос закрыт документом; открыты только **🏠 сэмплы** (домашка интервью 004 Q3).
  **(в) Активаторы — ДВА: «Джарвис» и «Джои»**, по одному на персону. У `jarvis` модель готова, для
  «Джои» нужно обучить свою — что теперь разрешено пунктом (а). Бонус: слово активации выбирает и
  голос, и промпт персоны; конвейер обучения активатора использует **тот же Piper**, что и голос.
- **⚠️ Урок сессии (EXP-0025):** ограничение «обучение тяжёлое, инференс лёгкий» **уже стояло в
  `ideas/05_Jarvis.md`** месяцем раньше — я читал документ ради имени персоны и прошёл мимо
  архитектурной строки, потратив разведку на кандидата, который её нарушает. Перед разведкой домена
  выписывай из документов владельца НЕФУНКЦИОНАЛЬНЫЕ ограничения отдельным списком.
- **🏠 Осталось от владельца:** сэмплы голосов Вихрова и кандидаток Joi · «Джои» или «Джой»
  (от написания зависит обучающий корпус) · что делает система, услышав имя, пока говорит сама.
- **Не сделано за окно и передано дальше:** скачивание и живой A/B клонов ·
  фикстура НАСТОЯЩЕЙ речи для честного A/B ушей (⚠️ проверено: записей голоса владельца на диске
  **не осталось**, синтезом судить нельзя — EXP-0016) · разведка зрения (`plans/17`) · `bugs/07`.
  🚫 **Намеренно НЕ трогал venv `F:\KLAS\voice\venv`** (шаг 1 плана активатора — поставить
  `openwakeword`): установка пакетов рядом с рабочими Silero и GigaAM может уронить голосовой тракт,
  а владельца нет за компом. Ставить в начале следующей сессии, с прогоном `voice-bench` после.

---

**2026-07-29 (утро, блок 2) — 🗣 ГОЛОСОВАЯ ВОЛНА: 6 документов багов, 4 закрыто, 4 новых охранника,
2 карты разведки. Владелец назвал ДВА дефекта — охранники нашли ещё ТРИ:**

- **✅ `bugs/11` ЗАКРЫТ — голос больше не меняется посреди реплики.** Жалоба: «русский мужской, а
  английское слово — женский британский; нужно ОДИН голос». Корень: рот собирает реплику из ДВУХ
  моделей Silero, а английский дефолт `en_0` был взят агентом «первым в списке» и оказался женским
  (Silero не сообщает пол дикторов `v3_en`). **Голос выбран ИЗМЕРЕНИЕМ**, а не на слух: у `eugene` и
  всех 118 английских дикторов замерены F0 и спектральный центроид → **`en_23`: F0 93.8 Гц, ровно как
  у `eugene`** (прежний `en_0` — 202.5 Гц). Новый инструмент харнесса **`tools/voice/pitch-check.py`**
  (`--match` — перебор дикторов; без флага — охранник единства регистра, FAIL при расхождении >25%);
  проверен на сломанной версии: `en_0` → FAIL 115.8%, `en_23` → PASS 0.1%. Флаг `--voice-en` даёт
  владельцу сравнить кандидатов ушами (`en_23` · `en_46` · `en_112`) без правки кода.
- **🟡 `bugs/10` — уши не умеют латиницу и пунктуацию. Лечение НАЙДЕНО, развёрнуто ОПЦИЕЙ, ждёт
  вердикта владельца на живом микрофоне.** Дефект доказан алфавитом: `tokens.txt` нынешней модели —
  **34 токена**, ни одной латинской буквы, ни одного знака препинания, даже «ё» нет. Это предел
  модели, а не поломка. **Разведка `researches/13_multilingual_stt.md`** (правило владельца: гуглёж до
  кода) нашла независимое подтверждение симптома (Хабр 2026: у GigaAM v3 «Gemini превращается в
  „Jemni“») и — главное — **что менять стек НЕ нужно**: у того же GigaAM-v3 от того же автора есть
  выпуск **`…-ctc-punct-…`** в том же формате NeMo-CTC → **257 токенов: 54 с латиницей, 78 с
  пунктуацией, заглавные, «ё»**. Развёрнут в `voice\models\gigaam-v3-ctc-punct\`, включается
  `node tools/voice-talk.mjs --ears punct`. **Замер A/B честный и не единогласный:** punct закрывает
  обе жалобы (`Привет, Николай. Как дела?` · `…называется Machine Lirning, и она…`), но на плотных
  ИТ-терминах валятся обе модели, а латиница выходит с опечатками. Фикстура была СИНТЕЗИРОВАНА —
  судить должен микрофон владельца. **Дефолт намеренно НЕ переключён:** модель ушей — элемент стека.
- **✅ `bugs/09` ЗАКРЫТ — стриминг больше не глохнет.** Нарезка брала САМУЮ РАННЮЮ границу
  предложения и выходила из цикла, если кусок короче порога: на ответе «Привет! У меня пока нет
  имени…» границей навсегда оставался «Привет!» (8 символов < 12), и весь ответ уезжал ОДНИМ куском.
  Фикс — `firstSentenceCut()` ищет первую ПОДХОДЯЩУЮ границу. Охранник **T0** в бенче проверяет
  нарезку детерминированно, на фикстуре строк, ДО обращения к ядру (прежний бенч этот класс поймать
  не мог: он зависел от того, что сегодня ответит модель). Проверен на старой логике: FAIL 2 против
  PASS. Наблюдение: «Привет! Как тебя зовут?» → было `1/1`, стало **`3/3`**, TTFA **6.2 с**.
  ⚠️ Попутный урок: первая версия фикстуры была НЕВЕРНОЙ (ждал 3 куска там, где контракт даёт 2) —
  охранник краснел бы на исправном коде. Поймано тем, что его прогнали на ОБЕИХ версиях до того,
  как ему поверили.

- **🏁 ВПЕРВЫЕ бенч прогнан В ОКРУЖЕНИИ ВЛАДЕЛЬЦА (без `PYTHONIOENCODING`) — 35/35.** Это и было
  лечение EXP-0018: до сегодня харнесс жил в привилегированной среде и потому был слеп.
  Все три сегодняшних фикса подтверждены на боевом конвейере; единицы звучат словами
  («около двадцати двух градусов Цельсия», «сорока пяти миллиметров»), смесь языков работает.
- **🐞 …и тот же прогон вскрыл `bugs/12`: одна из 35 галочек была ЛОЖНОЙ.** Кейс «единицы» получил
  **5/5** при ответе ядра `No response from OpenClaw.` — ход провалился, а бенч поставил полный балл:
  T1 судил по непустоте строки, T4 честно сверил звук с этой же заглушкой, T5 нашёл в ней кириллицу.
  **Весь набор проверок оставался зелёным на провалившемся ходе** — и это в инструменте, который
  существует ИМЕННО чтобы не отдавать владельцу сырое. Фикс: T1 отвергает строки-заглушки ядра
  (детектор проверен с обеих сторон: ловит 4 формы отказа, не трогает настоящие ответы).
  ⚠️ Честная граница: это ЧЁРНЫЙ список; правильное лечение — белый (`finish_reason: stop` из SSE),
  оно записано в документе бага как следующий шаг. **Перепрогон после фикса: 35/35, балл честный** —
  кейс «единицы» получил настоящий ответ. `voice-say.mjs` (путь TTS-провайдера ядра) отдельно
  проверен на регрессию после правки сайдкара — чист.
- **🐞 Смежная находка (НЕ чинена намеренно):** фикстура кейса «единицы» спрашивает про RTX 5070 Ti,
  а модель отвечает, что такой видеокарты не существует, — вопрос зависит от ЗНАНИЙ модели, а не от
  разворота единиц, который обязан проверять (класс EXP-0013). Требование владельца при этом покрыто
  кейсом «длинный ответ» («двадцати двух градусов Цельсия», «сорока пяти миллиметров»). Правильно —
  проверять разворот ДЕТЕРМИНИРОВАННО на уровне сайдкара. Не правил на финише сессии: правка теста
  без перепрогона и есть то непроверенное изменение, за которым охотится `/fable-judge`.
- **🚨 `bugs/13` ЗАКРЫТ — РОТ МОЛЧА ГЛОТАЛ ЦИФРЫ. Самая тяжёлая находка дня.** «Температура 20
  градусов» звучало как «температура градусов»: Silero **не читает цифры вообще** — ни русская
  модель, ни английская. Доказано ДЛИТЕЛЬНОСТЬЮ (уши GigaAM не умеют печатать цифры, по их выводу
  обвинять рот нельзя — урок `bugs/08`): «115 килобайт в секунду» = **1.09 с**, «килобайт в секунду»
  без числа = **1.09 с**, «сто пятнадцать килобайт в секунду» = 1.74 с. Число давало **ноль звука**.
  Дефект был замаскирован тремя слоями: `VOICE_STYLE` просит модель писать числа словами (она
  обычно слушается) · подстраховка срабатывала только на безбуквенном тексте · **порог бенча 0.6
  прощал пропажу одного токена из четырёх**. Фикс: разворот цифр в слова у ЛЮБОГО отрезка, плюс
  написан `number_to_en_words` для латиницы. Охранник **T0b** (инвариант «в речи не осталось ни одной
  цифры») проверен на сломанной версии: **FAIL 0/6 → PASS 6/6**. Известные границы записаны честно:
  падежи («два терабайт») и дробные числа не согласуются — это морфология, то есть новая зависимость
  ради косметики, а число теперь ЗВУЧИТ.
- **🐞 `bugs/14` ЗАКРЫТ — разметка TTS-директивы ядра уходила В РЕЧЬ.** Ядро вернуло реплику
  обёрнутой в собственную директиву синтеза `[[tts(text="…")]]` как обычный текст; очистка markdown
  скобки не покрывает, а `split_by_script` отправляет латинский кусок `tts text` в английскую модель —
  владелец услышал бы «ти-ти-эс текст» перед ответом. **И снова все пять проверок остались зелёными:**
  T4 сверяет звук с тем, что МЫ синтезировали, а не с тем, что СЛЕДОВАЛО сказать (тот же слепой угол,
  что в `bugs/12`). Чистка сделана на НАШЕЙ стороне (конфиг ядра — стек владельца, там директива может
  быть нужна другим каналам). Охранник **T0c** проверен в ОБЕ стороны: снимает разметку (в том числе
  разорванную стримингом между фразами) и **не трогает** законный текст со скобками — `f(x) = 2x при
  x [1..5]` остаётся как есть.
- **🔧 Закрыта дыра в САМОРАСКРЫТИИ (идея 03).** Манифест `tools/deploy.manifest.json` не знал об
  английской модели `v3_en.pt`: на чистой машине `clone + deploy` поставил бы venv, русский Silero и
  уши, но двуязычная речь приехала бы СЛОМАННОЙ — латинские куски не синтезировались бы. Добавлено;
  в комментарии зафиксирована грабля (у `models.silero.ai` истёк SSL: **Python цепочку не принимает,
  Node и PowerShell принимают** — качать только деплоем). Модель `punct` намеренно НЕ в манифесте:
  это опция, ждущая вердикта владельца, а 224 МБ в каждую установку ради недефолтной модели — против
  «минимума сущностей»; готовые URL лежат в `$todo`.
- **⚠️ Гейтвей OpenClaw был найден упавшим** (llama-swap жив, LLM 200, а `18789` не отвечал) и поднят
  канонически `node openclaw.mjs gateway run` — иначе `klas.ps1 down` не нашёл бы его. Причина падения
  не установлена: процесс исчез между прогонами. **Кандидат в наблюдение для следующей сессии.**
- **📚 `researches/14_voice_gold_standard.md` — КАРТА ПОЛЯ (заказ владельца).** Тракт до сих пор
  строился по дефектам; карты золотых стандартов не было. Собрано: вердикт индустрии 2026
  (**производственный выбор — каскад**, то есть наш путь верен; full-duplex Moshi ~200 мс — горизонт,
  не сегодня) · turn-taking как **ТРИ слоя** (акустический VAD → endpointing 800–1200 мс →
  семантический детектор конца хода) · бюджет **barge-in <150 мс** (flush TTS <60, отмена LLM <40,
  ложные перебивания <2%) · **AEC обязателен ПЕРЕД VAD** · постадийный бюджет латентности.
  **Главное — §4 инвентарь паритета (17 строк): 7 ✅ · 2 🟡 · 8 ❌.** Наш рот уже в норме эталона
  (первый байт 80–110 мс при норме 75–200), контроль по звуку — выше типовой практики; но нет НИ
  ОДНОГО слоя живого разговора, а **AEC — порядковый блокер** (без него VAD услышит голос самого
  ассистента). Предложенный порядок: AEC → Silero VAD + endpointing → barge-in; переписывать тракт
  на Pipecat/LiveKit НЕ предлагается (взять из них идеи и по одной OSS-детали). 4 вопроса владельцу.

---

**2026-07-29 (утро) — 🔴→✅ БОРМОТАНИЕ ГОЛОСА ПОБЕЖДЕНО: причина доказана ПОБАЙТОВО, и она была
в ОКРУЖЕНИИ — ровно там, куда указывал владелец:**

- **Владелец был прав, агент — нет.** Он написал: *«Я ДУМАЮ, ЧТО ЧТО-ТО В МОЁМ ОКРУЖЕНИИ!! РАЗ АГЕНТ
  НЕ МОЖЕТ ВОСПРОИЗВЕСТИ ЭТОТ БАГ!»* — и это был верный след. Диагноз прошлой сессии («две голосовые
  сессии говорят в одни колонки», `plans/13` «Инцидент») **оказался неверным**: замок полезен, но
  дефект был другой и остался жив.
- **🐞 `bugs/08` ЗАКРЫТ: моджибейк UTF-8 → cp1251 на стыке Node ↔ Python.** Node пишет в сайдкар
  UTF-8, а питон на Windows открывает stdin-**пайп** в кодировке ЛОКАЛИ (ANSI этой машины = **1251**).
  «Привет, Николай.» приезжает в Silero как `РџСЂРёРІРµС‚, РќРёРєРѕР»Р°Р№.` — и рот честно проговаривает
  «Р-џ-С-Ђ…». Коварство: моджибейк состоит из **настоящих кириллических букв**, поэтому проходит
  охранник `bugs/06` насквозь, не даёт ни исключения, ни лога — **дефект только ЗВУЧИТ**.
- **📏 Доказано побайтово, а не «похоже».** Улики нашлись на диске (`voice-talk` не удаляет wav
  ответов): в файлах сессии владельца от 0:22 уши KLAS услышали «сверс валера» и «разраз р р р раз».
  Точный текст того хода взят из лога ядра; синтез моджибейк-версии дал **те же sha1 у обоих файлов**.
- **✅ Лечение — контракт в самом сайдкаре, а не переменная у вызывающего:**
  `sys.std*.reconfigure(encoding="utf-8", errors="strict")` в `silero_daemon.py` и `silero_say.py`.
  Теперь тракт не зависит ни от локали машины, ни от того, кто и откуда его запустил.
- **🐤 Охранник — канарейка кодировки.** `TtsDaemon.ready()` гоняет `Проверка кодировки — ёжик, 42.`
  Node → питон → Node и сверяет побуквенно; новый статус **`encoding-broken`** заставляет `voice-talk`
  и `voice-bench` ОТКАЗАТЬСЯ стартовать. Проверен на сломанной версии: краснеет, печатая
  `enc.in = cp1251` и сам моджибейк.
- **🚨 Главный урок процесса (EXP-0018): харнесс жил в ПРИВИЛЕГИРОВАННОМ окружении.** Claude Code
  задаёт своим процессам `PYTHONIOENCODING=utf-8:surrogateescape`; у владельца её нет (пуста и в
  User-, и в Machine-области). Эта переменная молча лечила тракт в каждом прогоне агента — отсюда
  «35/35» при бормотании у человека. **Зелёный бенч не гарантировал ничего.** Опыт: EXP-0018 (разница
  окружений) · EXP-0019 (порча, которая только звучит → ищи артефакт на диске и доказывай байтами).
- **🐞 Попутно найден `bugs/09` (OPEN, не чинён):** нарезка ответа по предложениям глохнет навсегда,
  если ответ начинается с КОРОТКОГО предложения (`buf.match` возвращает первое совпадение, порог
  `MIN_CHUNK_CHARS` его не пропускает → `break` на каждой итерации). Звук не портит, но обнуляет
  стриминг: весь ответ уходит одним куском. Чинить с охранником-случаем в бенче.
- **Проверено наблюдением в окружении ВЛАДЕЛЬЦА** (`PYTHONIOENCODING` снята принудительно): канарейка
  зелёная (`enc utf-8/utf-8`), синтез «Привет, Николай.» sha1 = эталон, сквозной `voice-talk --text` —
  2/2 фразы, уши слышат верные слова.

---

## Где продолжить в следующей сессии

> Сессия 2026-07-28: живой UI-тест (plans/16 фаза 4) → корень дефекта тул-коллов найден в ДВИЖКЕ
> (b9538 старше апстрим-фикса #24807) → обновление на b10167 откачено как ломающее (`bugs/05`) →
> ретрай моделей починен (не передавать `--model`) → документы приведены к правде.

### 🤝 ПЕРЕДАЧА ЭСТАФЕТЫ (сессия 2026-07-29, утро — закрыта в 11:00 по слову владельца)

**Что произошло за сессию.** Владелец вернулся с живым микрофоном и принёс три дефекта. Разбор каждого
из них рождал следующий: итог — **шесть документов багов, четыре закрытых, два новых охранника в бенче
и две карты разведки**.

| # | Дефект | Состояние |
|---|---|---|
| `bugs/08` | голос бормотал (моджибейк UTF-8→cp1251) | ✅ **ЗАКРЫТ**, подтверждён владельцем: «заработало» |
| `bugs/11` | голос менялся посреди реплики (муж→жен) | ✅ **ЗАКРЫТ**, ждёт вкусового вердикта |
| `bugs/09` | стриминг глох на коротком первом предложении | ✅ **ЗАКРЫТ** (1/1 → 3/3 фраз, TTFA 6.2 с) |
| `bugs/10` | уши без латиницы и пунктуации | 🟡 лечение развёрнуто **опцией** `--ears punct`, дефолт не тронут |
| `bugs/12` | бенч ставил 5/5 провалившемуся ходу ядра | ✅ **ЗАКРЫТ** (чёрный список заглушек); белый список — записанный следующий шаг |
| `bugs/13` | **рот молча глотал ЦИФРЫ** («20 градусов» → «градусов») | ✅ **ЗАКРЫТ** на обоих языках, охранник T0b проверен на сломанной версии |
| `bugs/14` | разметка TTS-директивы ядра уходила В РЕЧЬ | ✅ **ЗАКРЫТ**, охранник T0c проверен в обе стороны |

**Цепочка находок дня стоит того, чтобы её заметить:** жалоба владельца на бормотание → `bugs/08`
(кодировка) → первый прогон бенча в ЕГО окружении → `bugs/12` (бенч врал зелёным) → «следующий шаг»,
записанный в `bugs/12` (детерминированная проверка нормализации) → **`bugs/13`: рот молча глотал
числа** → прогон, подтвердивший 13, вскрыл **`bugs/14`: разметка ядра уходила в речь**. Ни один из
трёх последних дефектов владелец не называл — их нашли ОХРАННИКИ, поставленные после предыдущего.
Заплатка закрывает симптом; охранник открывает следующий дефект.

**Общий корень трёх последних — один, и его стоит держать в голове:** T4 сравнивает звук с тем, что
МЫ синтезировали, а не с тем, что СЛЕДОВАЛО сказать. Пока эталон сравнения — собственный вход тракта,
любой мусор, честно доехавший до динамиков, засчитывается как успех. Это неустранимо проверками
«тракт верен самому себе» и лечится только детерминированными охранниками на КОНТРАКТ (T0/T0b/T0c) —
плюс ушами владельца. Поэтому **T1 дополнительно валит ход, если в ответе ядра осталась ЛЮБАЯ машинная
разметка** (`[[`/`]]`, `<tool_call>`, `<|…|>`, JSON в начале) — T0c чинит известную форму, T1 кричит
о неизвестной. Проверено в обе стороны: ловит 4 формы, пропускает живую речь, включая
`Функция f(x) = 2x при x [1..5]` и `Эта технология называется machine learning.`
**Детектор сработал сразу:** бенч после его установки дал **34/35**, поймав живую утечку на кейсе
«единицы» — том самом, который до этого получал 5/5. Улика дословно: `[[tts(text="У моей текущей
модели нет точных данных по спецификациям видеокарты RTX пятьсот семьдесят ти…")]]`.
⚠️ **Утечка ПЕРЕМЕЖАЮЩАЯСЯ** — 2 полных прогона из 3, всегда на «единицах»; тот же вопрос в другом
прогоне приходит чистым. Поэтому: «не воспроизвелось один раз» ≠ «дефекта нет», а **34/35 с красным
T1 на разметке — это ПРАВДА О ЯДРЕ, а не регрессия тракта** (звук чист, разметка снимается).
🚫 **Следующей сессии: не ослаблять охранник ради возврата к 35/35** — он ровно для этого и поставлен.
Разделение ролей: `reply` (чистый) идёт человеку в чат, `rawReply` (сырой) — охранникам, иначе
детектор ослеп бы на собственной чистке.

**Артефакты знания:** `researches/13` (уши и языки) · `researches/14` (**карта золотых стандартов +
инвентарь паритета 17 строк**) · EXP-0018…EXP-0022.

**⚠️ Наблюдение без объяснения:** гейтвей OpenClaw за сессию **упал сам** (llama-swap жив, LLM 200,
порт 18789 молчит, процесса нет). Поднят канонически `node openclaw.mjs gateway run`. Причина не
установлена — если повторится, это отдельный баг: голосовой тракт без гейтвея не работает вовсе.

### 🔥 Первое дело следующей сессии — ❓ **`interviews/interview_007_voice_next_steps.md`**

Развилки оформлены интервью (A/B/C с рекомендацией первым пунктом) и **ждут ответа владельца**:
**Q1** менять ли модель ушей на `punct` · **Q2** чем платить за «ОДИН голос» · **Q3** убирать ли Enter
из разговора (это про ПРИВАТНОСТЬ: VAD = микрофон слушает постоянно) · **Q4** какая задержка означает
«живой разговор». В интервью же таблица «что агент сделает по каждому ответу», чтобы решение не зависло.

Три вердикта, которые может дать только он, и без которых следующий шаг — угадывание:

1. **Уши `punct`** — `node tools/voice-talk.mjs --ears punct` своим микрофоном. Вопрос: стоят ли
   запятые и латиница той цены, что покажет живая речь? Замер на синтетике — в `bugs/10`, он
   НЕ единогласен. Переключать дефолт только по его слову (модель ушей = элемент стека).
2. **Английский голос** — `--voice-en en_23` (сейчас) против `en_46` / `en_112`. И более крупная
   развилка из `bugs/11`: вариант B (транслитерация → буквально ОДИН голос, но русский акцент) и
   вариант C (кросс-язычный клон, эпик). Это выбор ГОЛОСА ассистента ⇒ `/interview`.
3. **Три вопроса из `researches/13`** (чем платим за латиницу; можно ли занимать GPU под уши;
   насколько священно качество чистого русского) **и четыре из `researches/14`** — главный:
   **убирать ли Enter из разговора?** VAD + endpointing делают диалог живым, но означают, что
   микрофон слушает ПОСТОЯННО; для системы, чей принцип — приватность, это решение владельца.

### 🧭 Куда двигать тракт дальше (порядок выведен инвентарём паритета, `researches/14` §4–5)

Не по вкусу, а по зависимостям: **AEC → Silero VAD + endpointing (800–1200 мс) → barge-in (<150 мс)**.
AEC первым, потому что без эхоподавления VAD слышит голос самого ассистента из динамиков владельца и
принимает его за перебивание — следующие два слоя без него не заработают в принципе, сколько их ни
пиши. Переписывать тракт на Pipecat/LiveKit НЕ предлагается: взять из них идеи и по одной OSS-детали.
Резерв латентности — префилл 20-тысячного системного промпта (строка 4 инвентаря), а НЕ «модель
поменьше»: это подтверждено замерами дважды.

### ⚠️ Что следующий агент обязан знать, чтобы не повторить провал этой сессии

- **Терминал агента ≠ терминал владельца.** Claude Code задаёт своим процессам
  `PYTHONIOENCODING=utf-8:surrogateescape`; у владельца её нет. Месяц эта переменная молча лечила
  голосовой тракт, бенч показывал 35/35, а человек слышал кашу. **Любой голосовой фикс проверяй
  прогоном со снятой переменной** (`Remove-Item Env:PYTHONIOENCODING`). Уроки — EXP-0018, EXP-0019.
- **Дефект, который только ЗВУЧИТ, не ищется чтением кода.** Ищи артефакт, который система уже
  сохранила на диск (`voice\out\*.wav` переживают сессию), слушай его ушами KLAS и доказывай причину
  побайтовым воспроизведением, а не похожестью симптома.
- **Прошлый диагноз может быть неверен.** Вечер 28-го списал бормотание на «две сессии в одни
  колонки» и закрыл симптом замком — настоящий дефект остался жив, и владелец услышал его снова.
  Замок при этом полезен; неверен был вывод, что он всё вылечил.

### 🧰 Новое в харнессе за сессию

- `python tools/voice/pitch-check.py [--match]` — охранник единства голоса (F0 обоих языков).
- `node tools/voice-hear.mjs <wav> --model ru|punct` — уши переключаемы между двумя алфавитами.
- `node tools/voice-talk.mjs --ears punct --voice-en en_46` — живой диалог с любой комбинацией.
- Канарейка кодировки в `TtsDaemon.ready()` → статус `encoding-broken` (`voice-talk` и `voice-bench`
  на нём отказываются стартовать).
- **Четыре новых охранника в бенче:** `T0` нарезка (`bugs/09`) · `T0b` нормализация — «в речи не
  осталось ни одной цифры» (`bugs/13`) · `T0c` очистка разметки ядра (`bugs/14`) · строки-заглушки
  ядра в `T1` (`bugs/12`). Первые три детерминированны (фикстуры — строки) и работают ДО обращения к
  ядру, поэтому не зависят от того, что сегодня ответит модель. **Все проверены на сломанных версиях**
  — ни один не «зелёный с рождения».

**Метод, который окупился трижды за сессию и стоит перенимать:** когда прибор не умеет выразить то,
что ищешь (уши не печатают цифры; каша на слух неотличима от поломки), — ищи величину, которую он
выразить МОЖЕТ: длительность звука, sha1 файла, счётчик фраз, F0. Три из четырёх дефектов дня доказаны
именно так, а не рассуждением о коде.

**Состояние тракта на конец сессии (прогон в окружении владельца, `PYTHONIOENCODING` снята):**
`voice-bench` → **35/35 (100%)**, все три детерминированных охранника зелёные
(`T0` 4/4 · `T0b` 6/6 · `T0c` 4/4); `voice-roundtrip` → **97% (59/61)**, ровно исторический уровень.
TTFA тёплых ходов **9.6–15.1 с**, первый ход НОВОЙ сессии — **49.7 с** (префилл 20k промпта; это
известная норма, сессию дня не пересоздавать). ⚠️ Числа выше, чем 8.1–8.5 с в дневных прогонах:
гейтвей за сессию перезапускался, сессии холодные. Резерв назван в `researches/14` §5 — префилл, а не
размер модели.

**Что владелец получил на руки, кроме фиксов:** `interviews/interview_007` (четыре развилки с
рекомендациями и таблицей «что агент сделает по каждому ответу») · `researches/14` (карта поля и
порядок работ по зависимостям) · обе карты проекта, впервые знающие о голосовом тракте · поправки к
`bugs/06` и `plans/13`, где прошлые формулировки оказались шире правды.

> ⚠️ **ПРАВИЛО, ОПЛАЧЕННОЕ ВЕЧЕРОМ 28.07:** перед тем как отдать тракт владельцу — прогнать
> `node tools/voice-bench.mjs` (норма 35/35). Метрика рядом с артефактом не заменяет проверки
> артефакта. И НИКОГДА не гонять звуковые тесты (`--audible`/`--acoustic`), пока человек может быть
> в колонках, — замок теперь не даст, но лучше не подходить к границе.

1. **🏠 Живой микрофонный прогон:** `node tools/voice-talk.mjs` (гейтвей должен быть поднят —
   `powershell -File tools\klas.ps1 -Action up`). Впечатление → `plans/13`. Что спросить владельца:
   (а) достаточно ли быстро (критерий «≤8 с TTFA» придуман агентом из возможностей железа, а не задан
   им); (б) **как звучит английский** — это единственное, что бенч проверить не может, уши GigaAM
   русские. Если медленно — следующий шаг назван и оценён: **урезать 20-тысячный системный промпт для
   голосовых сессий** (остаток ~5 с ядра — это его префилл), затем нарезка по запятым с упреждением
   25 символов.
2. **Атрибуция регресса ядра (10.4 с против ~7 с базы 07-17)** — стендом, без риска для боевого стека:
   `node tools/llamacpp-probe.mjs b9538` против `b10167` на ОДНОЙ модели. Разделит «цену нового билда»
   и «цену перехода на qwen3.6». ⚠️ Замеры латентности теперь берутся с резидентного тракта — старые
   числа с ним не сравнимы напрямую.
3. ✅ **`bugs/06` закрыт** (двуязычность сделана 29.07) — пункт снят.
4. **`bugs/07` — воспроизвести искажение `Type` изолированно** (20–50 прогонов одной строкой, без
   ядра) и с цифрой идти в апстрим-тикет `CursorTouch/Windows-MCP`.
5. **Проверить наблюдением риск одобрений MCP в голосовом канале** (`researches/12` §5 «б»): через
   гейтвей действия инструментов могут требовать подтверждения оператора, чего `--local` не спрашивал.
   В прогонах этой сессии тул-коллов не было — риск НЕ проверен.

> Нормы, закреплённые прогоном 28.07 (соблюдать в живых UI-тестах): запись экрана обязательна; текст
> читать `--crop` в нативном масштабе, а спорную букву — вообще спрашивать у ОС строкой, не глазами
> (EXP-0010); фикстура несёт уникальный токен прогона, иначе тест проходит на остатках (EXP-0012);
> непривилегированный прогон — `runas /trustlevel:0x20000` (EXP-0011).

### Дальше по ценности

- **🏠 Обратная связь владельца по голосу (домашка 02) разнесена ЧАСТИЧНО** (2026-07-28, ночь):
  ✅ **«всё медленно»** — вылечено резидентной архитектурой (24.5 → 7.6–8.0 с, `researches/12`);
  ✅ **голос дефолтом `eugene`** — вписан в `voice-talk` (раньше код молча брал `aidar`);
  ❌ **«голоса уёбищные»** — ждёт Г5 (клоны, `plans/15`, нужны 🏠 сэмплы);
  ✅ **«TTS не произносит английские слова»** — двуязычность сделана (`bugs/06`), голос выровнен по
  регистру (`bugs/11`);
  🟡 **«STT пишет английское кириллицей»** — лечение найдено и развёрнуто опцией `--ears punct`
  (`bugs/10`), **ждёт вердикта владельца на живом микрофоне**.
- **Беспроводная нода** (видение владельца — основной канал): разрулить NordVPN ↔ Tailscale, вернуть
  `gateway.bind=tailnet` + `openclaw gateway --tailscale serve` (wss), перепейринг. Сейчас нода живёт
  только на USB-кабеле.
- **Ротация токена гейтвея** (светился в локальных логах).
- **Поиск свежих моделей** (идея 06) — просрочен с 2026-07-17. ✅ Теперь можно: движок чинён, тул-коллы
  парсятся, сравнение моделей по цепочкам снова достоверно (и цифры на b10167 будут честнее прежних).
- **🔬 Зрение Jarvis/Joi — задание на разведку (`plans/17_jarvis_vision_research.md`, заказ владельца
  2026-07-28):** гуглёж «быстрое и надёжное зрение экрана» (OpenCV и другие подходы) → документ
  `researches/11_agent_vision.md` → только потом код. Первый потребитель — план 16.

1. **Голосовой тракт — фазы Г1→Г5** (зонтик `plans/07`): **Г1 🟡 · Г2 ✅ · Г3 🟡 — KLAS ГОВОРИТ,
   СЛЫШИТ И ВЕДЁТ ДИАЛОГ** (2026-07-17, всё локально/офлайн): Silero v5 (RTF 0.19) + GigaAM-v3
   (RTF 0.032, round-trip 97%) + каскад `voice-talk` с ядром OpenClaw — тёплый ход 20–22 с,
   контекст сессии держится. 🏠 Владельцу: живой микрофон (`node tools/voice-talk.mjs`) и выбор
   голоса (`plans/11`). **Г4 начата (хостовая половина ✅):** аудио-архитектура ноды исследована
   (research 09 §3.4: STT на устройстве, TTS хоста через talk.speak; Voice Wake на Android пока
   отключён апстримом) и **наш Silero подключён TTS-провайдером ядра** (`tts-local-cli` →
   `voice-say.mjs`, проверено тулом tts) — нода из коробки заговорит нашим голосом. Осталась
   телефонная половина Г4 (🏠 с владельцем) → Г5 клоны (`plans/15`, ждёт 🏠 сэмплы).
2. ✅ Хвосты Фазы 7 закрыты (2026-07-17): deploy.mjs добит (см. пул), манифест знает openclaw и
   голосовой тракт. Открытым из старого: ответы владельца по интервью 001 (Q3–Q4) — если появятся.
   Следующее автономное — поиск свежих моделей (идея 06, просрочен с 03.07, пункт пула).
3. Если владельца нет и вопросов нет — `/autoloop` по пулу выше. Помни: порт 8080 делят
   llama-server и kiwix; docker после перезагрузки контейнеры НЕ поднимает (проверено 2026-07-02);
   GPU может быть занят задачей владельца — проверяй перед бенчами.

---

**2026-07-29 (ночь) — 🗣 ТРАКТ ЗАГОВОРИЛ НА ДВУХ ЯЗЫКАХ + ПОЯВИЛСЯ БЕНЧ, СУДЯЩИЙ ПО ЗВУКУ:**

- **⚠️ Сначала о провале процесса, он важнее фич.** Перестроенный тракт был отдан владельцу как
  «проверенный»: файлы создавались, тайминги печатались, TTFA замерен. **Содержимое звука не
  проверялось ни разу.** Владелец получил сырую работу и сказал прямо: *«прежде чем мне сырое гавно
  отдавать в тесты»*. Лечение — не обещание быть внимательнее, а инструмент (ниже). Урок — EXP-0016.
- **🚨 «Сломанный звук» оказался НЕ дефектом кода.** Владелец слышал кашу («сррс рсрср срр») при верном
  тексте. Три гипотезы по коду не воспроизвелись; воспроизведение ЕГО сессии (агент сам жал Enter,
  играл вопрос в колонки, писал ответ микрофоном) показало **три всплеска речи вместо одного**. Причина:
  параллельно работали ДВЕ голосовые сессии — диалог владельца и звуковой бенч агента, — и обе говорили
  в одни колонки. Кашу устроил агент, гоняя `--audible` тесты на машине человека. Лечение —
  **замок единственной сессии** `tools/voice/single-instance.mjs`. Разбор — `plans/13` («Инцидент»),
  урок — EXP-0017.
- **🧪 `node tools/voice-bench.mjs` — бенч голосового тракта (заказ владельца).** Судит по ЗВУКУ:
  T1 ход завершён · T2 у каждой фразы свой файл · T3 файл не менялся при проигрывании (sha до/после —
  улика затирания) · T4 звук распознан ушами GigaAM и совпал с текстом · T5 кириллица озвучена.
  `--acoustic` замыкает петлю **через микрофон** (идея владельца): играет вслух и слышит комнату.
  Гоняет ТОТ ЖЕ конвейер (`tools/voice/pipeline.mjs`), что и живой диалог. **Норма — 35/35.**
  Охранник проверен на сломанной версии: T3 печатает «ЗАТЁРТ во время воспроизведения».
- **🗣 Двуязычность в одной реплике (требование владельца):** текст режется ПО АЛФАВИТУ, русские куски
  синтезирует Silero `v5_ru`, английские — `v3_en`, склейка в одну дорожку. Проверено: «Эта технология
  называется machine learning» звучит двумя моделями. ⚠️ Грабля: `v3_en.pt` питоном не скачать —
  у `models.silero.ai` истёк SSL-сертификат (Windows цепочку принимает, Python нет); файл добыт
  `Invoke-WebRequest`, лежит в `F:\KLAS\voice\models\`.
- **✅ `bugs/06` ЗАКРЫТ полностью** (`06_DONE_…`): числа произносятся словами (`56` → «пятьдесят
  шесть»), латиница — английской моделью, единицы разворачиваются в полные слова («115 кб/c» →
  «сто пятнадцать килобайт в секунду», требование владельца), markdown вычищается. Молчания больше нет.
- **⚠️ Честная граница:** КАЧЕСТВО английского произношения бенч не проверяет — уши GigaAM понимают
  только русский. Английский пока судит только владелец.

---

**2026-07-28 (ночь) — ⚡ ГОЛОСОВОЙ ТРАКТ УСКОРЕН ВТРОЕ: 24.5 → 7.6–8.0 с (жалоба владельца «всё
медленно» вылечена архитектурно):**

- **🔎 План ошибался в приоритете, и это поймал замер.** План 13 ставил первым стриминг TTS. Сложил
  стадии из его же замера: уши 1.5 + ядро 10.4 + рот 5.0 = **17 с при итоге 24.5 с**. Семь секунд не
  принадлежали ничему. За расхождением нашлись **~11 с перезапуска процессов**: `openclaw agent
  --local` поднимал агента заново КАЖДЫЙ ход (**8.4–10.6 с**), а питон-сайдкар Silero грузил модель
  заново на КАЖДУЮ фразу (**2.2 с**). Цель вдвое крупнее той, куда целился план.
- **📚 Разведка перед кодом (правило владельца): `researches/12_voice_latency_streaming.md`.** Золотой
  стандарт: метрика — **TTFA** (время до первого звука), бюджет облачного хода ~800 мс, но для
  локального CPU/скромной GPU индустрия сама называет 1.2–1.5 с; 90%+ времени петли сидит в
  `LLM TTFT + TTS TTFB`, стриминговый STT почти бесплатен. Приёмы: параллельный конвейер, нарезка по
  границам фраз, модель держать загруженной.
- **✅ Лечение — резидентность, без единой новой зависимости** (Оккам: всё собрано из уже развёрнутого):
  1. **Ядро резидентное.** Нашлось в первой строке справки CLI: `openclaw agent` **без `--local`
     исполняет ход через гейтвей**. Взят ещё более прямой путь — `POST /v1/chat/completions`
     (`stream: true`) прямо на гейтвей: тот же кодовый путь, ноль запусков процессов, SSE-дельты.
     Включается ключом `gateway.http.endpoints.chatCompletions` (внесён и в рабочий конфиг, и в
     git-шаблон `openclaw/openclaw.json.example`).
  2. **Рот резидентный** — новые `tools/voice/silero_daemon.py` + `tools/voice/tts-daemon.mjs`:
     модель Silero живёт в процессе, синтез фразы **3.7 → 0.08–0.11 с** (RTF 0.05).
  3. **Стриминг с нарезкой по предложениям** — сделан, но **честно: даёт лишь 0.1–1 с**. Замер
     объяснил почему: ядро молчит ~5 с (префилл), затем выдаёт весь ответ за полсекунды — перекрывать
     нечего. Оставлен как задел (длинные ответы, barge-in Г4/Г5).
- **📏 Результат наблюдением:** сквозной прогон с ушами — **8.0 и 7.6 с · TTFA 7.9 и 7.5 с**; без ушей
  6.0–7.0 с; живой смоук со звуком пройден; контекст беседы держится (на «а самое большое озеро там?»
  ядро верно поняло «там»). ⚠️ **Первый ход НОВОЙ сессии — 41.9 с** (полный префилл 20k промпта):
  сессию дня не пересоздавать, имеет смысл прогревать.
- **Следующий резерв назван:** остаток ядра ~5 с — это префилл **20-тысячного системного промпта**.
  Голый llama-server на коротком вопросе отвечает за **0.4–0.7 с**, то есть «модель поменьше»
  вычеркнута окончательно (уже второе независимое подтверждение).
- **Опыт:** EXP-0014 (расхождение суммы стадий с итогом — это и есть находка; план прошлой сессии —
  гипотеза, а не приказ) · EXP-0015 (запуск чужого CLI на каждый запрос — почти всегда главный
  источник латентности, и почти всегда у вендора есть резидентный режим).

---

**2026-07-28 (поздний вечер) — ✅ ПЛАН 16 ЗАКРЫТ + голосовой тракт перепроверен на b10167 (и это
принесло два дефекта):**

- **✅ Фаза 4 прогнана НЕ из-под администратора — план 16 получил `DONE`.** Лог доказывает права
  машинно: `IsAdmin = False`; ход — 23 тул-колла, **0 отказов**, `stopReason=stop`, 306.9 с; работали
  `App`, `Wait`, `Snapshot`, `Screenshot`, `Click`, `Type`. **EXP-0007 частично опровергнут:** понизить
  права изнутри админ-сессии МОЖНО — `runas /trustlevel:0x20000` (промпт передавать `--message-file`).
  Прогон писался экраном целиком (645 кадров, `screenrec\2026-07-28T19-00-19_rec`).
- **🐞 `bugs/07` — `Type` искажает символ.** Транскрипт сессии: модель послала `KLAS UIA test` ТРИЖДЫ,
  а в Блокноте осело `KLAS IIA test`. Подтверждено строкой ОС (заголовок окна), не картинкой, — поэтому
  вердикт EXP-0010 «дефекта не было» поправлен: дефект есть, но **перемежающийся** (в прогоне 22:00 тот
  же `Type` сработал верно). Корень не установлен, лечение не применялось.
- **🔧 Тест фазы 4 чинён: он мог проходить по НЕВЕРНОЙ причине.** Блокнот Windows 11 хранит
  несохранённую вкладку между запусками — текст от прошлого прогона уже лежал в редакторе, и критерий
  «текст виден» выполнялся сам собой. Теперь фикстура несёт **уникальный токен прогона**, а вердикт
  сверяется с ОС (`promptUi(token)`, `notepadTitles()`, вердикт `FAIL-UI-UNVERIFIED` в `llamacpp-probe`).
- **🎙 Голосовой тракт: аудио-половины чисты, ядро замедлилось.** `voice-roundtrip` — те же **97%**
  (59/61). А вот ход: **24.5 · 25.7 · 26.7 с** (ядро 10.1–10.8 · уши 1.3–1.7 · рот 4.4–5.3) против базы
  20–22 с (ядро ~7) → **критерий Г3 «<25 с» больше не держится**. Гипотеза «виноваты схемы Windows-MCP»
  ПРОВЕРЕНА И ОТВЕРГНУТА (1.8k токенов из 20.5k). Остались два подозреваемых, разделяемые только A/B на
  стенде: смена билда (b9538→b10167) и смена основной модели (базу мерили до перехода на qwen3.6).
  Важное для оптимизации: в ходе 20 560 токенов промпта (19 576 из кеша) при выходе **4 токена** —
  время уходит НЕ на генерацию, значит «модель поменьше» проблему не решит; бить надо в стриминг TTS.
- **🐞 `bugs/06` — реплика без русских букв роняла ВЕСЬ голосовой ход.** Ядро ответило `56.` → Silero
  кинул `ValueError` → каскад упал стеком. Перебор: `56`, `56.`, `OK.` падают, `Ответ: 56.`,
  `42 плюс 14.`, `Минск.` — нет. **Крах устранён** (охранник кириллицы в `voice-say.mjs`, код выхода 2;
  `voice-talk` переживает случай и продолжает диалог — проверено тем же падавшим прогоном). Полное
  лечение (числа→слова, произношение латиницы) — владельцу: это его же жалоба на двуязычность.
- **Опыт:** EXP-0011 (штатный `runas /trustlevel` против «средствами ОС нельзя») · EXP-0012 (тест,
  чей результат переживает прошлый прогон, проверяет историю → уникальный токен) · EXP-0013 (фикстура
  из десяти русских фраз не проверяет тракт, а проверяет себя).

---

**2026-07-28 (вечер) — 🏆 БЛОКЕР СНЯТ: ядро управляет Windows UI, движок обновлён, бисекция не
понадобилась:**

- **Корень «дефекта движка» оказался НАШИМ.** Встроенный тул OpenClaw `cron` объявляет
  `trigger.script` с `maxLength: 65536` → генератор GBNF выдаёт `char{1,65536}`, а llama.cpp режет
  всё выше `MAX_REPETITION_THRESHOLD = 2000` → `400 … failed to parse grammar` на ЛЮБОМ ходе, даже
  «привет», даже с выключенным MCP. Лечение — одна строка в конфиге ядра:
  `compat.unsupportedToolSchemaKeywords: ["pattern","maxLength","minLength","maxItems","minItems"]`
  (тем же приёмом сам OpenClaw лечит модели xAI). Внесено и в `~/.openclaw/openclaw.json`, и в
  git-шаблон `openclaw/openclaw.json.example`.
- **Движок обновлён: `llamacpp\` = b10167** (28.07.2026). Проверено стендом на обеих границах
  диапазона (b9760 и b10167 — оба PASS после фикса схем), затем в бою: `health-check` зелёный,
  `llama-server --version` = `10167 (ee3d1b54c)`, ход ядра через 8080 отвечает. Откат — `llamacpp-b9538\`.
- **✅ Фаза 4 плана 16 ВЗЯТА:** ядро живьём открыло Блокнот, выполнило `Click` и **напечатало текст**
  (`Type`) — то, что на b9538 убивало ход. Поправка к тесту: новый Блокнот Windows 11 не отдаёт
  область ввода в UIA, поэтому `Type` идёт по `loc` (координаты), а не по `label`.
- **Новые инструменты харнесса:** `tools/llamacpp-probe.mjs` (стенд смены билда: качает релиз, поднимает
  движок на 5900, гоняет НАСТОЯЩИЙ ход ядра; `--ui` — живой UI-тест) и `tools/screen-rec.mjs`
  («зрение» агента: запись экрана 1 кадр/с, идея владельца) — оба в таблице харнесса `AGENT_GUIDE.md`.
- **Канон пополнен правилом владельца:** перед крупной/эпик-фичей — обязательный гуглёж и документ
  разведки в `researches/` (состав документа и признаки эпика — `AGENT_GUIDE.md`).
- **Опыт:** EXP-0009 (первым подозреваемым при «сломалось после обновления» держи свои схемы; читай
  ошибку с НАЧАЛА вывода) · EXP-0010 (зрение врёт на ужатом кадре — для чтения текста только `--crop`
  в нативном масштабе; на этом я выдумал несуществующий дефект ввода, владелец поправил).

---

**2026-07-28 (день) — живой UI-тест прогнан + поиск корня дефекта тул-коллов:**

- **Фаза 4 плана 16 ПРОГНАНА по «go» владельца (4 прогона).** Ядро управления Windows UI доказано
  ЧАСТИЧНО: `App launch_executable` (Блокнот реально открылся — подтвердил владелец), `Wait`,
  `WaitFor`, `Snapshot` (UIA-дерево читается) работают, модель верно взяла label области
  редактирования (**475**). Ход умирает на первом ДЕЙСТВУЮЩЕМ тул-колле (`Type`/`Click`).
- **🔎 Корень: не модель и не Windows-MCP, а движок.** Апстрим
  [llama.cpp#24807](https://github.com/ggml-org/llama.cpp/issues/24807) — ровно наша модель и симптом
  (peg-native роняет XML-тул-колл Qwen3.6) — **закрыт 21.06.2026, рабочий билд после b9754**; наш
  движок собран 06.06.2026 = **b9538**, старше фикса. Это переворачивает диагноз EXP-0003: модель
  выдаёт свой штатный формат, парсер хоста его не умеет.
- **⚠️ Обновление на свежий b10167 НЕ подошло — откачено.** На нём падает ЛЮБОЙ ход ядра
  (`400 Failed to initialize samplers: failed to parse grammar`), даже «привет» и даже с выключенным
  Windows-MCP: генератор GBNF ссылается на неопределённые правила (`value ::= object | array | …`).
  Заведён **`bugs/05_llamacpp_b10167_toolcall_grammar.md`** (улика — грамматика из stderr движка).
  Боевой стек возвращён на b9538 и проверен (ядро отвечает, MCP 7 тулов, `/health` OK); сборка
  b10167 лежит в `F:\KLAS\llamacpp-b10167` для бисекции.
- **✅ Починен ретрай моделей (задание владельца).** `fallbacks` не срабатывали НИ РАЗУ, потому что
  **явный `--model` обнуляет цепочку** (`modelOverrideSource:"user"` → пустой список; прочитано в
  исходниках OpenClaw 2026.7.1 и подтверждено живым прогоном `qwen3.6 → qwen3.6-r → qwen3.5`).
  Правило вписано в `AGENT_GUIDE` и в команду фазы 4 плана 16.
- **Прочее, оплаченное прогоном:** одобрений на MCP-действия при `agent --local` НЕТ (Control UI не
  поднят); понизить права из административной сессии не удалось ни `explorer.exe`, ни COM — лечится
  средой (не держать VS Code от админа); фаззи-`App switch "Notepad"` на русской Windows уводит в
  **Notepad++**, запускать строго по пути .exe.
- **Опыт пополнен 4 записями:** EXP-0005 (`--model` выключает fallback) · EXP-0006 (обновление движка
  проверяй ходом ЯДРА, не curl-ом; держи откат папкой) · EXP-0007 (права/локализованный grep) ·
  EXP-0008 (стенд на отдельном порту + stderr движка называет виновника).

---

**2026-07-18 — наработки, которых не было в документах (внесены задним числом 2026-07-28):**

- **Android-нода OpenClaw подключена и approved** (телефон Krinik's Titan1, Android 16) — но **через
  USB**, не по tailnet: нода → `ws://127.0.0.1:18789` → `adb reverse tcp:18789 tcp:18789` → гейтвей.
  Пейринг `openclaw qr --url ws://127.0.0.1:18789 --setup-code-only`, одобрение `openclaw nodes approve`.
  **Мешали:** конфликт NordVPN ↔ Tailscale (хост offline в tailnet), расхождение часов 6 мин
  (`DEVICE_AUTH_SIGNATURE_EXPIRED`), bootstrap оператора → **`gateway.bind` переведён `tailnet → loopback`**
  (только true-loopback даёт операторский scope). Нода живёт, пока телефон на USB.
- **Windows-MCP v0.8.2 установлен и подключён** (`F:\KLAS\mcp\Windows-MCP-v0.8.2`, uv-бинарь в
  `F:\KLAS\mcp\uv\`, венв Python 3.13.14), allow-list в два слоя = **ровно 7 безопасных тулов**
  (`App, Snapshot, Screenshot, Click, Type, Wait, WaitFor`), опасных нет. План — `plans/16`.
- **⚠️ Токен гейтвея светился локально** (в session-логах и удалённом прокси) — по-хорошему ротировать.
  В git не утекал.

---

**2026-07-26 — 🔧 KAIF обновлён 1.2 → 1.6 «Homeostatic KAIF» ✅ (обёртка проекта, не продукт):**
- **Маршрут:** проект был на 1.2 = без машинерии, поэтому legacy-путь — тонкий `KAIF.md` →
  `KAIF-LOADER.mjs` → механический установщик как ОБНОВЛЕНИЕ. Механический проход чистый: из 64 файлов
  предварительной резервной копии изменились РОВНО 2 (`.kaif/kaif.json`, `package.json`) — контент
  владельца (`GOAL`/`STATUS`/`MASTER_PLAN`/карты/директории знаний) не тронут вообще.
- **Что нового в проекте:** `TESTING_FRAMEWORK.md` (7 принципов + маркеры доверия
  `[NOT-TESTED]`/`[TESTED: …]`), `EXPERIENCE.md` (журнал уроков, засеян 4 реальными уроками KLAS),
  **26 скиллов** (+5: `/experience` и семейство `/fable-method` `/fable-loop` `/fable-judge`
  `/fable-domain` — дисциплина исполнения), `.kaif/spheres/`, машинерия `.kaif/kaif-core.mjs`
  (`npm run kaif:version|check|update`). AGENT_GUIDE получил роутер контекста, артефакты разведки,
  гигиену git, write-гейт и метки `[AI]`; BUG_FIXING — гейт `INTENT:`, проверку `TWINS:`, охранников;
  PHILOSOPHY — «учись один раз», «наблюдение вместо домысла», «три двери».
- **🇷🇺 Решение владельца:** обёртка **остаётся русской** (в 1.6 апстрим держит агентские документы
  по-английски + русские триггер-алиасы). Смыслы 1.3–1.6 перенесены в русский текст вручную. **Цена
  зафиксирована:** 44 из 52 файлов слепка числятся «разошедшимися» → каждое будущее обновление KAIF
  требует РУЧНОГО переноса. Порядок работы — в скилле `/kaif-update` («Особенности ИМЕННО ЭТОГО
  проекта»). Намеренно оставлены английскими и потому обновляются механически: 3 файла
  `fable-method/references/*` и 5 неиспользуемых библиотек сфер (проверено: 8/8 байт-идентичны).
- **Решение владельца:** скиллы развёрнуты под **все пять** агентских систем (`.claude/` `.roo/`
  `.agents/` `.grok/` `.cline/` — 26 × 4 копии + канон). Копии раскладывает машинерия из канона
  `.claude/skills/` (`resyncCopies`), **руками их не правим**.
- **Доп. фиксы KLAS-стороны:** обёртка нормализована к LF (159/159; CRLF-эпоха 1.2 обнуляла бы
  sha-снимки); `.gitignore` расширен на новые артефакты KAIF (`.agents/` `.grok/` `.cline/`
  `.clinerules/` `EXPERIENCE.md` `TESTING_FRAMEWORK.md` + транзиентные) — KAIF по-прежнему вне git;
  **`tools/kaif.mjs` выведен из эксплуатации** (валидатор эпохи 1.2 печатал ложное «структура целостна:
  21 скиллов»); заглушка `<BUILD_COMMAND>` в сфере заполнена; карта проекта обновлена.
- **Проверка:** `npm run kaif:check` → 52 файла + 108 агентских артефактов ✅; `update-verify` пройден
  (113 копий пере-синхронизированы, установщик самоочистился); свой механический аудит — **30/30 ok, 0
  FAIL** (маркер, комплектность 26 скиллов, frontmatter, парность фенсов, литеральные токены
  `INTENT`/`AUTH`/`TWINS`/`PENDING`/`[TESTED:]`/`VERIFIED`, плейсхолдеры, синхронность копий, ссылки).
- **Новый инструмент:** `node tools/kaif-baseline-diff.mjs <версия> [--to <версия>]` — восстанавливает
  базовый слепок любой версии KAIF из релизного артефакта и диффает: показывает объём работы по
  переносу и наши кастомизации. Им же измеряли это обновление (15 изменено · 7 новых · 22 без изменений).
- **📄 Отчёт разработчику KAIF:** `researches/10_kaif_1.6_update_field_report.md` — 10 трудностей
  миграции с предложениями по механизму (главные: legacy-путь не даёт списка расхождений; `templateNotes`
  1.6 описывают 1.5 и не кумулятивны; «откатываемый дифф» через git не работает, когда обёртка
  gitignored; CRLF/LF обнуляет sha-снимки) + что сработало отлично.

---

**2026-07-17 — 🎉 OpenClaw ПРИНЯТ ЯДРОМ АССИСТЕНТА, выход из пилота — ступень 0 выполнена ✅:**
- **Решение владельца:** «принято, выходим из пилота» — Q1 интервью 004 закрыт окончательно.
  Зафиксировано: MASTER_PLAN (журнал + Фаза 8 шаг 1 ✅), research 09 §3.4, plans/08 (Блок 0),
  новый **план выката `plans/10_openclaw_core_rollout.md`** (ступени полномочий по Q4 инт. 004).
- **Ступень 0 развёрнута и проверена:** рабочий (дефолтный) профиль `~/.openclaw/openclaw.json` —
  провайдер klas, fallbacks `[qwen3.5-35b-r, gemma-4-12b]`, compat-фиксы; **exec зажат**
  (`tools.exec.security: allowlist`, `ask: off` — проверено отказом «allowlist miss» на reg query);
  **гейтвей/каналы/cron НЕ подняты**; дисциплина тул-коллов в `~/.openclaw/workspace/TOOLS.md`.
  Смоук: цепочка read→write честная (токен перенесён, 2 колла). Шаблон конфига — `openclaw/` в git
  (`openclaw.json.example` + README). Пилот-профиль оставлен песочницей для бенчей.
- **Ступень 1 выполнена ✅ (интервью 005 отвечено, все A):** гейтвей поднят — bind **только tailnet**
  (100.80.125.66:18789, auth token в `caddy/PASSWORD.local.txt`), Control UI проверен web-shot'ом
  (страница отдаётся, без токена не пускает); exec НЕ эскалирован (allowlist/deny); навыки ужаты до
  shortlist `[healthcheck, skill-creator]` (подтверждено системным промптом); гейтвей встроен в
  `tools/klas.ps1` up/down. Постоянный сервис — опция владельца: `openclaw gateway install`.
  **Видение владельца (Q4):** основной канал будущего — Android-нода с голосовым управлением,
  аутентификация — голос и паттерны владельца → голос-биометрия (plans/09, идея 14) = вход в канал;
  зафиксировано в plans/10 ступень 2. **Следующее по ядру — ступень 2** (Android-нода + Windows-MCP)
  — после голосового тракта (plans/07).

---

**2026-07-17 — мини-бенч цепочек OpenClaw ВЫПОЛНЕН ✅ (развилка «ядро» решена) + `/revision` ✅:**
- **`tools/openclaw-chain-bench.mjs`** — 6 градуированных задач-цепочек внутри агентного цикла
  OpenClaw (`agent --local --json`, профиль `pilot`, свежая сессия на задачу); фикстуры со случайными
  токенами — правильный выход ДОКАЗЫВАЕТ реальное чтение; + `meta.toolSummary`. Запуск:
  `node tools/openclaw-chain-bench.mjs <model>` (⚠️ только на свободном GPU — свапает модели).
- **Результаты (полная таблица и разбор — research 09 §3.4): qwen3.5-35b — 5.00/6.0** (цепочки
  честные; единственный дефект — многострочный write → срыв в XML-псевдо-формат тул-колла, ход
  падает, 2/2 воспроизводимо); **gemma-4-12b — 3.00/6.0** (в чужом цикле обрывает цепочки после
  первого шага, неудачные ходы 140–160 с — fallback в OpenClaw на ней не строить).
- **Вердикт Q1 инт. 004: ядро OpenClaw жизнеспособно с основной qwen3.5-35b.** До «ядра надолго»
  оставалось два условия; **первое закрыто (2026-07-17): write-дефект разобран и обойдён** — корень:
  qwen пишет фразу перед `<tool_call>`, peg-парсер llama.cpp этого не переносит (апстрим #20260,
  фикс не завершён). Меры: сэмплинг qwen прижат (temp 0.7/top-p 0.8/top-k 20/min-p 0 в
  `llama-swap/config.yaml`), дисциплина тул-коллов в TOOLS.md пилота, фейловер на ту же модель через
  алиас `qwen3.5-35b-r` (llama-swap aliases → fallbacks OpenClaw). Замер: 10/11 чистых (~9% флейка
  против 75% до) + сеть 5/5. Детали — research 09 §3.4 «Дожим». **Осталось второе условие:
  безопасность гейтвея (только tailnet, exec-approvals) + решение владельца о выходе из пилота.**
- **`/revision` мастер-плана выполнен:** фазы 1–6 закрыты релизом 1.0 и свёрнуты в историю; новые
  фазы — 7 (самораскрытие до идеала) и 8 (эпик ассистента конструктором из OSS); эволюция ядра LLM
  вынесена в «непрерывные процессы». Пункт «⚠️ /revision» снят.

---

**2026-07-16 — ревизия OSS-поля: «собирать KLAS конструктором из крупных готовых модулей»** (вопрос
владельца) — 🔬 исследование готово: `researches/09_oss_constructor_map.md`. Метод: снимок GitHub API
по ~60 репозиториям (точные звёзды/активность/лицензии) + веб-поиск (fetch-фаза упёрлась в лимит
сессии — сниппет-уровень помечен ⚠️). Ключевое: **OpenClaw** (экс-Clawdbot, 383k⭐, Node/TS, MIT,
навыки-SKILL.md, Android-нода, локальные LLM) — кандидат на готовое ядро ассистента; русский голосовой
тракт собирается из готового (**sherpa-onnx+GigaAM-v3** STT · **Silero v5** TTS-старт · клоны —
CosyVoice 3/Chatterbox V3/F5-TTS_RUSSIAN · каркас **Pipecat**); Windows-контроль — **Windows-MCP**
(6.4k⭐ MIT); Piper/Rhasspy заморожены (Piper → OHF-Voice/piper1-gpl, GPL). Ротацию памяти по лимиту
ГБ никто не даёт — пишем сами. **Интервью 004 отвечено ✅ (2026-07-16):** Q1 — короткий **смоук-пилот
OpenClaw** → выбор ядра на фактах; Q2 — клиент следует за ядром (OpenClaw-бекенд → его Android-нода);
Q3 — слепой A/B TTS-клонов (CosyVoice 3 / Chatterbox V3 / F5-TTS_RUSSIAN), до них Silero v5; Q4 —
полномочия над Windows ступенчато с allow-list. **+ Правило владельца: протухшие OSS не брать — только
живые** (зафиксировано в MASTER_PLAN, AGENT_GUIDE). Решения разнесены: plans/07 (новые дефолты:
Pipecat · sherpa-onnx+GigaAM-v3 · Silero v5), plans/08, idea 05.

**Смоук-пилот OpenClaw выполнен ✅ (2026-07-16, вердикт — research 09 §3.4):** v2026.7.1 поставлен
(`npm i -g openclaw`), изолирован в профиле `pilot` (`~/.openclaw-pilot`), llama-swap подключён
кастомным провайдером. Итог: **работает** — tool calling на основной qwen3.5-35b ✅ (после фикса
`compat.unsupportedToolSchemaKeywords:["pattern"]` — иначе llama.cpp 400 на схемах инструментов),
автофейловер моделей ✅, русский ✅, 53 навыка SKILL.md (18 ready), тёплый ход ~16 с / холодный
60–130 с. ⚠️ Модель срезает шаги цепочки (write без read) → перед решением «ядро надолго» нужен
мини-бенч цепочек в цикле OpenClaw + правила безопасности (гейтвей только tailnet, exec-approvals).
Голосовой realtime это не заменяет — Gateway-каскад (Pipecat) остаётся в плане 07.

---

**2026-07-08 — идея 14: идентичность/доверие/защита владельца** (`ideas/14_...md`) — 🔬 гуглёж-исследование
+ план (задел). Профилирование собеседников, непрерывная модель доверия, идентификация/аутентификация,
**распознавание принуждения (duress)** — Jarvis ставит защиту владельца выше буквального исполнения.
Знания — `researches/08_jarvis_identity_trust.md` (голосовая биометрия ECAPA/SpeechBrain, anti-spoofing
ASVspoof, стилометрия, adaptive risk-based auth, duress/panic-коды). План — `plans/09_jarvis_identity_trust.md`
(6 фаз; каркас доверия можно начать рано). Вписано в эпик `plans/08` (Блок 8). Честная рамка: голос ≠
доказательство (подделывается) → многофакторность + step-up; решения вероятностны; всё локально/зашифрованно.

---

## 🎯 ЯДРО — следующее по порядку (приоритет сейчас, Q1 интервью 003 = фокус на ядре)

1. ✅ **agent-bench v3 сделан (2026-07-08):** `tools/agent-bench-v3.mjs`, 8 градуированных задач.
   Лидер — **gemma-4-12b 7.5/8** (единственная взяла tool-chain с ветвлением на 1.0); рекомендация по
   ядру: gemma-4-12b — дейли-драйвер, Qwythos-9B — спец long-context; ornith исключить (медленный pp).
   Таблица и выводы — `researches/02` (§ «Агентный бенч v3»). Идеи v4 там же.
2. **Мини-бенч цепочек в цикле OpenClaw** (следствие смоука, см. автономный пул ниже) — решает
   развилку «OpenClaw как ядро ассистента» (Q1 интервью 004).
3. **Добить бенч новых кандидатов** — скачать и прогнать `bench-model` + `agent-bench-v2/v3`:
   **Qwen3-Coder-30B-A3B UD-Q5**, **GPT-OSS 20B** (чек-лист «Следующие шаги» в `researches/02`).
   Требует скачивания (~15–20 ГБ) → делать, когда есть место/время; результаты в таблицы `researches/02`.
4. **`/revision` мастер-плана** — MASTER_PLAN отстаёт от реальности: вписать релиз 1.0, фазу
   самораскрытия (идеи 03 + 12), горизонт ассистента (эпик `plans/08`), стратегию «конструктор из
   OSS-модулей» (`researches/09`, частично уже в принципах). Документ, без железа.

---

**2026-07-07 — идея PAI (Психо-Акустический Интерфейс) + выбор личности Jarvis** — 🔬 исследование и
план (реализация НЕ начата, задел на будущее). Кластер документов в `ideas/psycho_acoustic_interface/`:
- **PAI** — подмодуль Jarvis (idea 05): по голосу формировать и играть в наушники психоакустические
  протоколы (сон/фокус/релакс — режим A) и звуковые уведомления (акустический CLI — режим B). Собран
  гуглёж (наука+безопасность+инструменты), причёсана идея, написаны 2 исследования и детальный план
  по 6 фазам (см. `ideas/psycho_acoustic_interface/README.md`). Ключевое: честная рамка «аудио-UX, не
  программирование мозга»; безопасность — обязательный слой; стек — свой генератор на Web Audio/Tone.js.
- **Jarvis (idea 05) дополнен:** **выбор личности** — Jarvis (муж., как у Тони Старка) ↔ **Joi** (жен.,
  по «Бегущему по лезвию 2049»: мягкая, добрая, услужливая; голос девичий, женственный, бархатный).
  Перед реализацией персон/голосов — `/interview`.
- **Связка хост ↔ Android-клиент ↔ носимое устройство** (вопрос владельца): 🔬 исследование
  `researches/07_jarvis_wearable_transport.md` + план `plans/07_jarvis_gateway_and_client.md`.
  Вывод: НЕ «обширный кастомный API», а **тонкий Jarvis Gateway** (realtime-голос WS/WebRTC + маленький
  control-API) поверх **уже развёрнутого Tailscale** и llama-server `/v1`; каскад STT(Whisper)→LLM→TTS(Piper)
  со стримингом+barge-in; on-device wake word на Android; голос только в tailnet (приватность). 5 фаз.
- **Интервью 003 отвечено ✅ (2026-07-07)** (`interviews/interview_003_jarvis_pai_android.md`): решения по
  эпику ассистента. **Q1: фокус на ЯДРЕ, но закладываемся под Jarvis** (в homepage — отдельный web-тул
  управления ассистентом, только для Криника под паролем). **Q2:** дефолт Jarvis, обе персоны на оба
  имени («коллега отдыхает»). **Q3:** PAI — только режим A + модуль **PAI-тренажёр** (калибровка
  «воздействие→отклик», PID-аналогия). **Q4:** Android-клиент — форк готовой базы (HA Companion App).
  **Q5:** голос — только tailnet. **⚠️ Принцип стека:** выбор языка по поддержке/сообществу/скорости/
  простоте (НЕ «Node по умолчанию»): realtime-аудио/ML — вероятно Python, control-API — Node.
- **Эпик-роадмап `plans/08_assistant_epic_roadmap.md`** — высокоуровневый секвенс всего эпика
  (Jarvis/Joi + PAI + носимый доступ) + «закладки в ядро» уже сейчас. Реализация — задел (Q1=A).

---

**2026-07-04 — баг 04: Open WebUI «под ключ» + веб llama-swap** (`bugs/04_openWEBUI_FCK_UP.md`) —
🔧 фикс применён и проверен браузером, ждёт подтверждения владельца:
- **Open WebUI turnkey** — родные регистрируются и СРАЗУ чатятся со всеми моделями, без ручных групп/прав.
  В `docker-compose.yml` (сервис `open-webui`): `ENABLE_PERSISTENT_CONFIG=false` (env — источник правды,
  лечит «БД перекрывает env»), `ENABLE_SIGNUP=true`, `DEFAULT_USER_ROLE=user` (без ожидания одобрения),
  `BYPASS_MODEL_ACCESS_CONTROL=true` (все видят все модели), `USER_PERMISSIONS_WORKSPACE_*_ACCESS=true`.
  Проверено playwright: новый юзер → сразу в чат, видит все 5 моделей (тестовый аккаунт удалён из БД).
- **llama-swap Playground 404** — в `caddy/Caddyfile` матчер `@swap_api` расширен на `/v1/*` и
  `/api/captures*` (Playground шлёт на корень `/v1/chat/completions`, раньше проваливался на homepage →
  404). Проверено: ответ модели 200.
- **llama-swap боковая панель** — не баг KLAS: залипший cookie `sidebar_state=false` (shadcn Sidebar).
  Лечение — очистить куки сайта / кликнуть гамбургер. Владелец подтвердил: почистил куки — панель есть.
- **Новый харнесс** `tools/web-shot.mjs` (playwright/Chromium): скриншот + ошибки консоли/сети — «глаза»
  агента в вебе; первый кирпич «зрения» Jarvis (идея 05). В таблице харнесса `AGENT_GUIDE.md`.

---

**2026-07-04 — баг 02 + идея 10 (управление KLAS):**
- **Баг 02** (`bugs/02_remote_access_via_funnel.md`) — фикс применён + полный homepage-over-funnel:
  **единый вход через Caddy** — локальный `:80` (docker `127.0.0.1:80`, БЕЗ пароля, пульт на
  `http://localhost/`) и публичный `:443` (funnel, basicauth). Общий сниппет `(klas_web)`: `/wiki/`
  (Kiwix), `/ui/` (llama-swap UI, нативный префикс), корень (пульт). Ссылки-виджеты ОТНОСИТЕЛЬНЫЕ —
  работают и локально, и через funnel. **Open WebUI** — root-приложение → отдельный порт funnel
  **:8443** напрямую (своя авторизация). llama-swap тихий фон. Funnel-лимит: только 443/8443/10000,
  но Caddy на 443 разводит сколько угодно сервисов по путям. Всё проверено (200 локально и через funnel).
- **Идея 10** (`ideas/10_start_stop_control.md`) — ✅ реализовано: трей-контроллер `tools/klas.ps1`
  (иконка «Кот Криник», меню Open/Stop, пуш при старте, up/down всего стека), скрытый лаунчер
  `klas-control.vbs`, ярлык `install-klas-shortcut.ps1`, ссылка на репозиторий в homepage
  (anonymize её убирает). **Автозапуск ставит владелец:** `powershell -File tools\install-autostart.ps1`.

---

**2026-07-03 — идея 07 выполнена ПОЛНОСТЬЮ + релиз MVP 0.1** (`ideas/07_DONE_remote_api_access.md`,
`plans/03_remote_and_mvp.md`):
- **Удалённый доступ** к LLM-API через интернет: `https://krinikspc.forest-ratio.ts.net/llm/v1` +
  Bearer-ключ (Caddy `/llm` → llama-swap; ключ в `caddy/PASSWORD.local.txt`; Zoo Code — `homeworks/01`).
- **Контекст расширен** (главный крик владельца): **Qwythos-9B — основная, 256K** (needle 148K ✅,
  agent-bench 6/6); qwen3.6 400 исправлен (→64K); все модели KV q8_0. Модели в `llama-swap/config.yaml`.
- **KAIF 1.2**: маркер, скиллы `/fix-vision` `/what-next`, Zoo Code-адаптер (`.roo/rules/kaif.md`
  авто-подгружает канон — лечит «модели забывают KAIF»).
- **KLAS ≠ KAIF**: KAIF вынесен из git-трекинга (3rd-party dev-фреймворк; файлы на диске, деплой
  умеет `--with-kaif`). См. принцип в `GOAL.md`.
- **Инструменты**: `tools/anonymize.mjs` (анонимный деплой), `tools/leaderboard.mjs` (свежие модели
  из HF API), `tools/sync-zoo-code.mjs`, автостарт llama-swap (`tools/install-autostart.ps1`).
- Исследования: `researches/03` (кванты GGUF), `04` (архитектуры), `05` (датасеты/лидерборды).

Прошлый контекст (Фаза 2 исследование) — в `researches/02_optimal_stack_2026.md`.

**Видение оформлено идеями владельца:**
- `ideas/03_klas_single_unit_self_deploy.md` — KLAS = одна законченная единица; репозиторий —
  обёртка/каркас/фрейм; развёртывание на пустой ПК = `git clone` + одна команда deploy, которая
  **сама скачивает** тяжёлое (движок, модели, docker-образы, .zim).
- `ideas/05_Jarvis.md` — 💡 на будущее (после основы): голосовой человекоподобный ИИ-ассистент
  (навыки-CRUD, управление Windows 11, зрение экрана, память с ротацией, голос Вихрова).

✅ **Опубликовано (2026-07-03):** первый push в origin сделан — https://github.com/MikalaiKryvusha/KLAS
(README с логотипом виден). Установлен GitHub CLI (`gh`), владелец авторизован (аккаунт
MikalaiKryvusha, keyring); git-email = noreply-адрес GitHub (защита приватной почты). Лицензия —
**MIT** (выбор владельца при создании репо; Q2 интервью 001 закрыт).

✅ `/revision` мастер-плана выполнен (2026-07-17) — фаза самораскрытия и горизонт ассистента вписаны.

> `F:\LOCAL_NOMAD` удалён владельцем (2026-07-04) — наследие забыто, вопросов по нему нет.

---

**2026-07-03 — дневной цикл (итерации 1–4):**
- **Баг 01: фикс применён** 🟡 — истинные причины: конфликт порта с kiwix + два дефекта запуска
  bat (полный путь к exe обязателен: `NoDefaultCurrentDirectoryInExePath=1`; CRLF). Модель
  реабилитирована: `n_ctx_train=262144`, стек здоров (68 t/s; 25K промпт за 7.8 с). Профиль:
  `-c 131072`, явный порт 8080; kiwix → host-8081. Ждёт подтверждения владельца в Zoo Code.
- **llama-swap развёрнут** (Фаза 4 частично): прокси на 8080, автозапуск модели по запросу
  (холодный старт 5.5 с), конфиг `llama-swap/config.yaml`. ⚠️ ttl-автовыгрузка ПОКА не
  подтверждена наблюдением — проверяется; автостарт llama-swap при загрузке ПК не настроен.
- **Харнесс:** `tools/health-check.ps1` (одной командой: порт/процессы/health/props/VRAM).
- **План 02** (`plans/02_self_deploy.md`): архитектура «манифест + deploy.mjs», шаги. SHA256
  модели снят: `EE33AB5B…7020` (для манифеста).

| Фаза (по `MASTER_PLAN.md` после `/revision` 2026-07-17) | Статус |
|------|--------|
| 0–6 Фундамент → рабочий KLAS-сервер (= релиз 1.0) | ✅ |
| 7 Самораскрытие «clone + deploy» до идеала | 🔧 хвосты в автономном пуле |
| 8 Эпик ассистента (конструктор из OSS) | 🔬 шаг 1: выбор ядра (мини-бенч OpenClaw) |
| Непрерывные процессы: эволюция ядра LLM · ревизия OSS · гигиена беклога | ♻️ |

---

## 🤖 Пул автономного беклога (не нужен человек / особое железо)

- [x] **Мини-бенч цепочек в цикле OpenClaw (2026-07-17):** qwen3.5-35b **5.00/6.0** (цепочки
      честные; дефект: многострочный write → XML-псевдо-формат тул-колла, воспроизводимо),
      gemma-4-12b 3.00/6.0 (обрывает цепочки в чужом цикле). Вердикт: ядро жизнеспособно с
      qwen3.5-35b. Таблица и разбор — research 09 §3.4. Скрипт: `tools/openclaw-chain-bench.mjs`
      (⚠️ гонять только на свободном GPU — свапает модели).
- [ ] **Поиск свежих моделей (регулярно, идея 06):** раз в 1–2 недели — `node tools/leaderboard.mjs`
      (HF-API лидерборды, подключён в цикл 2026-07-17) + веб-поиск (r/LocalLLaMA, HF trending);
      кандидатов — через `node tools/try-model.mjs <repo> <файл>` и `agent-bench-v3`; результаты в
      таблицу `researches/02`. Последний раз: 2026-07-17 (кандидаты: Qwen3.6-35B-A3B, GPT-OSS 20B).
- [x] **agent-bench v2 (2026-07-07):** `tools/agent-bench-v2.mjs` — 9 дифференцирующих задач.
      Де-сатурировал (разброс 6–7/9 vs 6/6 у v1). Лидеры gemma-4-12b и Qwythos-9B (7/9) сильны в
      РАЗНОМ (gemma — инструкции/JSON; Qwythos — длинный контекст); qwen3.5/qwen3.6/ornith — 6/9;
      ornith медленный (long-ctx 106 с). ВСЕ валят multistep-math (десятичный %). Таблица+выводы —
      `researches/02` (§ «Агентный бенч v2»). Идеи v3 там же. Многофайловая правка/длинная сессия — в v3.
- [ ] **Фаза 2b — локальная проверка кандидатов:** по чек-листу «Следующие шаги» из
      `researches/02_optimal_stack_2026.md` (обновить llama.cpp под sm_120/CUDA 13, бенч
      Qwen3.5-35B-A3B UD-IQ3_S / Qwen3-Coder-30B UD-Q5 / GPT-OSS 20B, поставить llama-swap,
      сравнить Cline vs Zoo Code).
- [x] **deploy.mjs — добит (2026-07-17, Фаза 7):** манифест дополнен новыми компонентами
      (основная model-qwen3.5-35b — её не было!, openclaw npm, voice: venv+Silero v5+GigaAM);
      новые kinds `npm`/`commands`; финальный health-check в конце `--apply` + напоминания
      владельцу. Проверено на живой системе: dry-run — все 11 элементов ✓ (идемпотентность),
      `--apply` — no-op + зелёный health-check. `homepage/config/*.yaml` оказались уже в git
      (хвост закрылся раньше). Развёртывание KLAS = clone + `node tools/deploy.mjs --apply` ✅.
- [x] **Тихий фон + автозапуск через ТРЕЙ (баг 02 + идея 10, 2026-07-04):** llama-swap запускается
      без окна консоли (`llama-swap\start-hidden.vbs`, проверено MainWindowHandle=0, /health 200).
      Автозапуск теперь ставит ВЛАДЕЛЕЦ одной командой `powershell -File tools\install-autostart.ps1`
      — задача Планировщика «KLAS» при входе тихо поднимает весь стек + трей «Кот Криник» + пуш
      (агенту classifier запрещает создавать persistence). ⚠️ Пока владелец не запустил — остаётся
      старая задача «KLAS llama-swap» (только llama-swap, без трея); новый скрипт её снимает.

---

## ❓ Ждёт ревью человека

- ❓ **Интервью 007 — голосовой тракт, четыре развилки (2026-07-29, СВЕЖЕЕ):**
  `interviews/interview_007_voice_next_steps.md`. Q1 уши `punct` · Q2 «один голос» · Q3 Enter и
  приватность · Q4 целевая задержка. Все четыре — уровня видения, агент их не решает.
  🏠 Проверить своим микрофоном: `node tools/voice-talk.mjs --ears punct`

- ✅ **Интервью 006 отвечено и ИСПОЛНЕНО (2026-07-17): основная модель теперь `qwen3.6-35b-a3b`**
  (qwen3.5 — fallback до обкатки; gpt-oss удалён по критерию «≥ gemma»). Проверено смоуками: ядро
  на новой основной (fallback не понадобился), голосовой каскад работает. Видение владельца
  зафиксировано: скорости достаточно — покупаем УМ, эталон Claude Code; gemma «болезненная».
- 🏠 **Домашка ОБНОВЛЕНА: Zoo Code переключить на НОВУЮ основную** — model = `qwen3.6-35b-a3b`
  (API тот же `http://127.0.0.1:8080`, контекст профиля ≤98304). Запасные: `qwen3.5-35b`, `gemma-4-12b`.
- 🏠 **ЕДИНЫЙ вечерний тест-план: `homeworks/02_voice_and_android_tests.md`** — выбор голоса (2 мин)
  + живой микрофонный диалог (3 мин) + Android-нода (вместе с агентом). Результаты — в домашку или чат.
- 🏠 **ВЕЧЕРОМ (владелец, 2026-07-17): голосовые тесты** — оставлено на вечер по слову владельца:
  1) живой микрофонный диалог Г3: `node tools/voice-talk.mjs` (Enter старт/стоп, выход — пустая
  реплика; впечатление → `plans/13`); 2) выбор дефолтного голоса Г1:
  `node tools/voice-say.mjs "Привет, Криник!" --play --voice aidar|baya|kseniya|xenia|eugene`
  (ответ → `plans/11`). До проверки Г1/Г3 стоят в статусе 🟡.

- ✅ Интервью 005 отвечено (2026-07-17, все A): гейтвей в tailnet поднят, exec не эскалирован,
  навыки shortlist, канал — Control UI; видение Q4 (Android-нода + голосовая аутентификация)
  разнесено в plans/10/09. 🏠 **Домашка (по желанию): постоянный сервис гейтвея** —
  `openclaw gateway install` (Scheduled Task от имени владельца; иначе гейтвей поднимает klas.ps1).

- ✅ Интервью 004 отвечено (2026-07-16): смоук-пилот OpenClaw / клиент за ядром / A/B TTS-клонов /
  ступенчатые полномочия Windows + правило живости OSS. Решения разнесены по документам.
- 🏠 **Домашка (появится после пилота): референс-сэмплы голосов** — для A/B-теста клонов (Q3 инт. 004)
  владелец собирает/одобряет сэмплы Вихрова и кандидаток Joi.
- 🏠 **Домашка: переключить Zoo Code на основную модель** — в настройках профиля Zoo Code указать
  model = `qwen3.5-35b` (API остался тот же `http://127.0.0.1:8080`); контекст профиля ≤98304.
  Запасные там же: `gemma-4-12b` (128K, зрение в будущем), `qwen3.6-27b` (32K).
- ✅ Интервью 002 отвечено (2026-07-03): A/A/A — основная Qwen3.5-35B (контекст расширен до 98K
  через KV q8_0), старый квант 16.7 ГБ удалён, MTP-вариант замеряется.

- ✅ **SECURITY закрыто (2026-07-04):** секреты ротированы (новый basicauth-пароль + новый Bearer-ключ,
  проверено: старые → 401, новые → 200); git-история вычищена от утёкшего хеша и force-push в origin.
  Caddyfile вне git (шаблон `Caddyfile.example`); секреты — в `caddy/PASSWORD.local.txt` + `LINKS.local.md`.
- ❓ Интервью 001 — границы автономии (Q3), docker-сервисы (Q4). Q1 и Q2 (MIT) закрыты.

---

## Баги

### Голосовая волна 2026-07-29 (шесть документов за одну сессию)

- ✅ `bugs/08_DONE_voice_mojibake_cp1251.md` — **ЗАКРЫТ, подтверждён владельцем («заработало»).**
  Текст доезжал до Silero моджибейком UTF-8→cp1251; причина доказана побайтово. Охранник — канарейка
  кодировки в `TtsDaemon.ready()`.
- ✅ `bugs/09_DONE_voice_stream_chunking_stall.md` — **ЗАКРЫТ.** Нарезка глохла на коротком первом
  предложении → стриминг обнулялся (1/1 фраза вместо 3/3). Охранник — `T0`.
- 🟡 `bugs/10_stt_no_latin_no_punctuation.md` — **ЖДЁТ ВЕРДИКТА ВЛАДЕЛЬЦА.** Уши не умеют латиницу и
  пунктуацию (доказано алфавитом: 34 токена). Лечение найдено и развёрнуто **опцией**
  `node tools/voice-talk.mjs --ears punct` (тот же GigaAM-v3 от того же автора, 257 токенов).
  Дефолт НЕ переключён: модель ушей — элемент стека. Разведка — `researches/13`.
- ✅ `bugs/11_DONE_tts_voice_switches_gender.md` — **ЗАКРЫТ.** Английский диктор был женским
  (`en_0`, F0 202 Гц); подобран измерением `en_23` (F0 93.8 Гц — как у `eugene`). Охранник —
  `tools/voice/pitch-check.py`.
- ✅ `bugs/12_DONE_bench_greenlights_failed_core_turn.md` — **ЗАКРЫТ.** Бенч ставил 5/5 ходу, который
  провалился. Следующий шаг записан: белый список (`finish_reason: stop`) вместо чёрного.
- ✅ `bugs/13_DONE_tts_silently_drops_digits.md` — **ЗАКРЫТ.** Рот молча глотал цифры на ОБОИХ языках.
  Охранник — `T0b`. Границы: падежи и дробные не согласуются (морфология, осознанно не делалось).
- ✅ `bugs/14_DONE_core_tts_markup_leaks_into_speech.md` — **ЗАКРЫТ.** Разметка `[[tts(...)]]` ядра
  уходила в речь. Охранники — `T0c` (известная форма) + детектор в `T1` (неизвестная).

### Ранее

- ✅ `bugs/06_DONE_voice_tts_needs_cyrillic.md` — **ЗАКРЫТ 2026-07-29.** Реплика без русских букв
  роняла голосовой ход, потом молчала. Теперь: числа словами, английский — своей моделью, единицы
  полными словами. Развилка «как читать латиницу» решена словом владельца в чате, а не интервью.
- 🔬 `bugs/07_windows_mcp_type_char_corruption.md` — `Type` Windows-MCP перемежающеся искажает символ
  (послано `KLAS UIA test`, на экране `KLAS IIA test`). Улики собраны, корень не установлен, лечение
  не применялось. Опасен тем, что молча искажает ввод в чужие приложения.
- ✅ `bugs/05_DONE_llamacpp_toolcall_grammar.md` — **ЗАКРЫТ 2026-07-28.** «Движок ломает грамматику»
  оказалось нашей схемой: тул `cron` просил `char{1,65536}` при пороге llama.cpp 2000. Лечение —
  `compat.unsupportedToolSchemaKeywords`; движок обновлён до b10167, оба критерия годности взяты.
  Апстрим-тикет НЕ заведён (дефекта llama.cpp здесь нет; кандидат — тикет в OpenClaw, по слову владельца).
- 🔧 `bugs/04_openWEBUI_FCK_UP.md` — Open WebUI не «под ключ» + веб llama-swap (панель, Playground 404).
  Фикс применён и проверен браузером (см. «Где мы сейчас»); ждёт подтверждения владельца.

### Ранее закрытые ✅ (2026-07-04)

- ✅ `bugs/01_DONE_context_overflow_zoo_code.md` — переполнение контекста в Zoo Code. Подтверждено
  владельцем: ошибок нет.
- ✅ `bugs/02_DONE_remote_access_via_funnel.md` — удалённый доступ через funnel + краснеющий пульт +
  окно консоли при старте. Подтверждено владельцем: funnel работает, пульт не краснеет, окна нет.
- ✅ `bugs/03_DONE_homepage_links_fck_up.md` — ссылки пульта + Kiwix в чатах. **Закрыто, подтверждено
  владельцем (2026-07-04).** Корень багов 1/3/4 — host-валидация homepage (`HOMEPAGE_ALLOWED_HOSTS`
  без голого `localhost`); фикс — env в `docker-compose.yml`. Баг 2 (чат зашит на funnel) — ссылка
  `/chat`, Caddy редиректит локально→`:3080`, funnel→`:8443`. Баг 5 (llama-swap UI красный) — его
  live-API на КОРНЕ (`/api/events`…) проксируется на llama-swap матчером `@swap_api` в Caddyfile.
- ✅ **Идея 11** (`ideas/11_DONE_kiwix_in_openwebui.md`) — знания Kiwix в чатах Open WebUI. Готово и
  работает: тул `open-webui/tools/kiwix_search.py` (+ импортируемый `kiwix_search.tool.json`), LLM
  сам вызывает `search_local_knowledge` и отвечает по локальной Википедии (проверено). После импорта
  тул надо ВКЛЮЧИТЬ (в чате или привязать к модели: Workspace → Models → Tools).

---

## Что сделано

### 🚀 Релиз 1.0 «Universal KLAS» ✅ (2026-07-04)
- Опубликован https://github.com/MikalaiKryvusha/KLAS/releases/tag/v1.0 (Latest, тег `v1.0`,
  package.json → 1.0.0). README переписан заново (герой `KLAS_latest.jpg`, без истории версий),
  README.pdf перегенерён, release notes `docs/RELEASE_1.0.md`. План — `plans/06_release_1_0.md`.
- В релизе: чат Open WebUI для родных + поиск Kiwix из чатов (идея 11), трей + ярлыки Run/Stop/Panel
  (идеи 10/13), **умный мастер-установщик** с каталогом баз знаний (идея 12, `tools/install.mjs` +
  `tools/lib/*`, план 05), безопасность (секреты ротированы, история вычищена).

### Фаза 0 — Фундамент ✅ (2026-07-02)
- KAIF v1.1 развёрнут; `GOAL.md` → `MASTER_PLAN.md`; аудит окружения
  (`researches/01_local_nomad_audit.md`); git (только `main`); баг 01; интервью 001.

### Фаза 0.5 — Рождение KLAS ✅ (2026-07-02)
- Система живёт в `F:\KLAS\`: корень = git-репозиторий-каркас, инфраструктура — поддиректории вне
  git (`.gitignore`; версионируются только текстовые конфиги: `llamacpp/bat/`, `caddy/Caddyfile`,
  `docker-compose.yml`). Remote `origin` = github.com/MikalaiKryvusha/KLAS.
- Логотип владельца `logo/KLAS.jpg` (OG 1200×630, кот 👍) — hero в `README.md` (стиль KAIF-репо).
- Идея 04 владельца исполнена (2026-07-03): следы разовой стартовой миграции вычищены из документов
  и инструментов — фокус только на видении KLAS.
