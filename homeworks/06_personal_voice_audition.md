# Домашка 06 — Принять свои 50 записей имени «Джарвис»

> **Кому:** владельцу. **От:** агента. **Заведена:** 2026-08-01.
> **Зачем домашка:** «не обкусано ли начало имени» — перцептивный критерий, наблюдатель здесь по
> необходимости человек (`TESTING_FRAMEWORK.md`, класс TASTE). Числа этого не видят: разбор по
> длительности показывает ровный корпус, а срезанный первый звук «Дж» даёт нормальную длительность.
> **Статус:** 🟡 **ЗАПИСАНО И ПРОСЛУШАНО ВЛАДЕЛЬЦЕМ, вердикт не прозвучал.** Чат закрылся раньше.

## Что сделано и что надо услышать

2026-08-01 владелец записал **50 произнесений** имени «Джарвис» своим голосом (стенд
`tools/voice/wakeword-enroll.py`, микрофон BY-V20, 48 кГц). Запись прошла за один заход по сценарию:
20 обычно · 8 вполголоса · 6 быстро · 6 отвернувшись · 6 издалека · 4 с вопросительной интонацией.

Монтаж всех 50 записей проигран владельцу целиком (63 с, блоки разделены паузой). **Вердикт не
получен** — сессия закончилась на другом.

## Вопрос владельцу — один

**Ровно ли звучат записи?** Слушать надо ровно одно: не съедено ли **начало «Дж»** и не оборван ли
**конец «-с»**. Запас по краям сделан НЕсимметричным именно из-за этого (120 мс слева, 180 мс
справа), но проверяется это ухом, а не числом.

Пересобрать прослушку:

```
F:\KLAS\voice\venv-wakeword\Scripts\python.exe tools\voice\wakeword-enroll.py --slug jarvis --audition --play
```

Печатается раскладка по секундам — если что-то не понравится, достаточно назвать **время**, и файл
находится по ней. Самые короткие клипы (0.47 с) стоят на **0:36.5** и **0:54.6**: если резка где-то
обкусала слово, слышно будет там.

**Ответ:**

---

## Что уже известно по числам (это НЕ заменяет ухо)

| | значение |
|---|---|
| принято записей | 50 из 50 |
| медиана длительности | **0.76 с** |
| выбросов после пересборки | **0** |
| клипов улучшено пересборкой резки | 39 из 50 |
| худший клип до/после | 2.95 с → 0.81 с |

Оригиналы в 48 кГц целы (`voice/sources/own_voice/jarvis/`, 50 файлов) — из них учебные клипы
пересобираются командой `--retrim` без участия владельца. Именно поэтому исправление резки, найденное
уже ПОСЛЕ записи, не потребовало переписывать человека.

## Что будет с ответом

- **«ровно»** ⇒ раскладка обучения с личными клипами и переобучение (`plans/23`, шаги 4–6).
- **«вот тут плохо»** ⇒ правится резка, `--retrim` пересобирает всё из оригиналов, прослушка
  повторяется. Перезапись владельцем — только если испорчен сам оригинал.

## Ссылки

`plans/23` (процедура целиком) · `tools/voice/wakeword-enroll.py` · EXP-0059 · `ideas/20` (эта
прослушка должна жить в админской панели — требование владельца)
